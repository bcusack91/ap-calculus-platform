/**
 * Exit Quiz — SAT Command of Evidence
 * 40 randomized questions on identifying textual evidence, evaluating support, and citing evidence
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string; difficulty?: 'easy' | 'medium' | 'hard' }
interface QuestionTemplate { id: string; category: string; difficulty: 'easy' | 'medium' | 'hard'; generate: () => ExitQuizQuestion }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }

const questionPool: QuestionTemplate[] = [
  // Identifying best evidence
  { id: 'ce-q1', category: 'Best Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('The quote that directly supports your answer to the previous question', ['The quote that repeats the most words from the previous question', 'The quote that summarizes the main idea of the entire passage', 'The quote that appears in the same paragraph as the tested claim']); return { id: this.id, category: this.category, question: `On the SAT, "Which choice provides the best evidence?" asks you to find:`, options, correctIndex, explanation: `Best evidence questions ask for the specific text that supports your previous answer.` } } },
  { id: 'ce-q2', category: 'Best Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Consider both questions together, matching each claim to supporting evidence', ['Settle the first question completely, then scan the evidence choices for matching keywords', 'Lock in the first answer, then pick whichever quote sounds most related', 'Choose the longest quote, since it likely contains the most support']); return { id: this.id, category: this.category, question: `The best strategy for paired evidence questions is:`, options, correctIndex, explanation: `Paired questions work together — the evidence should directly support the claim you chose.` } } },
  { id: 'ce-q3', category: 'Best Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('It states or directly implies the point being made', ['It uses sophisticated vocabulary that signals an authoritative tone', 'It appears near the beginning of the passage, where claims are introduced', 'It contains the most facts and details of any choice offered']); return { id: this.id, category: this.category, question: `Evidence is "best" when:`, options, correctIndex, explanation: `Best evidence directly states or clearly implies the answer to the question.` } } },
  { id: 'ce-q4', category: 'Best Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Lines that state the character feels "increasingly uneasy and watchful"', ['Lines describing the setting as dark, cold, and silent at midnight', 'Lines about another character laughing loudly across the room', 'Lines listing the character\'s age, hometown, and family background in detail']); return { id: this.id, category: this.category, question: `If asked for evidence that a character feels anxious, the best choice would be:`, options, correctIndex, explanation: `"Increasingly uneasy and watchful" directly describes the anxiety — best evidence.` } } },
  { id: 'ce-q5', category: 'Best Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Check which evidence option supports one of the first question\'s answer choices', ['Pick the evidence option with the most emotional or vivid language', 'Select evidence from the final paragraph, where conclusions usually appear', 'Choose the evidence option before reading either question closely']); return { id: this.id, category: this.category, question: `When stuck on a paired question, you should:`, options, correctIndex, explanation: `Work backwards — see which evidence option supports one of the answer choices for the first question.` } } },
  // Evaluating strength of evidence
  { id: 'ce-q6', category: 'Evaluating Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Statistical data from a peer-reviewed study', ['A personal anecdote from a single patient', 'An endorsement from a famous actor', 'A rhetorical question posed by the author']); return { id: this.id, category: this.category, question: `Which type of evidence is generally strongest for supporting a scientific claim?`, options, correctIndex, explanation: `Peer-reviewed data is the most rigorous and reliable evidence for scientific claims.` } } },
  { id: 'ce-q7', category: 'Evaluating Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('It is relevant, sufficient, and comes from a credible source', ['It is lengthy, detailed, and fills most of a paragraph of the passage', 'It uses strong emotional language that stirs the reader\'s feelings', 'It restates the same point several times so the reader remembers it']); return { id: this.id, category: this.category, question: `Strong evidence must be:`, options, correctIndex, explanation: `Evidence must be relevant (on topic), sufficient (enough to support), and credible (trustworthy source).` } } },
  { id: 'ce-q8', category: 'Evaluating Evidence', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('It is on topic but does not prove the specific claim', ['It directly contradicts the claim it is supposed to support', 'It proves the claim but is too brief to quote on its own', 'It uses language too simple to count as formal evidence']); return { id: this.id, category: this.category, question: `A common trap in evidence questions: an answer choice is "relevant but not sufficient." This means:`, options, correctIndex, explanation: `The evidence is on-topic but doesn't specifically prove the exact claim being made.` } } },
  { id: 'ce-q9', category: 'Evaluating Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Emotional appeal without factual support', ['Logical reasoning backed by measured data', 'Testimony from a relevant expert', 'Historical precedent from similar cases']); return { id: this.id, category: this.category, question: `Which is the WEAKEST form of evidence?`, options, correctIndex, explanation: `Emotional appeal (pathos) without facts is the weakest — it persuades feelings, not logic.` } } },
  { id: 'ce-q10', category: 'Evaluating Evidence', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Treating a single example as proof of a broad trend', ['Requiring multiple sources to confirm a claim', 'Citing peer-reviewed data to support a claim', 'Checking that evidence is current and relevant']); return { id: this.id, category: this.category, question: `Which reasoning flaw should you watch for when evaluating evidence?`, options, correctIndex, explanation: `A single anecdote or example doesn't prove a general trend — that's hasty generalization.` } } },
  // Citing evidence from different text types
  { id: 'ce-q11', category: 'Text Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Direct quotes and specific actions/dialogue that reveal traits', ['What the reader assumes about the character from personal experience', 'Descriptions of the setting where the story takes place', 'The number of times the character speaks in the story']); return { id: this.id, category: this.category, question: `When citing evidence for character analysis in fiction, use:`, options, correctIndex, explanation: `Direct quotes, actions, and dialogue are concrete evidence of character traits.` } } },
  { id: 'ce-q12', category: 'Text Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Specific data, results, and conclusions from the text', ['Interesting vocabulary the author uses to describe the research', 'The length and level of detail of the methods section', 'General background information about the field\'s history']); return { id: this.id, category: this.category, question: `For a scientific passage, the best evidence includes:`, options, correctIndex, explanation: `Data, results, and expert conclusions are the strongest evidence in science texts.` } } },
  { id: 'ce-q13', category: 'Text Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Examples, statistics, and expert quotes that build the argument', ['Details from the author\'s biography and professional background', 'The publication date and the name of the journal that printed it', 'The number of paragraphs the passage contains']); return { id: this.id, category: this.category, question: `In an argumentative passage, effective evidence consists of:`, options, correctIndex, explanation: `Examples, stats, and expert quotes are the building blocks of an argument.` } } },
  // Quantitative evidence (graphs, tables)
  { id: 'ce-q14', category: 'Quantitative Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('A specific data point from the graph that matches the claim', ['A general trend described in words without any numbers from the graph', 'A data point from the graph that contradicts the passage\'s claim', 'The title of the graph, since it names the variable being measured']); return { id: this.id, category: this.category, question: `When a question asks for graphic evidence to support a claim, choose:`, options, correctIndex, explanation: `The best evidence is a specific, matching data point — not vague trends or contradictory data.` } } },
  { id: 'ce-q15', category: 'Quantitative Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Compare the claim to the actual data and check for accuracy', ['Trust the passage and treat the graphic as decoration', 'Choose the most dramatic data point in the graphic', 'Focus on the graph\'s title rather than its values']); return { id: this.id, category: this.category, question: `When a passage makes a claim and a graph is provided, you should:`, options, correctIndex, explanation: `Always verify claims against the actual data — the graph may support, weaken, or contradict the claim.` } } },
  { id: 'ce-q16', category: 'Quantitative Evidence', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('It contradicts or limits the scope of the author\'s claim', ['It reinforces the claim with additional supporting numbers', 'It measures a different variable than the passage discusses', 'It uses a different type of chart than expected']); return { id: this.id, category: this.category, question: `Sometimes graphic data UNDERMINES a passage claim. This means:`, options, correctIndex, explanation: `Graphics can weaken or limit a claim — data doesn't always support the author's assertion.` } } },
  // Rhetorical evidence
  { id: 'ce-q17', category: 'Rhetorical Evidence', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('To make the argument more convincing through a real-world example', ['To provide statistical proof that the claim holds for the whole population', 'To replace statistics, which persuasive essays should avoid', 'To shift the essay\'s focus away from its central claim']); return { id: this.id, category: this.category, question: `Why might an author include an anecdote as evidence in a persuasive essay?`, options, correctIndex, explanation: `Anecdotes make abstract arguments concrete and relatable, strengthening persuasion — but a single story is not statistical proof.` } } },
  { id: 'ce-q18', category: 'Rhetorical Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('An authority with expertise relevant to the topic', ['A famous public figure widely recognized by readers', 'Someone who already agrees with the author\'s position', 'A respected historical figure from any field of study']); return { id: this.id, category: this.category, question: `An expert citation is most credible when the expert is:`, options, correctIndex, explanation: `Credibility depends on relevant expertise — a biologist on biology, not on economics.` } } },
  // Practice passages
  { id: 'ce-q19', category: 'Practice', difficulty: 'medium', generate() { const passage = `Dr. Hernandez's research team tracked 500 participants over 5 years. Those who read for 30+ minutes daily showed 20% better memory retention and 15% lower rates of cognitive decline compared to non-readers.`; const { options, correctIndex } = makeStringOptions('"showed 20% better memory retention"', ['"tracked 500 participants over 5 years"', '"Those who read for 30+ minutes daily"', '"Dr. Hernandez\'s research team"']); return { id: this.id, category: this.category, question: `"${passage}"\n\nWhich best supports the claim that reading improves cognitive function?`, options, correctIndex, explanation: `The specific statistic about better memory retention directly supports cognitive improvement; the other excerpts describe the study or the group, not the result.` } } },
  { id: 'ce-q20', category: 'Practice', difficulty: 'hard', generate() { const passage = `The town's new recycling program reduced landfill waste by 40% in its first year. However, contamination rates remained high at 25%, meaning one in four items placed in recycling bins was not actually recyclable.`; const { options, correctIndex } = makeStringOptions('"contamination rates remained high at 25%"', ['"reduced landfill waste by 40% in its first year"', '"The town\'s new recycling program reduced landfill waste"', '"in its first year"']); return { id: this.id, category: this.category, question: `"${passage}"\n\nWhich evidence supports the claim that the program still faces challenges?`, options, correctIndex, explanation: `The 25% contamination rate is evidence of an ongoing problem despite the program's success.` } } },
  // Distinguishing evidence types
  { id: 'ce-q21', category: 'Evidence Types', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('An analogy makes a comparison; evidence provides factual support', ['They are interchangeable terms for the same kind of support', 'An analogy offers factual proof, while evidence only illustrates a comparison', 'Evidence must be numerical, while an analogy can be verbal']); return { id: this.id, category: this.category, question: `What distinguishes an analogy from direct evidence?`, options, correctIndex, explanation: `Analogies compare to clarify; evidence directly supports with facts, data, or examples.` } } },
  { id: 'ce-q22', category: 'Evidence Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Empirical evidence (from observation or experiment)', ['Theoretical evidence (from reasoning alone)', 'Testimonial evidence (from eyewitness accounts)', 'Anecdotal evidence (from personal stories)']); return { id: this.id, category: this.category, question: `Which type of evidence is based on direct observation or experimentation?`, options, correctIndex, explanation: `Empirical = from observation/experiment. It is the foundation of the scientific method.` } } },
  // SAT-specific strategies
  { id: 'ce-q23', category: 'SAT Strategy', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Eliminate true statements that don\'t support the specific claim', ['Favor the most general statement, since it covers the most ground', 'Pick the choice with the most words, since it offers the most support', 'Select the choice that sounds the most academic and formal in tone']); return { id: this.id, category: this.category, question: `Key strategy for SAT evidence questions:`, options, correctIndex, explanation: `Many choices are true statements but don't support the specific claim asked about.` } } },
  { id: 'ce-q24', category: 'SAT Strategy', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Work backwards from the evidence choices to test answers', ['Reread the entire passage from the start before choosing anything', 'Guess the first answer, then pick evidence near that guess', 'Pick whichever evidence choice is listed first to save time']); return { id: this.id, category: this.category, question: `If unsure about a paired evidence question, the best approach is:`, options, correctIndex, explanation: `Working backwards—reading each evidence option and seeing which first-question answer it supports—is effective.` } } },
  { id: 'ce-q25', category: 'SAT Strategy', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Reconsider your answer to the first question, which is likely wrong', ['Keep your first answer and choose the evidence that comes closest', 'Skip both questions and return to them only if time remains', 'Reread the evidence choices until one of them seems to fit your answer']); return { id: this.id, category: this.category, question: `If none of the evidence options support your answer to the first question:`, options, correctIndex, explanation: `If no evidence fits, your first-question answer is probably wrong. Re-evaluate both together.` } } },
  // More practice
  { id: 'ce-q26', category: 'Practice', difficulty: 'medium', generate() { const passage = `While social media connects people globally, studies indicate it may increase feelings of isolation. Users who spent more than 3 hours daily on platforms reported 70% higher rates of loneliness than those who spent less than 30 minutes.`; const { options, correctIndex } = makeStringOptions('"reported 70% higher rates of loneliness"', ['"While social media connects people globally"', '"Users who spent more than 3 hours daily on platforms"', '"than those who spent less than 30 minutes"']); return { id: this.id, category: this.category, question: `"${passage}"\n\nBest evidence that social media can harm well-being:`, options, correctIndex, explanation: `The specific statistic (70% higher loneliness) directly supports the harm claim; the other excerpts describe usage groups or benefits.` } } },
  { id: 'ce-q27', category: 'Practice', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A verifiable statement from the text that supports a claim', ['A statement that sounds impressive, whether or not it is accurate', 'A question the author poses to engage the reader', 'An outside fact the reader recalls about the topic']); return { id: this.id, category: this.category, question: `"Textual evidence" on the SAT refers to:`, options, correctIndex, explanation: `Textual evidence is a verifiable statement from the text that supports a particular reading.` } } },
  { id: 'ce-q28', category: 'Best Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The evidence should logically lead to the conclusion in the first answer', ['The evidence can address a different topic than the answer', 'The evidence should be longer than the answer it supports', 'The evidence should come from the passage\'s first paragraph']); return { id: this.id, category: this.category, question: `How should the evidence and the conclusion relate in a paired question?`, options, correctIndex, explanation: `There must be a clear logical connection — the evidence must actually lead to the conclusion.` } } },
  // Counterargument evidence
  { id: 'ce-q29', category: 'Evaluating Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('By refuting opposing evidence or viewpoints', ['By leaving out counterarguments so readers never consider them', 'By dismissing critics without explaining their objections', 'By replacing facts with emotionally charged language']); return { id: this.id, category: this.category, question: `How does an author strengthen an argument?`, options, correctIndex, explanation: `Acknowledging and refuting counterarguments shows thoroughness and strengthens credibility.` } } },
  { id: 'ce-q30', category: 'Evaluating Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Evidence that contradicts or weakens the main claim', ['Evidence that supports the claim through a fresh example', 'The single strongest data point in the argument', 'Background information that sets up the main claim']); return { id: this.id, category: this.category, question: `A "counterexample" is:`, options, correctIndex, explanation: `A counterexample contradicts or weakens a claim — it's evidence against the position.` } } },
  // Additional concept questions
  { id: 'ce-q31', category: 'Evidence Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A survey of 2,000 commuters conducted by the researchers themselves', ['A blog post summarizing several published commuting studies', 'A textbook chapter on commuting that cites the researchers\' work', 'A news article quoting the researchers about their commuting findings']); return { id: this.id, category: this.category, question: 'Researchers claim commute length affects job satisfaction. Which would be a PRIMARY source supporting their claim?', options, correctIndex, explanation: 'Original data collected by the researchers is a primary source; summaries, textbooks, and news coverage are secondary.' } } },
  { id: 'ce-q32', category: 'Text Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Quote or accurately paraphrase it, noting where it appears', ['Summarize the whole passage instead of the relevant lines', 'Restate the idea in your own words without referring to the text', 'Copy the entire paragraph that surrounds the relevant idea']); return { id: this.id, category: this.category, question: `To properly cite textual evidence, you should:`, options, correctIndex, explanation: `Either quote directly or paraphrase accurately, always pointing to the specific text.` } } },
  { id: 'ce-q33', category: 'SAT Strategy', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('About 1-2 minutes per question, including reading time', ['About 5 minutes per question, plus reading time', 'About 30 seconds per question, including reading', 'As long as needed, since there is no time limit']); return { id: this.id, category: this.category, question: `Approximately how much time should you spend on each SAT evidence question?`, options, correctIndex, explanation: `On the digital SAT, each Reading & Writing module gives you 32 minutes for 27 questions — about 71 seconds per question including reading time.` } } },
  { id: 'ce-q34', category: 'Practice', difficulty: 'hard', generate() { const passage = `Deforestation in the Amazon has accelerated by 15% annually. Satellite imagery reveals that an area the size of Connecticut is lost each year. Indigenous communities dependent on the forest report devastating losses of both biodiversity and cultural heritage.`; const { options, correctIndex } = makeStringOptions('"an area the size of Connecticut is lost each year"', ['"Deforestation in the Amazon has accelerated by 15% annually"', '"Indigenous communities dependent on the forest report devastating losses"', '"both biodiversity and cultural heritage"']); return { id: this.id, category: this.category, question: `"${passage}"\n\nWhich provides the strongest visual evidence of deforestation's scale?`, options, correctIndex, explanation: `Comparing to Connecticut's size makes the scale tangible and visually impactful; a growth rate or a description of losses gives no picture of area.` } } },
  { id: 'ce-q35', category: 'Best Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Direct quotation from the text that explicitly addresses the question', ['An inference the reader draws from the passage\'s overall tone', 'A summary of the whole passage in the reader\'s own words', 'Background knowledge the reader brings to the topic']); return { id: this.id, category: this.category, question: `The most compelling textual evidence is usually:`, options, correctIndex, explanation: `Direct, explicit quotes are strongest because they leave no room for misinterpretation.` } } },
  { id: 'ce-q36', category: 'Quantitative Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Read axis labels, title, and units before interpreting data', ['Focus on the tallest bar, since it shows the key result', 'Skip the legend and compare the colors by eye', 'Assume the graph agrees with the passage before reading its values']); return { id: this.id, category: this.category, question: `Before using a graph as evidence, always:`, options, correctIndex, explanation: `Understanding labels, title, and units prevents misreading the data.` } } },
  { id: 'ce-q37', category: 'Rhetorical Evidence', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('To show the consequences of inaction through a real-world example', ['To shift the essay\'s focus from pollution to city budgets', 'To prove that pollution policies fail in most cities', 'To display the author\'s knowledge of urban history']); return { id: this.id, category: this.category, question: `An author cites a case study of a city that failed to address pollution. This evidence serves to:`, options, correctIndex, explanation: `Case studies show real consequences, making the argument concrete and urgent; one city cannot prove a pattern across most cities.` } } },
  { id: 'ce-q38', category: 'Practice', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('The one that most directly answers the question asked', ['The one that seems most interesting or surprising to you', 'The one that includes the most detail and supporting facts', 'The one whose wording appears most often in the passage']); return { id: this.id, category: this.category, question: `When multiple evidence options seem partially correct, choose:`, options, correctIndex, explanation: `Partial support isn't enough — pick the evidence most directly answering the specific question.` } } },
  { id: 'ce-q39', category: 'Evaluating Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Check if the source is current, credible, and relevant to the claim', ['Treat published sources as equally valid regardless of author', 'Prefer older sources, since they have stood the test of time', 'Count the sources cited rather than evaluating their quality or relevance']); return { id: this.id, category: this.category, question: `How should you evaluate the credibility of evidence?`, options, correctIndex, explanation: `Good evidence comes from current, credible, and relevant sources.` } } },
  { id: 'ce-q40', category: 'SAT Strategy', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Underline or mentally note key claims as you read the passage', ['Skip the passage and go straight to the questions', 'Read every word twice to lock in the details', 'Focus on the introduction and conclusion alone']); return { id: this.id, category: this.category, question: `To prepare for evidence questions while reading the passage:`, options, correctIndex, explanation: `Noting key claims as you read makes it faster to locate supporting evidence later.` } } },
]

function selectTemplates(count: number, difficulty?: 'easy' | 'medium' | 'hard'): QuestionTemplate[] {
  if (difficulty) {
    const fillOrder: Record<'easy' | 'medium' | 'hard', ('easy' | 'medium' | 'hard')[]> = { easy: ['easy', 'medium', 'hard'], medium: ['medium', 'easy', 'hard'], hard: ['hard', 'medium', 'easy'] }
    const selected: QuestionTemplate[] = []
    for (const tier of fillOrder[difficulty]) { if (selected.length >= count) break; for (const q of shuffle(questionPool.filter(t => t.difficulty === tier))) { if (selected.length >= count) break; selected.push(q) } }
    return selected
  }
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return selected
}

export function generateExitQuiz(count: number = 10, _topicSlug?: string, difficulty?: 'easy' | 'medium' | 'hard'): ExitQuizQuestion[] {
  return shuffle(selectTemplates(count, difficulty)).map(t => ({ ...t.generate(), difficulty: t.difficulty }))
}
