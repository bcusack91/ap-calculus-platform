/**
 * Exit Quiz — SAT Command of Evidence
 * 40 randomized questions on identifying textual evidence, evaluating support, and citing evidence
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string; difficulty?: 'easy' | 'medium' | 'hard' }
interface QuestionTemplate { id: string; category: string; difficulty: 'easy' | 'medium' | 'hard'; generate: () => ExitQuizQuestion }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const fillers = ['None of the above', 'Cannot be determined', 'None of these']; for (const f of fillers) { if (u.length >= 3) break; if (f !== c && !u.includes(f)) u.push(f) } const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }

const questionPool: QuestionTemplate[] = [
  // Identifying best evidence
  { id: 'ce-q1', category: 'Best Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('The quote that most directly supports the answer to the previous question', ['The longest quote from the passage', 'The first sentence of the passage', 'Any quote the reader finds interesting']); return { id: this.id, category: this.category, question: `On the SAT, "Which choice provides the best evidence?" asks you to find:`, options, correctIndex, explanation: `Best evidence questions ask for the specific text that supports your previous answer.` } } },
  { id: 'ce-q2', category: 'Best Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Answer both questions together — find the evidence that matches a supported claim', ['Answer the first question, then randomly pick evidence', 'Always choose the longest quote', 'Skip evidence questions to save time']); return { id: this.id, category: this.category, question: `The best strategy for paired evidence questions is:`, options, correctIndex, explanation: `Paired questions work together — the evidence should directly support the claim you chose.` } } },
  { id: 'ce-q3', category: 'Best Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('It explicitly states or directly implies the point being made', ['It uses sophisticated vocabulary', 'It appears at the beginning of the passage', 'It contains the most information']); return { id: this.id, category: this.category, question: `Evidence is "best" when:`, options, correctIndex, explanation: `Best evidence directly states or clearly implies the answer to the question.` } } },
  { id: 'ce-q4', category: 'Best Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Lines that state the character feels "increasingly uneasy and watchful"', ['Lines describing the setting as dark', 'Lines about another character smiling', 'Lines listing the character\'s age and background']); return { id: this.id, category: this.category, question: `If asked for evidence that a character feels anxious, the best choice would be:`, options, correctIndex, explanation: `"Increasingly uneasy and watchful" directly describes the anxiety — best evidence.` } } },
  { id: 'ce-q5', category: 'Best Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Check if each evidence option could lead to a valid answer for the first question', ['Choose evidence before reading the questions', 'Pick the evidence with the most emotional language', 'Select evidence from the last paragraph']); return { id: this.id, category: this.category, question: `When stuck on a paired question, you should:`, options, correctIndex, explanation: `Work backwards — see which evidence option supports one of the answer choices for the first question.` } } },
  // Evaluating strength of evidence
  { id: 'ce-q6', category: 'Evaluating Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Statistical data from a peer-reviewed study', ['A personal anecdote', 'A celebrity endorsement', 'A rhetorical question']); return { id: this.id, category: this.category, question: `Which type of evidence is generally strongest for supporting a scientific claim?`, options, correctIndex, explanation: `Peer-reviewed data is the most rigorous and reliable evidence for scientific claims.` } } },
  { id: 'ce-q7', category: 'Evaluating Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('It is relevant, sufficient, and comes from a credible source', ['It is the longest piece of information', 'It uses strong emotional language', 'It repeats the same point multiple times']); return { id: this.id, category: this.category, question: `Strong evidence must be:`, options, correctIndex, explanation: `Evidence must be relevant (on topic), sufficient (enough to support), and credible (trustworthy source).` } } },
  { id: 'ce-q8', category: 'Evaluating Evidence', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('It is related to the topic but does not directly support the specific claim', ['It contradicts the claim', 'It is too short', 'It uses simple language']); return { id: this.id, category: this.category, question: `A common trap in evidence questions: an answer choice is "relevant but not sufficient." This means:`, options, correctIndex, explanation: `The evidence is on-topic but doesn't specifically prove the exact claim being made.` } } },
  { id: 'ce-q9', category: 'Evaluating Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Emotional appeal without factual support', ['Logical reasoning with data', 'Expert testimony', 'Historical precedent']); return { id: this.id, category: this.category, question: `Which is the WEAKEST form of evidence?`, options, correctIndex, explanation: `Emotional appeal (pathos) without facts is the weakest — it persuades feelings, not logic.` } } },
  { id: 'ce-q10', category: 'Evaluating Evidence', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('A single example proves a trend exists', ['Requiring multiple sources before accepting a claim', 'Citing peer-reviewed data to support a claim', 'Checking whether evidence is current and relevant']); return { id: this.id, category: this.category, question: `Which reasoning flaw should you watch for when evaluating evidence?`, options, correctIndex, explanation: `A single anecdote or example doesn't prove a general trend — that's hasty generalization.` } } },
  // Citing evidence from different text types
  { id: 'ce-q11', category: 'Text Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Direct quotes and specific actions/dialogue that reveal traits', ['What the reader assumes about the character', 'Descriptions of the setting', 'The number of times a character speaks']); return { id: this.id, category: this.category, question: `When citing evidence for character analysis in fiction, use:`, options, correctIndex, explanation: `Direct quotes, actions, and dialogue are concrete evidence of character traits.` } } },
  { id: 'ce-q12', category: 'Text Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Specific data, results, and expert conclusions from the text', ['Interesting vocabulary the author uses', 'The length of the methods section', 'General background information']); return { id: this.id, category: this.category, question: `For a scientific passage, the best evidence includes:`, options, correctIndex, explanation: `Data, results, and expert conclusions are the strongest evidence in science texts.` } } },
  { id: 'ce-q13', category: 'Text Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Examples, statistics, and expert quotes the author uses to build the argument', ['The author\'s biography', 'The publication date', 'The passage\'s paragraph count']); return { id: this.id, category: this.category, question: `In an argumentative passage, effective evidence consists of:`, options, correctIndex, explanation: `Examples, stats, and expert quotes are the building blocks of an argument.` } } },
  // Quantitative evidence (graphs, tables)
  { id: 'ce-q14', category: 'Quantitative Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('A specific data point from the graph that aligns with the passage\'s claim', ['A general trend with no numbers', 'Data that contradicts the passage', 'The title of the graph']); return { id: this.id, category: this.category, question: `When a question asks for graphic evidence to support a claim, choose:`, options, correctIndex, explanation: `The best evidence is a specific, matching data point — not vague trends or contradictory data.` } } },
  { id: 'ce-q15', category: 'Quantitative Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Compare the claim to the actual data and check for accuracy', ['Trust the passage and ignore the graphic', 'Choose the most dramatic data point', 'Focus only on the graph\'s title']); return { id: this.id, category: this.category, question: `When a passage makes a claim and a graph is provided, you should:`, options, correctIndex, explanation: `Always verify claims against the actual data — the graph may support, weaken, or contradict the claim.` } } },
  { id: 'ce-q16', category: 'Quantitative Evidence', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('It contradicts or limits the scope of the author\'s claim', ['It always supports the passage', 'It is decorative and can be ignored', 'It replaces the need to read the passage']); return { id: this.id, category: this.category, question: `Sometimes graphic data UNDERMINES a passage claim. This means:`, options, correctIndex, explanation: `Graphics can weaken or limit a claim — data doesn't always support the author's assertion.` } } },
  // Rhetorical evidence
  { id: 'ce-q17', category: 'Rhetorical Evidence', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('To make the argument more convincing by providing real-world examples', ['To increase the word count', 'To confuse the reader', 'To change the topic']); return { id: this.id, category: this.category, question: `Why might an author include an anecdote as evidence in a persuasive essay?`, options, correctIndex, explanation: `Anecdotes make abstract arguments concrete and relatable, strengthening persuasion.` } } },
  { id: 'ce-q18', category: 'Rhetorical Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('An authority whose expertise is relevant to the specific topic', ['Any famous person', 'Someone who agrees with the author', 'A historical figure regardless of field']); return { id: this.id, category: this.category, question: `An expert citation is most credible when the expert is:`, options, correctIndex, explanation: `Credibility depends on relevant expertise — a biologist on biology, not on economics.` } } },
  // Practice passages
  { id: 'ce-q19', category: 'Practice', difficulty: 'medium', generate() { const passage = `Dr. Hernandez's research team tracked 500 participants over 5 years. Those who read for 30+ minutes daily showed 20% better memory retention and 15% lower rates of cognitive decline compared to non-readers.`; const { options, correctIndex } = makeStringOptions('"Those who read for 30+ minutes daily showed 20% better memory retention"', ['"Dr. Hernandez\'s research team tracked 500 participants"', '"compared to non-readers"', '"over 5 years"']); return { id: this.id, category: this.category, question: `"${passage}"\n\nWhich best supports the claim that reading improves cognitive function?`, options, correctIndex, explanation: `The specific statistic about better memory retention directly supports cognitive improvement.` } } },
  { id: 'ce-q20', category: 'Practice', difficulty: 'hard', generate() { const passage = `The town's new recycling program reduced landfill waste by 40% in its first year. However, contamination rates remained high at 25%, meaning one in four items placed in recycling bins was not actually recyclable.`; const { options, correctIndex } = makeStringOptions('"contamination rates remained high at 25%"', ['"reduced landfill waste by 40%"', '"in its first year"', '"placed in recycling bins"']); return { id: this.id, category: this.category, question: `"${passage}"\n\nWhich evidence supports the claim that the program still faces challenges?`, options, correctIndex, explanation: `The 25% contamination rate is evidence of an ongoing problem despite the program's success.` } } },
  // Distinguishing evidence types
  { id: 'ce-q21', category: 'Evidence Types', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('An analogy makes a comparison; evidence provides factual support', ['They are the same thing', 'Analogies are stronger than evidence', 'Evidence is always numerical']); return { id: this.id, category: this.category, question: `What distinguishes an analogy from direct evidence?`, options, correctIndex, explanation: `Analogies compare to clarify; evidence directly supports with facts, data, or examples.` } } },
  { id: 'ce-q22', category: 'Evidence Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Empirical evidence (from observation or experiment)', ['Theoretical evidence (from logic alone)', 'Testimonial evidence (from witnesses)', 'Anecdotal evidence (from personal stories)']); return { id: this.id, category: this.category, question: `Which type of evidence is based on direct observation or experimentation?`, options, correctIndex, explanation: `Empirical = from observation/experiment. It is the foundation of the scientific method.` } } },
  // SAT-specific strategies
  { id: 'ce-q23', category: 'SAT Strategy', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Eliminate options that are true but don\'t support the specific claim in question', ['Choose the most general statement', 'Pick the answer with the most words', 'Select the one that sounds most academic']); return { id: this.id, category: this.category, question: `Key strategy for SAT evidence questions:`, options, correctIndex, explanation: `Many choices are true statements but don't support the specific claim asked about.` } } },
  { id: 'ce-q24', category: 'SAT Strategy', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Work backwards from the evidence choices to validate your answer', ['Read the passage three times', 'Guess the answer to the first question before reading evidence', 'Always pick the first evidence choice']); return { id: this.id, category: this.category, question: `If unsure about a paired evidence question, the best approach is:`, options, correctIndex, explanation: `Working backwards—reading each evidence option and seeing which first-question answer it supports—is effective.` } } },
  { id: 'ce-q25', category: 'SAT Strategy', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Be suspicious — the answer to the first question likely needs to change too', ['Keep your first answer and find any evidence', 'Skip the question', 'Choose the evidence closest to your guess']); return { id: this.id, category: this.category, question: `If none of the evidence options support your answer to the first question:`, options, correctIndex, explanation: `If no evidence fits, your first-question answer is probably wrong. Re-evaluate both together.` } } },
  // More practice
  { id: 'ce-q26', category: 'Practice', difficulty: 'medium', generate() { const passage = `While social media connects people globally, studies indicate it may increase feelings of isolation. Users who spent more than 3 hours daily on platforms reported 70% higher rates of loneliness than those who spent less than 30 minutes.`; const { options, correctIndex } = makeStringOptions('"Users who spent more than 3 hours daily reported 70% higher rates of loneliness"', ['"Social media connects people globally"', '"studies indicate it may increase feelings"', '"less than 30 minutes"']); return { id: this.id, category: this.category, question: `"${passage}"\n\nBest evidence that social media can harm well-being:`, options, correctIndex, explanation: `The specific statistic (70% higher loneliness) directly supports the harm claim.` } } },
  { id: 'ce-q27', category: 'Practice', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A statement that can be verified and directly supports the main argument', ['A statement that sounds impressive', 'Any sentence from the passage', 'A question posed by the author']); return { id: this.id, category: this.category, question: `"Textual evidence" on the SAT refers to:`, options, correctIndex, explanation: `Textual evidence is a verifiable statement from the text that supports a particular reading.` } } },
  { id: 'ce-q28', category: 'Best Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The evidence should logically lead to the conclusion in the first answer', ['The evidence and answer can be about different topics', 'Longer evidence is always better', 'Evidence from the first paragraph is always best']); return { id: this.id, category: this.category, question: `How should the evidence and the conclusion relate in a paired question?`, options, correctIndex, explanation: `There must be a clear logical connection — the evidence must actually lead to the conclusion.` } } },
  // Counterargument evidence
  { id: 'ce-q29', category: 'Evaluating Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('By addressing and refuting opposing evidence or viewpoints', ['By ignoring counterarguments', 'By citing only sources that agree', 'By using emotional language']); return { id: this.id, category: this.category, question: `How does an author strengthen an argument?`, options, correctIndex, explanation: `Acknowledging and refuting counterarguments shows thoroughness and strengthens credibility.` } } },
  { id: 'ce-q30', category: 'Evaluating Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Evidence that contradicts or weakens the main claim', ['Evidence that supports the claim', 'The strongest data point', 'Background information']); return { id: this.id, category: this.category, question: `A "counterexample" is:`, options, correctIndex, explanation: `A counterexample contradicts or weakens a claim — it's evidence against the position.` } } },
  // Additional concept questions
  { id: 'ce-q31', category: 'Evidence Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A survey of 2,000 commuters conducted by the researchers themselves', ['A blog post summarizing other studies', 'A textbook chapter about commuting', 'A news article quoting the researchers']); return { id: this.id, category: this.category, question: 'Researchers claim commute length affects job satisfaction. Which would be a PRIMARY source supporting their claim?', options, correctIndex, explanation: 'Original data collected by the researchers is a primary source; summaries, textbooks, and news coverage are secondary.' } } },
  { id: 'ce-q32', category: 'Text Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Quote it directly or paraphrase it accurately with a citation', ['Summarize the whole passage', 'Use your own words without referencing the text', 'Copy the entire paragraph']); return { id: this.id, category: this.category, question: `To properly cite textual evidence, you should:`, options, correctIndex, explanation: `Either quote directly or paraphrase accurately, always pointing to the specific text.` } } },
  { id: 'ce-q33', category: 'SAT Strategy', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('1-2 minutes per question (with passage reading included in timing)', ['5 minutes per question', '30 seconds per question', 'No time limit on evidence questions']); return { id: this.id, category: this.category, question: `Approximately how much time should you spend on each SAT evidence question?`, options, correctIndex, explanation: `On the digital SAT, each Reading & Writing module gives you 32 minutes for 27 questions — about 71 seconds per question including reading time.` } } },
  { id: 'ce-q34', category: 'Practice', difficulty: 'hard', generate() { const passage = `Deforestation in the Amazon has accelerated by 15% annually. Satellite imagery reveals that an area the size of Connecticut is lost each year. Indigenous communities dependent on the forest report devastating losses of both biodiversity and cultural heritage.`; const { options, correctIndex } = makeStringOptions('"An area the size of Connecticut is lost each year"', ['"Deforestation has accelerated by 15%"', ['"Indigenous communities report losses"', '"Satellite imagery reveals"'][0], '"both biodiversity and cultural heritage"']); return { id: this.id, category: this.category, question: `"${passage}"\n\nWhich provides the strongest visual evidence of deforestation's scale?`, options, correctIndex, explanation: `Comparing to Connecticut's size makes the scale tangible and visually impactful.` } } },
  { id: 'ce-q35', category: 'Best Evidence', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Direct quotation from the text that explicitly addresses the question', ['An inference the reader makes', 'A summary of the whole passage', 'Background knowledge about the topic']); return { id: this.id, category: this.category, question: `The most compelling textual evidence is usually:`, options, correctIndex, explanation: `Direct, explicit quotes are strongest because they leave no room for misinterpretation.` } } },
  { id: 'ce-q36', category: 'Quantitative Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Read axis labels, title, and units before interpreting data', ['Focus only on the tallest bar', 'Ignore the legend', 'Assume the graph supports the passage']); return { id: this.id, category: this.category, question: `Before using a graph as evidence, always:`, options, correctIndex, explanation: `Understanding labels, title, and units prevents misreading the data.` } } },
  { id: 'ce-q37', category: 'Rhetorical Evidence', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('To show the consequences of inaction through a real-world example', ['To fill space', 'To change the subject', 'To show off the author\'s knowledge']); return { id: this.id, category: this.category, question: `An author cites a case study of a city that failed to address pollution. This evidence serves to:`, options, correctIndex, explanation: `Case studies show real consequences, making the argument concrete and urgent.` } } },
  { id: 'ce-q38', category: 'Practice', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('The one that most directly answers the specific question being asked', ['The most interesting answer', 'The one with the most detail', 'The answer that appears multiple times']); return { id: this.id, category: this.category, question: `When multiple evidence options seem partially correct, choose:`, options, correctIndex, explanation: `Partial support isn't enough — pick the evidence most directly answering the specific question.` } } },
  { id: 'ce-q39', category: 'Evaluating Evidence', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Check if the source is current, credible, and relevant to the claim', ['Assume all published sources are equally valid', 'Prefer older sources over newer ones', 'Count the number of sources rather than evaluating quality']); return { id: this.id, category: this.category, question: `How should you evaluate the credibility of evidence?`, options, correctIndex, explanation: `Good evidence comes from current, credible, and relevant sources.` } } },
  { id: 'ce-q40', category: 'SAT Strategy', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Underline or mentally note key claims as you read the passage', ['Skip the passage and go straight to questions', 'Read every word twice', 'Focus only on the introduction and conclusion']); return { id: this.id, category: this.category, question: `To prepare for evidence questions while reading the passage:`, options, correctIndex, explanation: `Noting key claims as you read makes it faster to locate supporting evidence later.` } } },
]

function selectTemplates(count: number, difficulty?: 'easy' | 'medium' | 'hard'): QuestionTemplate[] {
  if (difficulty) {
    const fillOrder: Record<'easy' | 'medium' | 'hard', ('easy' | 'medium' | 'hard')[]> = { easy: ['easy', 'medium', 'hard'], medium: ['medium', 'easy', 'hard'], hard: ['hard', 'medium', 'easy'] }
    const selected: QuestionTemplate[] = []
    for (const tier of fillOrder[difficulty]) { if (selected.length >= count) break; for (const q of shuffle(questionPool.filter(t => t.difficulty === tier))) { if (selected.length >= count) break; selected.push(q) } }
    return selected
  }
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return selected
}

export function generateExitQuiz(count: number = 10, _topicSlug?: string, difficulty?: 'easy' | 'medium' | 'hard'): ExitQuizQuestion[] {
  return shuffle(selectTemplates(count, difficulty)).map(t => ({ ...t.generate(), difficulty: t.difficulty }))
}
