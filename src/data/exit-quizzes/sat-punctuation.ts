/**
 * Exit Quiz — SAT Punctuation
 * 40 randomized questions on commas, semicolons, colons, apostrophes, dashes
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string; difficulty?: 'easy' | 'medium' | 'hard' }
interface QuestionTemplate { id: string; category: string; difficulty: 'easy' | 'medium' | 'hard'; generate: () => ExitQuizQuestion }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const fillers = ['No change', 'Cannot be determined', 'None of these']; for (const f of fillers) { if (u.length >= 3) break; if (f !== c && !u.includes(f)) u.push(f) } const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }
function pick<T>(arr: T[]): T { return arr[Math.floor(Math.random() * arr.length)] }

const questionPool: QuestionTemplate[] = [
  { id: 'pu-q1', category: 'Commas', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('I went to the store, but it was closed.', ['I went to the store but, it was closed.', 'I went to the store but it was closed.', 'I went, to the store but it was closed.']); return { id: this.id, category: this.category, question: `Choose correct comma usage with a coordinating conjunction:`, options, correctIndex, explanation: `Comma before "but" when joining two independent clauses.` } } },
  { id: 'pu-q2', category: 'Commas', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('After finishing dinner, we watched a movie.', ['After finishing dinner we watched a movie.', 'After finishing, dinner we watched a movie.', 'After, finishing dinner we watched a movie.']); return { id: this.id, category: this.category, question: `Choose correct comma usage after an introductory phrase:`, options, correctIndex, explanation: `Use a comma after an introductory adverbial phrase.` } } },
  { id: 'pu-q3', category: 'Commas', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('My sister, who lives in Boston, visits every summer.', ['My sister who lives in Boston, visits every summer.', 'My sister who lives in Boston visits every summer.', 'My sister, who lives in Boston visits every summer.']); return { id: this.id, category: this.category, question: `Correct nonrestrictive clause punctuation:`, options, correctIndex, explanation: `Nonrestrictive clause (extra info) is set off by commas on both sides.` } } },
  { id: 'pu-q4', category: 'Commas', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('We bought apples, oranges, and bananas.', ['We bought apples oranges and bananas.', 'We bought apples, oranges and bananas.', 'We bought, apples, oranges, and bananas.']); return { id: this.id, category: this.category, question: `Which uses the Oxford comma correctly in a list?`, options, correctIndex, explanation: `Oxford comma: comma before "and" in a list of three or more.` } } },
  { id: 'pu-q5', category: 'Commas', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('No comma needed', ['a comma before "that"', 'a comma after "believed"', 'commas around "the earth"']); return { id: this.id, category: this.category, question: `"Scientists once believed that the earth was flat." This sentence needs:`, options, correctIndex, explanation: `No comma before "that" in restrictive clauses.` } } },
  // Semicolons
  { id: 'pu-q6', category: 'Semicolons', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('I love reading; my brother prefers sports.', ['I love reading, my brother prefers sports.', 'I love reading: my brother prefers sports.', 'I love reading. my brother prefers sports.']); return { id: this.id, category: this.category, question: `Join two related independent clauses:`, options, correctIndex, explanation: `Semicolon joins two related independent clauses without a conjunction.` } } },
  { id: 'pu-q7', category: 'Semicolons', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('She studied hard; however, she failed the test.', ['She studied hard, however she failed the test.', 'She studied hard however; she failed the test.', 'She studied hard: however, she failed the test.']); return { id: this.id, category: this.category, question: `Correct use of semicolon with a conjunctive adverb:`, options, correctIndex, explanation: `Semicolon before "however," comma after: clause; however, clause.` } } },
  { id: 'pu-q8', category: 'Semicolons', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('We visited Paris, France; London, England; and Rome, Italy.', ['We visited Paris, France, London, England, and Rome, Italy.', 'We visited Paris France; London England; and Rome Italy.', 'We visited Paris, France: London, England: and Rome, Italy.']); return { id: this.id, category: this.category, question: `Correct semicolon use in a complex list:`, options, correctIndex, explanation: `Use semicolons to separate list items that already contain commas.` } } },
  // Colons
  { id: 'pu-q9', category: 'Colons', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('She needed three things: courage, patience, and luck.', ['She needed three things, courage, patience, and luck.', 'She needed: three things courage, patience, and luck.', 'She needed three things; courage, patience, and luck.']); return { id: this.id, category: this.category, question: `Correct colon usage before a list:`, options, correctIndex, explanation: `Colon after a complete sentence to introduce a list.` } } },
  { id: 'pu-q10', category: 'Colons', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The answer was clear: we had to leave immediately.', ['The answer was clear, we had to leave immediately.', 'The answer was clear; we had to leave immediately.', 'The answer was clear—we had to leave immediately.']); return { id: this.id, category: this.category, question: `A colon can introduce an explanation after a complete sentence. Which is correct?`, options, correctIndex, explanation: `Colon introduces an explanation/amplification after an independent clause. (Dash also acceptable.)` } } },
  { id: 'pu-q11', category: 'Colons', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Do NOT use a colon after "including" or "such as"', ['Always use a colon before a list', 'Use a colon after any verb', 'Colons and semicolons are interchangeable']); return { id: this.id, category: this.category, question: `Important colon rule:`, options, correctIndex, explanation: `Never use a colon after "including," "such as," or "for example" — the clause before a colon must be complete.` } } },
  // Apostrophes
  { id: 'pu-q12', category: 'Apostrophes', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions("the dog's bone", ["the dogs bone", "the dogs' bone", "the dog' bone"]); return { id: this.id, category: this.category, question: `One dog owns a bone: ___`, options, correctIndex, explanation: `Singular possessive: add 's → "the dog's bone."` } } },
  { id: 'pu-q13', category: 'Apostrophes', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions("the students' projects", ["the student's projects", "the students projects", "the students's projects"]); return { id: this.id, category: this.category, question: `Multiple students own projects: ___`, options, correctIndex, explanation: `Plural possessive (plural ending in s): add only apostrophe → "students'."` } } },
  { id: 'pu-q14', category: 'Apostrophes', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions("it's", ['its', "its'", "it is'"]); return { id: this.id, category: this.category, question: `"___ raining outside." (contraction of "it is")`, options, correctIndex, explanation: `"It's" = contraction of "it is." "Its" = possessive.` } } },
  { id: 'pu-q15', category: 'Apostrophes', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions("children's", ["childrens'", "childrens", "children'"]); return { id: this.id, category: this.category, question: `The toys belonging to the children: the ___ toys`, options, correctIndex, explanation: `Irregular plural not ending in s → add 's: "children's."` } } },
  // Dashes
  { id: 'pu-q16', category: 'Dashes', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Three students—Jane, Mark, and Ali—passed.', ['Three students, Jane, Mark, and Ali, passed.', 'Three students; Jane, Mark, and Ali; passed.', 'Three students (Jane, Mark, and Ali) passed.']); return { id: this.id, category: this.category, question: `Use em dashes for an interrupting list:`, options, correctIndex, explanation: `Em dashes set off a parenthetical list that contains commas. (Parentheses also work.)` } } },
  { id: 'pu-q17', category: 'Dashes', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('She had one goal—to win.', ['She had one goal: to win.', 'She had one goal, to win.', 'She had one goal; to win.']); return { id: this.id, category: this.category, question: `Which uses a dash for dramatic emphasis?`, options, correctIndex, explanation: `An em dash can introduce a dramatic or emphatic statement. (Colon also acceptable here.)` } } },
  // Mixed / SAT-style
  { id: 'pu-q18', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('comma splice (incorrect)', ['correct', 'needs a colon', 'needs a dash']); return { id: this.id, category: this.category, question: `"She runs every morning, she enjoys the fresh air." This is a:`, options, correctIndex, explanation: `Two independent clauses joined by only a comma = comma splice (error).` } } },
  { id: 'pu-q19', category: 'SAT-Style', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Add a conjunction: "She runs every morning, and she enjoys the fresh air."', ['Remove the comma entirely', 'Add a colon after "morning"', 'Change "she" to "She"']); return { id: this.id, category: this.category, question: `How to fix: "She runs every morning, she enjoys the fresh air."`, options, correctIndex, explanation: `Fix comma splice: add conjunction, use semicolon, or make two sentences.` } } },
  { id: 'pu-q20', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('However, the results were inconclusive.', ['However the results were inconclusive.', 'However; the results were inconclusive.', 'However: the results were inconclusive.']); return { id: this.id, category: this.category, question: `Correct punctuation when starting a sentence with "However":`, options, correctIndex, explanation: `Comma after introductory "However" when it means "nevertheless."` } } },
  { id: 'pu-q21', category: 'Commas', difficulty: 'medium', generate() { const name = pick(['Dr. Smith', 'Professor Lee', 'Ms. Johnson']); const { options, correctIndex } = makeStringOptions(`${name}, the department chair, announced the results.`, [`${name} the department chair announced the results.`, `${name} the department chair, announced the results.`, `${name}, the department chair announced the results.`]); return { id: this.id, category: this.category, question: `Punctuate the appositive: "${name} the department chair announced the results."`, options, correctIndex, explanation: `Appositive ("the department chair") set off by commas on both sides.` } } },
  { id: 'pu-q22', category: 'Semicolons', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('No — the clause before must be independent', ['Yes, always', 'Yes, if followed by a list', 'Only after "therefore"']); return { id: this.id, category: this.category, question: `Can you use a semicolon after a dependent clause?`, options, correctIndex, explanation: `Semicolons join independent clauses only.` } } },
  { id: 'pu-q23', category: 'Colons', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The study revealed an unexpected finding: the control group outperformed the test group.', ['The study revealed: an unexpected finding.', 'The study: revealed an unexpected finding.', 'The study revealed, an unexpected finding.']); return { id: this.id, category: this.category, question: `Correct colon usage for elaboration:`, options, correctIndex, explanation: `Complete sentence before the colon, with an explanation after.` } } },
  { id: 'pu-q24', category: 'Apostrophes', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions("The Smiths' house", ["The Smith's house", "The Smiths's house", "The Smiths house"]); return { id: this.id, category: this.category, question: `The house belonging to the Smith family:`, options, correctIndex, explanation: `Family name pluralized (Smiths) + apostrophe for possession: "The Smiths' house."` } } },
  { id: 'pu-q25', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('period or semicolon', ['comma only', 'no punctuation', 'a colon']); return { id: this.id, category: this.category, question: `Two independent clauses with no conjunction need what between them?`, options, correctIndex, explanation: `Independent clauses need a period, semicolon, or comma + conjunction.` } } },
  { id: 'pu-q26', category: 'Commas', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('No comma is needed', ['Comma after "tall"', 'Comma after "the"', 'Comma after "man"']); return { id: this.id, category: this.category, question: `"The tall young man entered the room." Comma needed?`, options, correctIndex, explanation: `No comma between cumulative adjectives (tall young). Test: "tall AND young" sounds awkward → no comma.` } } },
  { id: 'pu-q27', category: 'Dashes', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('They can replace commas, parentheses, or colons for emphasis', ['They are only for lists', 'They replace periods', 'They are informal and never used on the SAT']); return { id: this.id, category: this.category, question: `On the SAT, em dashes are used because:`, options, correctIndex, explanation: `Dashes are versatile: they add emphasis while functioning like commas, parentheses, or colons.` } } },
  { id: 'pu-q28', category: 'SAT-Style', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Use the same punctuation on both sides', ['Use a comma and a dash', 'Use a dash and a parenthesis', 'No rules apply']); return { id: this.id, category: this.category, question: `When punctuating a parenthetical element, the SAT expects you to:`, options, correctIndex, explanation: `Matching punctuation: two commas, two dashes, or two parentheses — never mix.` } } },
  { id: 'pu-q29', category: 'Commas', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('On January 15, 2024, the event took place.', ['On January 15 2024, the event took place.', 'On January 15, 2024 the event took place.', 'On January, 15, 2024, the event took place.']); return { id: this.id, category: this.category, question: `Punctuate the date correctly:`, options, correctIndex, explanation: `Commas after the day and after the year when the date appears mid-sentence.` } } },
  { id: 'pu-q30', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A sentence fragment', ['A run-on sentence', 'A comma splice', 'A correct sentence']); return { id: this.id, category: this.category, question: `"Running through the park on a sunny day." This is:`, options, correctIndex, explanation: `No subject and no main verb → sentence fragment.` } } },
  { id: 'pu-q31', category: 'Semicolons', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('therefore', ['and', 'but', 'because']); return { id: this.id, category: this.category, question: `Which word typically follows a semicolon? "She studied; ___, she passed."`, options, correctIndex, explanation: `Conjunctive adverbs (therefore, however, moreover) follow semicolons.` } } },
  { id: 'pu-q32', category: 'Colons', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('It must be an independent clause', ['It can be any phrase', 'It must be a question', 'It must contain a list']); return { id: this.id, category: this.category, question: `What must come BEFORE a colon?`, options, correctIndex, explanation: `The clause before a colon must be able to stand alone as a complete sentence.` } } },
  { id: 'pu-q33', category: 'Apostrophes', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions("who's", ['whose', "whos", "who'se"]); return { id: this.id, category: this.category, question: `"___ coming to the party?" (contraction)`, options, correctIndex, explanation: `"Who's" = "who is." "Whose" = possessive.` } } },
  { id: 'pu-q34', category: 'SAT-Style', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('The company—founded in 1990—has grown rapidly.', ['The company, founded in 1990, has grown rapidly.', 'The company (founded in 1990) has grown rapidly.', 'All three are acceptable']); return { id: this.id, category: this.category, question: `Which is the MOST emphatic way to set off the parenthetical?`, options, correctIndex, explanation: `Dashes add the most emphasis. All three options are grammatically correct, but dashes emphasize most.` } } },
  { id: 'pu-q35', category: 'Commas', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Do not use a comma', ['Use a comma before "and"', 'Use a semicolon', 'Use a colon']); return { id: this.id, category: this.category, question: `"She sings and dances." Two verbs joined by "and" with one subject:`, options, correctIndex, explanation: `Compound predicate (not compound sentence) → no comma before "and."` } } },
  { id: 'pu-q36', category: 'SAT-Style', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Restrictive — no commas needed', ['Nonrestrictive — add commas', 'Always add commas with "that"', 'Neither needs punctuation']); return { id: this.id, category: this.category, question: `"Students that study regularly perform better." The clause "that study regularly" is:`, options, correctIndex, explanation: `"That" introduces restrictive clauses → no commas. "Which" introduces nonrestrictive → commas.` } } },
  { id: 'pu-q37', category: 'Dashes', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('To set off a sudden break or change in thought', ['To end a sentence', 'To replace a period', 'To introduce a formal list']); return { id: this.id, category: this.category, question: `Em dashes are primarily used:`, options, correctIndex, explanation: `Em dashes signal a break, interruption, or emphasis.` } } },
  { id: 'pu-q38', category: 'Apostrophes', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions("its", ["it's", "its'", "their"]); return { id: this.id, category: this.category, question: `"The cat licked ___ paw." (possessive)`, options, correctIndex, explanation: `"Its" = possessive. No apostrophe. "It's" = "it is."` } } },
  { id: 'pu-q39', category: 'Semicolons', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('The weather was cold; we stayed inside.', ['The weather was cold, we stayed inside.', 'The weather was cold: we stayed inside.', 'The weather was cold we stayed inside.']); return { id: this.id, category: this.category, question: `Choose the correct punctuation between these clauses:`, options, correctIndex, explanation: `Two independent, related clauses → semicolon works perfectly.` } } },
  { id: 'pu-q40', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Delete the comma — no pause needed there', ['Add another comma', 'Replace with semicolon', 'Keep the comma']); return { id: this.id, category: this.category, question: `"The scientists, discovered a new element." Fix:`, options, correctIndex, explanation: `No comma between subject and verb. Remove the comma.` } } },
]

function selectTemplates(count: number, difficulty?: 'easy' | 'medium' | 'hard'): QuestionTemplate[] {
  if (difficulty) {
    const fillOrder: Record<'easy' | 'medium' | 'hard', ('easy' | 'medium' | 'hard')[]> = { easy: ['easy', 'medium', 'hard'], medium: ['medium', 'easy', 'hard'], hard: ['hard', 'medium', 'easy'] }
    const selected: QuestionTemplate[] = []
    for (const tier of fillOrder[difficulty]) { if (selected.length >= count) break; for (const q of shuffle(questionPool.filter(t => t.difficulty === tier))) { if (selected.length >= count) break; selected.push(q) } }
    return selected
  }
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return selected
}

export function generateExitQuiz(count: number = 10, _topicSlug?: string, difficulty?: 'easy' | 'medium' | 'hard'): ExitQuizQuestion[] {
  return shuffle(selectTemplates(count, difficulty)).map(t => ({ ...t.generate(), difficulty: t.difficulty }))
}
