/**
 * Exit Quiz — SAT Conciseness and Redundancy
 * 40 randomized questions on eliminating redundancy, wordiness, tautology, active vs passive, combining
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string; difficulty?: 'easy' | 'medium' | 'hard' }
interface QuestionTemplate { id: string; category: string; difficulty: 'easy' | 'medium' | 'hard'; generate: () => ExitQuizQuestion }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const fillers = ['No change', 'Cannot be determined', 'None of these']; for (const f of fillers) { if (u.length >= 3) break; if (f !== c && !u.includes(f)) u.push(f) } const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }
function pick<T>(arr: T[]): T { return arr[Math.floor(Math.random() * arr.length)] }

const questionPool: QuestionTemplate[] = [
  // Eliminating Redundancy
  { id: 'cr-q1', category: 'Eliminating Redundancy', difficulty: 'easy', generate() { const phrase = pick(['true fact', 'past history', 'free gift', 'advance warning', 'final outcome']); const fix = phrase === 'true fact' ? 'fact' : phrase === 'past history' ? 'history' : phrase === 'free gift' ? 'gift' : phrase === 'advance warning' ? 'warning' : 'outcome'; const { options, correctIndex } = makeStringOptions(fix, [phrase, `very ${fix}`, `actual ${fix}`]); return { id: this.id, category: this.category, question: `"${phrase}" is redundant. What is the concise version?`, options, correctIndex, explanation: `"${fix}" already implies the modifier, making "${phrase}" redundant.` } } },
  { id: 'cr-q2', category: 'Eliminating Redundancy', difficulty: 'medium', generate() { const sentence = pick(['She is a woman who is very talented and skilled.', 'The project is completely finished and done.', 'They gathered together in a group.']); const fix = sentence.includes('woman') ? 'She is very talented.' : sentence.includes('project') ? 'The project is finished.' : 'They gathered.'; const { options, correctIndex } = makeStringOptions(fix, [sentence, sentence.replace('.', ' indeed.'), sentence.replace('.', ' really.')]); return { id: this.id, category: this.category, question: `Fix the redundancy: "${sentence}"`, options, correctIndex, explanation: 'Remove words that repeat the same idea without adding new information.' } } },
  { id: 'cr-q3', category: 'Eliminating Redundancy', difficulty: 'easy', generate() { const phrase = pick(['each and every', 'first and foremost', 'one and only']); const fix = phrase === 'each and every' ? 'every' : phrase === 'first and foremost' ? 'first' : 'only'; const { options, correctIndex } = makeStringOptions(fix, [phrase, `the ${fix}`, `truly ${fix}`]); return { id: this.id, category: this.category, question: `"${phrase}" — concise replacement?`, options, correctIndex, explanation: `"${phrase}" uses two words for one idea — "${fix}" suffices.` } } },
  { id: 'cr-q4', category: 'Eliminating Redundancy', difficulty: 'easy', generate() { const phrase = pick(['close proximity', 'unexpected surprise', 'brief summary', 'added bonus', 'usual custom']); const fix = phrase === 'close proximity' ? 'proximity' : phrase === 'unexpected surprise' ? 'surprise' : phrase === 'brief summary' ? 'summary' : phrase === 'added bonus' ? 'bonus' : 'custom'; const { options, correctIndex } = makeStringOptions(fix, [phrase, `very ${fix}`, `real ${fix}`]); return { id: this.id, category: this.category, question: `Fix: "The store was in ___ to the highway." or similar. What replaces "${phrase}"?`, options, correctIndex, explanation: `"${fix}" inherently means "${phrase}" — the modifier is redundant.` } } },
  { id: 'cr-q5', category: 'Eliminating Redundancy', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The report summarizes the main points.', ['The report gives a brief summary of the main and important points.', 'The report provides a summarization of the key and essential main points.', 'The report gives an overview summary of the major main points.']); return { id: this.id, category: this.category, question: 'Which is the most concise version?', options, correctIndex, explanation: 'Eliminate redundant modifiers and wordy constructions — say it once, clearly.' } } },
  { id: 'cr-q6', category: 'Eliminating Redundancy', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('The experiment concluded.', ['The experiment came to a final conclusion.', 'The experiment reached its concluding end.', 'The experiment was concluded and finished.']); return { id: this.id, category: this.category, question: 'Which is most concise?', options, correctIndex, explanation: '"Concluded" already means "came to an end" — no need for extra words.' } } },
  // Wordiness
  { id: 'cr-q7', category: 'Wordiness', difficulty: 'easy', generate() { const wordy = pick(['in order to', 'for the purpose of', 'with the intention of', 'so as to']); const { options, correctIndex } = makeStringOptions('to', [wordy, 'in an effort to', 'with the goal of']); return { id: this.id, category: this.category, question: `Concise replacement for "${wordy}"?`, options, correctIndex, explanation: `"To" replaces "${wordy}" without losing meaning.` } } },
  { id: 'cr-q8', category: 'Wordiness', difficulty: 'easy', generate() { const wordy = pick(['at this point in time', 'at the present moment', 'in today\'s modern world']); const fix = wordy.includes('point') ? 'now' : wordy.includes('present') ? 'now' : 'today'; const { options, correctIndex } = makeStringOptions(fix, [wordy, `currently ${fix}`, `at this time, ${fix}`]); return { id: this.id, category: this.category, question: `Concise replacement for "${wordy}"?`, options, correctIndex, explanation: `"${fix}" says in one word what "${wordy}" says in many.` } } },
  { id: 'cr-q9', category: 'Wordiness', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('because', ['due to the fact that', 'owing to the fact that', 'by virtue of the fact that']); return { id: this.id, category: this.category, question: 'Concise replacement for "due to the fact that"?', options, correctIndex, explanation: '"Because" = one word. "Due to the fact that" = five unnecessary words.' } } },
  { id: 'cr-q10', category: 'Wordiness', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Using more words than necessary to express an idea', ['Using complex vocabulary', 'Writing long paragraphs', 'Using passive voice']); return { id: this.id, category: this.category, question: 'What is "wordiness"?', options, correctIndex, explanation: 'Wordiness = expressing an idea in more words than necessary.' } } },
  { id: 'cr-q11', category: 'Wordiness', difficulty: 'medium', generate() { const wordy = pick(['the reason why is because', 'it is important to note that', 'what I mean to say is']); const fix = wordy === 'the reason why is because' ? 'because' : wordy === 'it is important to note that' ? '[delete — state the point directly]' : '[delete — state the point directly]'; const { options, correctIndex } = makeStringOptions(fix, [wordy, `clearly, ${wordy}`, `indeed, ${wordy}`]); return { id: this.id, category: this.category, question: `How do you fix: "${wordy}"?`, options, correctIndex, explanation: `"${wordy}" is filler — remove it or replace with a concise alternative.` } } },
  // Tautology
  { id: 'cr-q12', category: 'Tautology', difficulty: 'easy', generate() { const phrase = pick(['round circle', 'empty void', 'burning fire', 'frozen ice', 'sharp point']); const fix = phrase === 'round circle' ? 'circle' : phrase === 'empty void' ? 'void' : phrase === 'burning fire' ? 'fire' : phrase === 'frozen ice' ? 'ice' : 'point'; const { options, correctIndex } = makeStringOptions(fix, [phrase, `a ${phrase}`, `the ${phrase} itself`]); return { id: this.id, category: this.category, question: `"${phrase}" is a tautology. Concise version?`, options, correctIndex, explanation: `A ${fix} is inherently ${phrase.split(' ')[0]} — the adjective is a tautology.` } } },
  { id: 'cr-q13', category: 'Tautology', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('collaborated', ['collaborated together', 'collaborated jointly together', 'mutually collaborated together']); return { id: this.id, category: this.category, question: '"The two labs ___ on the vaccine trial." Which choice is most concise?', options, correctIndex, explanation: '"Collaborated" already means working together — adding "together" or "jointly" is redundant.' } } },
  { id: 'cr-q14', category: 'Tautology', difficulty: 'hard', generate() { const phrase = pick(['completely unanimous', 'totally destroyed', 'absolutely essential']); const fix = phrase === 'completely unanimous' ? 'unanimous' : phrase === 'totally destroyed' ? 'destroyed' : 'essential'; const { options, correctIndex } = makeStringOptions(fix, [phrase, `very ${fix}`, `quite ${fix}`]); return { id: this.id, category: this.category, question: `"${phrase}" — remove the tautology:`, options, correctIndex, explanation: `"${fix}" is already absolute — it can't be more or less. The modifier is redundant.` } } },
  // Unnecessary Phrases
  { id: 'cr-q15', category: 'Unnecessary Phrases', difficulty: 'medium', generate() { const phrase = pick(['as a matter of fact', 'it goes without saying that', 'needless to say']); const { options, correctIndex } = makeStringOptions('Delete it and state the point directly', [`Keep "${phrase}" for emphasis`, 'Move it to the end', 'Add more context around it']); return { id: this.id, category: this.category, question: `"${phrase}" appears before a claim. What should you do?`, options, correctIndex, explanation: `"${phrase}" is filler — if it goes without saying, don't say it. Delete and state directly.` } } },
  { id: 'cr-q16', category: 'Unnecessary Phrases', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The rain delayed the game.', ['What happened was that the rain delayed the game.', 'The thing that happened was that the rain delayed the game.', 'It was the case that the rain delayed the game.']); return { id: this.id, category: this.category, question: 'Which is most concise?', options, correctIndex, explanation: 'Eliminate "what happened was," "the thing is," and "it was the case" — get to the point.' } } },
  { id: 'cr-q17', category: 'Unnecessary Phrases', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Basically and essentially — they rarely add meaning', ['Always keep transition words', 'Add "in my opinion" before every claim', 'Use "very" liberally']); return { id: this.id, category: this.category, question: 'Which words are commonly unnecessary fillers?', options, correctIndex, explanation: '"Basically," "essentially," "literally" (misused), and "very" are often unnecessary.' } } },
  { id: 'cr-q18', category: 'Unnecessary Phrases', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('She believes the policy works.', ['She is of the belief that the policy works.', 'She holds the opinion and belief that the policy works.', 'It is her belief that the policy in question works.']); return { id: this.id, category: this.category, question: 'Most concise version?', options, correctIndex, explanation: '"Believes" replaces "is of the belief that" in one word.' } } },
  // Active vs Passive
  { id: 'cr-q19', category: 'Active vs Passive', difficulty: 'medium', generate() { const passive = pick(['The report was written by the team.', 'The decision was made by the committee.', 'The experiment was conducted by the researchers.']); const active = passive.includes('report') ? 'The team wrote the report.' : passive.includes('decision') ? 'The committee made the decision.' : 'The researchers conducted the experiment.'; const { options, correctIndex } = makeStringOptions(active, [passive, passive.replace('.', ' quickly.'), passive.replace('.', ' recently.')]); return { id: this.id, category: this.category, question: `Convert to active voice: "${passive}"`, options, correctIndex, explanation: 'Active voice (subject does the action) is more concise and direct than passive voice.' } } },
  { id: 'cr-q20', category: 'Active vs Passive', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('The committee approved the proposal.', ['The proposal was approved by the committee.', 'The proposal was given approval by the committee.', 'Approval of the proposal was made by the committee.']); return { id: this.id, category: this.category, question: 'Which choice expresses the idea most directly?', options, correctIndex, explanation: 'The active construction is shortest and clearest; the passive versions add words without adding meaning.' } } },
  { id: 'cr-q21', category: 'Active vs Passive', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('When the actor is unknown, unimportant, or deliberately concealed', ['Always', 'Never', 'Only in formal writing']); return { id: this.id, category: this.category, question: 'When is passive voice appropriate?', options, correctIndex, explanation: 'Passive voice is acceptable when the actor is unknown or the focus is on the receiver.' } } },
  { id: 'cr-q22', category: 'Active vs Passive', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The researcher analyzed the data.', ['The data was analyzed by the researcher.', 'The data was subjected to analysis by the researcher.', 'Analysis of the data was performed by the researcher.']); return { id: this.id, category: this.category, question: 'Which is most concise?', options, correctIndex, explanation: 'Active voice: subject (researcher) + verb (analyzed) + object (data) — fewest words.' } } },
  // Combining Sentences
  { id: 'cr-q23', category: 'Combining Sentences', difficulty: 'medium', generate() { const s1 = pick(['The study was thorough.', 'The film was short.', 'The plan was ambitious.']); const s2 = s1.includes('study') ? 'The study was well-designed.' : s1.includes('film') ? 'The film was powerful.' : 'The plan was risky.'; const combined = s1.includes('study') ? 'The study was thorough and well-designed.' : s1.includes('film') ? 'The film was short but powerful.' : 'The plan was ambitious but risky.'; const { options, correctIndex } = makeStringOptions(combined, [`${s1} ${s2}`, `${s1} Also, ${s2}`, `${s1} In addition, ${s2}`]); return { id: this.id, category: this.category, question: `Combine concisely: "${s1}" and "${s2}"`, options, correctIndex, explanation: 'Combining with a conjunction eliminates the repetitive subject and creates a tighter sentence.' } } },
  { id: 'cr-q24', category: 'Combining Sentences', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('It reduces word count while preserving meaning', ['It always makes sentences longer', 'It changes the meaning', 'It is only for creative writing']); return { id: this.id, category: this.category, question: 'Why does combining sentences improve conciseness?', options, correctIndex, explanation: 'Combining eliminates repeated words and creates more efficient prose.' } } },
  { id: 'cr-q25', category: 'Combining Sentences', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Marie Curie, who discovered radium, won two Nobel Prizes.', ['Marie Curie discovered radium. Marie Curie won two Nobel Prizes.', 'Marie Curie discovered radium. She also won two Nobel Prizes, Marie Curie.', 'Marie Curie, she discovered radium, and she won two Nobel Prizes.']); return { id: this.id, category: this.category, question: 'Which combination is most concise?', options, correctIndex, explanation: 'A relative clause ("who discovered radium") efficiently adds information without repeating the name.' } } },
  // SAT-Style
  { id: 'cr-q26', category: 'SAT-Style', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('DELETE the underlined portion', ['Keep it for clarity', 'Replace with a longer phrase', 'Move it to the end']); return { id: this.id, category: this.category, question: '"She was happy and joyful about the results." The SAT underlines "and joyful." Best choice?', options, correctIndex, explanation: '"Happy" and "joyful" are redundant — delete "and joyful."' } } },
  { id: 'cr-q27', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('The shortest answer that preserves the full meaning', ['The longest, most detailed answer', 'The answer with the most adjectives', 'The answer that sounds most impressive']); return { id: this.id, category: this.category, question: 'On SAT conciseness questions, what should you generally choose?', options, correctIndex, explanation: 'The SAT rewards conciseness — choose the shortest option that preserves full meaning.' } } },
  { id: 'cr-q28', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('annually', ['on a yearly basis', 'once a year, every year', 'every single year without exception']); return { id: this.id, category: this.category, question: '"The event takes place ___." Most concise option?', options, correctIndex, explanation: '"Annually" = one word replacing a wordy phrase.' } } },
  { id: 'cr-q29', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('predicted', ['made a prediction about', 'gave a prediction regarding', 'offered a prediction concerning']); return { id: this.id, category: this.category, question: '"The analyst ___ the market would recover." Most concise?', options, correctIndex, explanation: 'One strong verb ("predicted") replaces a wordy phrase.' } } },
  { id: 'cr-q30', category: 'SAT-Style', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Look for answers that use one precise word instead of a wordy phrase', ['Always pick the longest option', 'Look for answers with the most commas', 'Pick the answer with passive voice']); return { id: this.id, category: this.category, question: 'What pattern helps identify concise answers on the SAT?', options, correctIndex, explanation: 'One precise verb or noun often replaces a wordy phrase — look for this pattern.' } } },
  { id: 'cr-q31', category: 'SAT-Style', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('She asked about the policy.', ['She asked a question about and regarding the policy.', 'She made an inquiry with respect to the policy.', 'She asked about and inquired regarding the policy.']); return { id: this.id, category: this.category, question: 'Most concise version?', options, correctIndex, explanation: '"Asked about" is direct and concise — no need for "inquiry," "with respect to," etc.' } } },
  { id: 'cr-q32', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('despite', ['despite the fact that', 'in spite of the fact that', 'regardless of the fact that']); return { id: this.id, category: this.category, question: 'Concise replacement for "despite the fact that"?', options, correctIndex, explanation: '"Despite" (one word) replaces "despite the fact that" (four words).' } } },
  { id: 'cr-q33', category: 'Wordiness', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('The results were significant.', ['The results that were achieved were of significant importance.', 'In terms of the results, they were significant in nature.', 'As far as the results go, they were of a significant nature.']); return { id: this.id, category: this.category, question: 'Most concise version?', options, correctIndex, explanation: 'Strip away "in terms of," "in nature," "as far as...go" — they add no meaning.' } } },
  { id: 'cr-q34', category: 'Tautology', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('She returned to her seat.', ['She returned back to her seat.', 'She went back and returned to her seat.', 'She returned back again to her seat.']); return { id: this.id, category: this.category, question: 'Fix the tautology:', options, correctIndex, explanation: '"Returned" already means "went back" — "back" is redundant.' } } },
  { id: 'cr-q35', category: 'Unnecessary Phrases', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('I think the plan will succeed.', ['In my personal opinion, I think that the plan will succeed.', 'I am of the opinion that I think the plan will succeed.', 'Personally speaking, in my opinion, the plan will succeed, I think.']); return { id: this.id, category: this.category, question: 'Most concise version?', options, correctIndex, explanation: '"I think" already implies personal opinion — no need for "in my opinion" or "personally."' } } },
  { id: 'cr-q36', category: 'Eliminating Redundancy', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('She repeated the process.', ['She repeated the process again.', 'She again repeated the process once more.', 'She repeated the process over again.']); return { id: this.id, category: this.category, question: 'Fix the redundancy:', options, correctIndex, explanation: '"Repeated" already means "did again" — "again" and "once more" are redundant.' } } },
  { id: 'cr-q37', category: 'Active vs Passive', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The students completed the assignment on time.', ['The assignment was completed on time by the students.', 'The assignment was able to be completed on time by the students.', 'Completion of the assignment was done on time by the students.']); return { id: this.id, category: this.category, question: 'Most concise version?', options, correctIndex, explanation: 'Active voice with a clear subject-verb-object structure is most concise.' } } },
  { id: 'cr-q38', category: 'Combining Sentences', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('The talented and experienced engineer solved the problem.', ['The engineer was talented. The engineer was experienced. The engineer solved the problem.', 'The engineer, who was talented and who was experienced, solved the problem.', 'Being talented and also experienced, it was the engineer who solved the problem.']); return { id: this.id, category: this.category, question: 'Most concise combination?', options, correctIndex, explanation: 'Stack adjectives before the noun to combine efficiently.' } } },
  { id: 'cr-q39', category: 'Wordiness', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('although', ['despite the fact that', 'in light of the fact that', 'taking into consideration that']); return { id: this.id, category: this.category, question: 'Concise replacement for "despite the fact that" (as a conjunction)?', options, correctIndex, explanation: '"Although" is one word replacing a multi-word phrase.' } } },
  { id: 'cr-q40', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('The key SAT skill is identifying words that add no new information and removing them', ['Every word in a sentence is important', 'Longer sentences score better', 'The SAT never tests conciseness']); return { id: this.id, category: this.category, question: 'What is the core conciseness skill tested on the SAT?', options, correctIndex, explanation: 'The SAT frequently tests your ability to identify and remove unnecessary words.' } } },
]

function selectTemplates(count: number, difficulty?: 'easy' | 'medium' | 'hard'): QuestionTemplate[] {
  if (difficulty) {
    const fillOrder: Record<'easy' | 'medium' | 'hard', ('easy' | 'medium' | 'hard')[]> = { easy: ['easy', 'medium', 'hard'], medium: ['medium', 'easy', 'hard'], hard: ['hard', 'medium', 'easy'] }
    const selected: QuestionTemplate[] = []
    for (const tier of fillOrder[difficulty]) { if (selected.length >= count) break; for (const q of shuffle(questionPool.filter(t => t.difficulty === tier))) { if (selected.length >= count) break; selected.push(q) } }
    return selected
  }
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return selected
}

export function generateExitQuiz(count: number = 10, _topicSlug?: string, difficulty?: 'easy' | 'medium' | 'hard'): ExitQuizQuestion[] {
  return shuffle(selectTemplates(count, difficulty)).map(t => ({ ...t.generate(), difficulty: t.difficulty }))
}
