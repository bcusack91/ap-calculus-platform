/**
 * Exit Quiz — SAT Transitions and Organization
 * 40 randomized questions on transition words, sentence placement, paragraph organization
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string }
interface QuestionTemplate { id: string; category: string; generate: () => ExitQuizQuestion }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const fillers = ['No change', 'Cannot be determined', 'None of these']; for (const f of fillers) { if (u.length >= 3) break; if (f !== c && !u.includes(f)) u.push(f) } const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }
function pick<T>(arr: T[]): T { return arr[Math.floor(Math.random() * arr.length)] }

const questionPool: QuestionTemplate[] = [
  // Cause/Effect Transitions
  { id: 'to-q1', category: 'Cause/Effect Transitions', generate() { const transition = pick(['Therefore', 'Consequently', 'As a result']); const scenario = pick(['the experiment failed, so the team redesigned it', 'funding increased, so the project expanded', 'pollution rose, so regulations were tightened']); const { options, correctIndex } = makeStringOptions(transition, ['However', 'Furthermore', 'Meanwhile']); return { id: this.id, category: this.category, question: `"The data clearly showed a decline. ___, the committee recommended changes." Which transition fits?`, options, correctIndex, explanation: `"${transition}" signals cause and effect — the data caused the recommendation.` } } },
  { id: 'to-q2', category: 'Cause/Effect Transitions', generate() { const { options, correctIndex } = makeStringOptions('because', ['although', 'furthermore', 'instead']); return { id: this.id, category: this.category, question: '"The roads were closed ___ the storm made travel dangerous." Which word shows cause?', options, correctIndex, explanation: '"Because" directly signals a cause-effect relationship.' } } },
  { id: 'to-q3', category: 'Cause/Effect Transitions', generate() { const { options, correctIndex } = makeStringOptions('thus', ['nevertheless', 'similarly', 'for example']); return { id: this.id, category: this.category, question: '"The species lost its habitat. ___, its population declined sharply." Best transition?', options, correctIndex, explanation: '"Thus" signals a logical result or consequence.' } } },
  { id: 'to-q4', category: 'Cause/Effect Transitions', generate() { const { options, correctIndex } = makeStringOptions('They show that one event or idea leads to another', ['They show contrast', 'They add unrelated information', 'They signal time order']); return { id: this.id, category: this.category, question: 'What do cause/effect transitions signal?', options, correctIndex, explanation: 'Cause/effect transitions (therefore, consequently, as a result) show one thing causes another.' } } },
  { id: 'to-q5', category: 'Cause/Effect Transitions', generate() { const { options, correctIndex } = makeStringOptions('Since the budget was cut, several programs were eliminated.', ['Since the budget was cut. Several programs were eliminated.', 'Since, the budget was cut several programs were eliminated.', 'The budget was cut, since several programs were eliminated.']); return { id: this.id, category: this.category, question: 'Which sentence correctly uses "since" to show cause?', options, correctIndex, explanation: '"Since" as a causal conjunction introduces the dependent clause before the result.' } } },
  // Contrast Transitions
  { id: 'to-q6', category: 'Contrast Transitions', generate() { const transition = pick(['However', 'Nevertheless', 'On the other hand']); const { options, correctIndex } = makeStringOptions(transition, ['Therefore', 'In addition', 'For example']); return { id: this.id, category: this.category, question: '"The study found positive results. ___, critics raised concerns about methodology." Best transition?', options, correctIndex, explanation: `"${transition}" signals a contrast or unexpected shift.` } } },
  { id: 'to-q7', category: 'Contrast Transitions', generate() { const { options, correctIndex } = makeStringOptions('although', ['because', 'moreover', 'consequently']); return { id: this.id, category: this.category, question: '"___ the evidence was strong, the jury remained unconvinced." Which word signals contrast?', options, correctIndex, explanation: '"Although" introduces a concession — a contrast between evidence and the jury\'s reaction.' } } },
  { id: 'to-q8', category: 'Contrast Transitions', generate() { const { options, correctIndex } = makeStringOptions('In contrast', ['Similarly', 'In addition', 'As a result']); return { id: this.id, category: this.category, question: '"Urban areas saw growth. ___, rural populations declined." Best transition?', options, correctIndex, explanation: '"In contrast" highlights a difference between urban and rural trends.' } } },
  { id: 'to-q9', category: 'Contrast Transitions', generate() { const { options, correctIndex } = makeStringOptions('however, nevertheless, on the other hand, although, despite, yet', ['therefore, consequently, as a result', 'first, second, finally', 'for example, such as, including']); return { id: this.id, category: this.category, question: 'Which set contains contrast transitions?', options, correctIndex, explanation: 'Contrast transitions signal an opposing or unexpected idea.' } } },
  { id: 'to-q10', category: 'Contrast Transitions', generate() { const { options, correctIndex } = makeStringOptions('Despite the rain, the event continued as planned.', ['Despite the rain. The event continued as planned.', 'Despite, the rain the event continued as planned.', 'The event continued as planned despite, the rain.']); return { id: this.id, category: this.category, question: 'Which sentence correctly uses "despite"?', options, correctIndex, explanation: '"Despite" is a preposition followed by a noun phrase — no comma needed directly after it.' } } },
  // Addition Transitions
  { id: 'to-q11', category: 'Addition Transitions', generate() { const transition = pick(['Furthermore', 'Moreover', 'In addition']); const { options, correctIndex } = makeStringOptions(transition, ['However', 'Instead', 'As a result']); return { id: this.id, category: this.category, question: '"The study confirmed the hypothesis. ___, it revealed unexpected secondary effects." Best transition?', options, correctIndex, explanation: `"${transition}" adds information that builds on the previous point.` } } },
  { id: 'to-q12', category: 'Addition Transitions', generate() { const { options, correctIndex } = makeStringOptions('also', ['but', 'yet', 'instead']); return { id: this.id, category: this.category, question: '"She excels in math. She ___ performs well in science." Which word adds information?', options, correctIndex, explanation: '"Also" signals addition — more information that supports the same point.' } } },
  { id: 'to-q13', category: 'Addition Transitions', generate() { const { options, correctIndex } = makeStringOptions('They add supporting or related information', ['They show a contrasting idea', 'They signal a conclusion', 'They indicate time order']); return { id: this.id, category: this.category, question: 'What do addition transitions do?', options, correctIndex, explanation: 'Addition transitions (furthermore, moreover, also, in addition) add related information.' } } },
  { id: 'to-q14', category: 'Addition Transitions', generate() { const { options, correctIndex } = makeStringOptions('Additionally', ['Conversely', 'Finally', 'Instead']); return { id: this.id, category: this.category, question: '"The program improved reading skills. ___, it boosted student confidence." Best transition?', options, correctIndex, explanation: '"Additionally" adds a second benefit to the first one mentioned.' } } },
  // Sequence Transitions
  { id: 'to-q15', category: 'Sequence Transitions', generate() { const { options, correctIndex } = makeStringOptions('First, next, then, finally', ['However, but, yet, although', 'Therefore, consequently, thus', 'For example, such as, including']); return { id: this.id, category: this.category, question: 'Which set of transitions shows sequential order?', options, correctIndex, explanation: 'Sequence transitions (first, next, then, finally) show order of events or steps.' } } },
  { id: 'to-q16', category: 'Sequence Transitions', generate() { const step = pick(['First', 'Next', 'Then', 'Finally']); const { options, correctIndex } = makeStringOptions(step, ['However', 'Therefore', 'For example']); return { id: this.id, category: this.category, question: `"___, the researchers gathered data from 500 participants." If this is the first step, which transition?`, options: (() => { const r = makeStringOptions('First', ['However', 'Therefore', 'For example']); return r.options })(), correctIndex: (() => { const r = makeStringOptions('First', ['However', 'Therefore', 'For example']); return r.correctIndex })(), explanation: '"First" signals the beginning of a sequence of steps.' } } },
  { id: 'to-q17', category: 'Sequence Transitions', generate() { const { options, correctIndex } = makeStringOptions('Meanwhile', ['Therefore', 'However', 'For instance']); return { id: this.id, category: this.category, question: '"The scientists analyzed the samples. ___, a second team prepared additional tests." Best transition?', options, correctIndex, explanation: '"Meanwhile" shows two events happening at the same time.' } } },
  { id: 'to-q18', category: 'Sequence Transitions', generate() { const { options, correctIndex } = makeStringOptions('subsequently', ['conversely', 'alternatively', 'instead']); return { id: this.id, category: this.category, question: '"The bill was passed by the Senate. ___, the president signed it into law." Best transition?', options, correctIndex, explanation: '"Subsequently" means "after that" — signaling the next event in sequence.' } } },
  // Conclusion Transitions
  { id: 'to-q19', category: 'Conclusion Transitions', generate() { const transition = pick(['In conclusion', 'To summarize', 'Ultimately']); const { options, correctIndex } = makeStringOptions(transition, ['Furthermore', 'However', 'For example']); return { id: this.id, category: this.category, question: '"___, the evidence supports the hypothesis that exercise improves cognitive function." Best transition for a concluding sentence?', options, correctIndex, explanation: `"${transition}" signals a summary or final point.` } } },
  { id: 'to-q20', category: 'Conclusion Transitions', generate() { const { options, correctIndex } = makeStringOptions('In short', ['In addition', 'On the contrary', 'Meanwhile']); return { id: this.id, category: this.category, question: '"___, the policy failed to achieve its intended goals." Which signals a conclusion?', options, correctIndex, explanation: '"In short" concisely wraps up the preceding argument.' } } },
  { id: 'to-q21', category: 'Conclusion Transitions', generate() { const { options, correctIndex } = makeStringOptions('They signal a summary, final point, or wrap-up of the argument', ['They introduce new information', 'They show contrast', 'They add examples']); return { id: this.id, category: this.category, question: 'What do conclusion transitions do?', options, correctIndex, explanation: 'Conclusion transitions (in conclusion, ultimately, in short) wrap up the argument.' } } },
  { id: 'to-q22', category: 'Conclusion Transitions', generate() { const { options, correctIndex } = makeStringOptions('All things considered', ['In the beginning', 'Moving on', 'Specifically']); return { id: this.id, category: this.category, question: 'Which phrase serves as a conclusion transition?', options, correctIndex, explanation: '"All things considered" weighs everything discussed and leads to a concluding point.' } } },
  // Sentence Placement
  { id: 'to-q23', category: 'Sentence Placement', generate() { const { options, correctIndex } = makeStringOptions('Read the sentences before and after each possible position to check logical flow', ['Always put it at the beginning', 'Always put it at the end', 'Put it wherever there is a blank line']); return { id: this.id, category: this.category, question: 'How do you determine the best placement for a sentence within a paragraph?', options, correctIndex, explanation: 'Check logical flow by reading surrounding sentences at each possible position.' } } },
  { id: 'to-q24', category: 'Sentence Placement', generate() { const { options, correctIndex } = makeStringOptions('After the sentence that introduces the concept it supports', ['At the very beginning of the passage', 'At the very end regardless of content', 'Before the introductory sentence']); return { id: this.id, category: this.category, question: 'A sentence provides a specific example. Where should it most likely be placed?', options, correctIndex, explanation: 'Examples should follow the claim or concept they illustrate.' } } },
  { id: 'to-q25', category: 'Sentence Placement', generate() { const { options, correctIndex } = makeStringOptions('Look for pronouns, transitions, and logical connections that link it to surrounding text', ['Count the number of words', 'Look for the shortest paragraph', 'Choose randomly']); return { id: this.id, category: this.category, question: 'What clues help determine where a sentence belongs?', options, correctIndex, explanation: 'Pronouns (it, they), transitions (however, moreover), and topic continuity reveal placement.' } } },
  { id: 'to-q26', category: 'Sentence Placement', generate() { const { options, correctIndex } = makeStringOptions('Before the sentence that begins with a pronoun referring to it', ['After the concluding paragraph', 'Before the title', 'In a footnote']); return { id: this.id, category: this.category, question: 'A sentence introduces "Dr. Smith." A later sentence starts with "She then..." Where does the first sentence go?', options, correctIndex, explanation: 'A noun must appear before a pronoun that refers to it — place the introduction first.' } } },
  // Paragraph Organization
  { id: 'to-q27', category: 'Paragraph Organization', generate() { const { options, correctIndex } = makeStringOptions('Topic sentence → supporting details → concluding/transition sentence', ['Random arrangement of ideas', 'Conclusion first, then evidence', 'Only supporting details, no topic sentence']); return { id: this.id, category: this.category, question: 'What is the standard organization within a body paragraph?', options, correctIndex, explanation: 'Paragraphs typically follow: topic sentence → evidence/details → concluding transition.' } } },
  { id: 'to-q28', category: 'Paragraph Organization', generate() { const { options, correctIndex } = makeStringOptions('It introduces the paragraph\'s main point and connects to the thesis', ['It summarizes the entire essay', 'It provides a specific statistic', 'It always asks a question']); return { id: this.id, category: this.category, question: 'What role does a topic sentence play in paragraph organization?', options, correctIndex, explanation: 'The topic sentence states the paragraph\'s main point and ties it to the overall argument.' } } },
  { id: 'to-q29', category: 'Paragraph Organization', generate() { const { options, correctIndex } = makeStringOptions('Look at how each paragraph\'s topic connects to the next', ['Paragraphs should be alphabetically ordered', 'The longest paragraph always goes first', 'Order doesn\'t matter']); return { id: this.id, category: this.category, question: 'How do you determine if paragraphs are in the best order?', options, correctIndex, explanation: 'Each paragraph should logically connect to and build on the previous one.' } } },
  { id: 'to-q30', category: 'Paragraph Organization', generate() { const { options, correctIndex } = makeStringOptions('It doesn\'t connect to the paragraph\'s topic sentence or main idea', ['It is the shortest sentence', 'It contains a transition word', 'It uses difficult vocabulary']); return { id: this.id, category: this.category, question: 'How can you tell if a sentence should be deleted from a paragraph?', options, correctIndex, explanation: 'A sentence that doesn\'t support the paragraph\'s main idea should be removed.' } } },
  // SAT-Style
  { id: 'to-q31', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('For instance', ['However', 'In conclusion', 'On the contrary']); return { id: this.id, category: this.category, question: '"Many species have adapted to urban environments. ___, pigeons thrive in cities worldwide." Best transition?', options, correctIndex, explanation: '"For instance" introduces a specific example of the general claim.' } } },
  { id: 'to-q32', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('Read the sentence before and after the transition to understand the relationship', ['Choose the fanciest word', 'Pick the shortest transition', 'Always choose "however"']); return { id: this.id, category: this.category, question: 'What is the best strategy for transition questions on the SAT?', options, correctIndex, explanation: 'Understanding the relationship between surrounding sentences determines the correct transition.' } } },
  { id: 'to-q33', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('Specifically', ['Moreover', 'Instead', 'Finally']); return { id: this.id, category: this.category, question: '"The research had broad implications. ___, it could change how doctors treat chronic pain." Best transition?', options, correctIndex, explanation: '"Specifically" narrows from a broad statement to a particular detail.' } } },
  { id: 'to-q34', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('Granted', ['Therefore', 'Additionally', 'First']); return { id: this.id, category: this.category, question: '"___, the policy has its flaws, but it addresses the most urgent needs." Which concession transition fits?', options, correctIndex, explanation: '"Granted" acknowledges a point before presenting a counterpoint — a concession transition.' } } },
  { id: 'to-q35', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('In other words', ['In contrast', 'In addition', 'In conclusion']); return { id: this.id, category: this.category, question: '"The compound is hydrophobic. ___, it repels water." Which transition clarifies?', options, correctIndex, explanation: '"In other words" restates the same idea in simpler terms.' } } },
  { id: 'to-q36', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('Match the transition to the logical relationship: addition, contrast, cause/effect, or example', ['Use the same transition every time', 'Always pick "furthermore"', 'Transitions are optional on the SAT']); return { id: this.id, category: this.category, question: 'What is the key principle for choosing transitions on the SAT?', options, correctIndex, explanation: 'The transition must match the logical relationship between the ideas.' } } },
  { id: 'to-q37', category: 'Sentence Placement', generate() { const { options, correctIndex } = makeStringOptions('After sentence 2, because it provides context for the example in sentence 3', ['Before sentence 1', 'After sentence 4', 'It should be deleted']); return { id: this.id, category: this.category, question: 'A paragraph has sentences 1-4. A new sentence defines a technical term used first in sentence 3. Where does it go?', options, correctIndex, explanation: 'Definitions should precede the first use of the term they define.' } } },
  { id: 'to-q38', category: 'Paragraph Organization', generate() { const { options, correctIndex } = makeStringOptions('Introduction → Body (with evidence) → Conclusion', ['Conclusion → Body → Introduction', 'Evidence only, no introduction', 'Introduction → Conclusion → Body']); return { id: this.id, category: this.category, question: 'What is the standard overall structure of a well-organized essay?', options, correctIndex, explanation: 'Essays follow: introduction → body paragraphs with evidence → conclusion.' } } },
  { id: 'to-q39', category: 'Contrast Transitions', generate() { const { options, correctIndex } = makeStringOptions('Nonetheless', ['Likewise', 'Therefore', 'Additionally']); return { id: this.id, category: this.category, question: '"The experiment had several limitations. ___, the results were considered reliable." Best transition?', options, correctIndex, explanation: '"Nonetheless" signals that despite the limitations, the results held — a contrast/concession.' } } },
  { id: 'to-q40', category: 'Addition Transitions', generate() { const { options, correctIndex } = makeStringOptions('Equally important', ['On the contrary', 'As a result', 'In conclusion']); return { id: this.id, category: this.category, question: '"Physical health affects academic performance. ___, mental health plays a significant role." Best transition?', options, correctIndex, explanation: '"Equally important" adds a parallel point of equal weight.' } } },
]

export function generateExitQuiz(count: number = 10): ExitQuizQuestion[] {
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return shuffle(selected).map(t => t.generate())
}
