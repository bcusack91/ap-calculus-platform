/**
 * Exit Quiz — SAT Punctuation: Commas and Semicolons
 * 40 randomized questions on comma rules, semicolons, colons, dashes, and apostrophes
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string; difficulty?: 'easy' | 'medium' | 'hard' }
interface QuestionTemplate { id: string; category: string; difficulty: 'easy' | 'medium' | 'hard'; generate: () => ExitQuizQuestion }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }

const questionPool: QuestionTemplate[] = [
  // Commas after introductory elements
  { id: 'pc-q1', category: 'Introductory Commas', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('After finishing her homework, she went outside.', ['After, finishing her homework, she went outside.', 'After finishing, her homework, she went outside.', 'After finishing her homework, she, went outside.']); return { id: this.id, category: this.category, question: `Which is correctly punctuated?`, options, correctIndex, explanation: `A comma follows an introductory phrase or clause before the main clause.` } } },
  { id: 'pc-q2', category: 'Introductory Commas', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('In 2024, the program expanded to three new states.', ['In 2024 the program expanded to three new states.', 'In, 2024 the program expanded to three new states.', 'In 2024, the program, expanded to three new states.']); return { id: this.id, category: this.category, question: `Which is correctly punctuated?`, options, correctIndex, explanation: `Introductory prepositional phrases are typically followed by a comma.` } } },
  { id: 'pc-q3', category: 'Introductory Commas', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('However, the results were inconclusive.', ['However the results were inconclusive.', 'However the results, were inconclusive.', 'However; the results were inconclusive.']); return { id: this.id, category: this.category, question: `Which correctly punctuates the transitional word?`, options, correctIndex, explanation: `Transitional words like "however" at the start of a sentence take a comma after them.` } } },
  { id: 'pc-q4', category: 'Introductory Commas', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Surprisingly, the underdog won the championship.', ['Surprisingly, the underdog, won the championship.', 'Surprisingly; the underdog, won the championship.', 'Surprisingly, the underdog won, the championship.']); return { id: this.id, category: this.category, question: `Which is correct?`, options, correctIndex, explanation: `Introductory adverbs modifying the whole sentence take a comma, and no comma belongs between the subject and its verb or between the verb and its object.` } } },
  // Commas in compound sentences
  { id: 'pc-q5', category: 'Compound Sentences', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('She studied hard, and she passed the exam.', ['She studied hard and she passed the exam.', 'She studied hard, and, she passed the exam.', 'She, studied hard and she passed the exam.']); return { id: this.id, category: this.category, question: `Which correctly joins two independent clauses?`, options, correctIndex, explanation: `Two independent clauses joined by a coordinating conjunction (FANBOYS) need a comma before the conjunction.` } } },
  { id: 'pc-q6', category: 'Compound Sentences', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('The concert was sold out, but we found tickets online.', ['The concert was sold out but we found tickets online.', 'The concert was sold out but, we found tickets online.', 'The concert was sold out, but, we found tickets online.']); return { id: this.id, category: this.category, question: `Correct punctuation for this compound sentence?`, options, correctIndex, explanation: `Comma before "but" when joining two independent clauses.` } } },
  { id: 'pc-q7', category: 'Compound Sentences', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('He likes tea and coffee.', ['He likes tea, and coffee.', 'He likes, tea and coffee.', 'He, likes tea and coffee.']); return { id: this.id, category: this.category, question: `Which version of the sentence is correctly punctuated?`, options, correctIndex, explanation: `No comma before "and" when it joins two items (not two independent clauses).` } } },
  // Commas with nonessential clauses
  { id: 'pc-q8', category: 'Nonessential Clauses', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('My sister, who lives in Boston, is visiting next week.', ['My sister, who lives in Boston, is visiting, next week.', 'My sister who lives in Boston, is visiting next week.', 'My sister, who lives, in Boston is visiting next week.']); return { id: this.id, category: this.category, question: `If you have only ONE sister (nonessential info), correct punctuation:`, options, correctIndex, explanation: `Nonessential clauses (extra info) need commas on BOTH sides, and no other commas belong in this sentence.` } } },
  { id: 'pc-q9', category: 'Nonessential Clauses', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The book that I borrowed is overdue.', ['The book, that I borrowed, is overdue.', 'The book, that I borrowed is overdue.', 'The book that I borrowed, is overdue.']); return { id: this.id, category: this.category, question: `"That I borrowed" is an essential clause. Which is correctly punctuated?`, options, correctIndex, explanation: `Essential (restrictive) clauses with "that" do NOT take commas.` } } },
  { id: 'pc-q10', category: 'Nonessential Clauses', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Einstein, arguably the greatest physicist ever, developed general relativity.', ['Einstein, arguably the greatest physicist ever developed general relativity.', 'Einstein arguably the greatest physicist ever, developed general relativity.', 'Einstein arguably, the greatest physicist ever developed general relativity.']); return { id: this.id, category: this.category, question: `Which correctly sets off the appositive phrase?`, options, correctIndex, explanation: `The appositive "arguably the greatest physicist ever" is nonessential → commas on both sides.` } } },
  // Semicolons
  { id: 'pc-q11', category: 'Semicolons', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('She loves hiking; he prefers swimming.', ['She loves hiking, he prefers swimming.', 'She loves hiking: he prefers swimming.', 'She loves hiking he prefers swimming.']); return { id: this.id, category: this.category, question: `Two related independent clauses without a conjunction. Correct punctuation:`, options, correctIndex, explanation: `A semicolon joins two related independent clauses without a conjunction.` } } },
  { id: 'pc-q12', category: 'Semicolons', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The project was challenging; nevertheless, the team succeeded.', ['The project was challenging, nevertheless the team succeeded.', 'The project was challenging; nevertheless the team succeeded.', 'The project was challenging nevertheless, the team succeeded.']); return { id: this.id, category: this.category, question: `Correct punctuation with a conjunctive adverb:`, options, correctIndex, explanation: `Semicolon before the conjunctive adverb, comma after: "; nevertheless,"` } } },
  { id: 'pc-q13', category: 'Semicolons', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('The conference had speakers from Paris, France; London, England; and Tokyo, Japan.', ['The conference had speakers from Paris, France, London, England, and Tokyo, Japan.', 'The conference had speakers from Paris France; London England; and Tokyo Japan.', 'The conference had speakers from Paris, France: London, England: and Tokyo, Japan.']); return { id: this.id, category: this.category, question: `Which correctly uses semicolons in a list with internal commas?`, options, correctIndex, explanation: `When list items contain commas, semicolons separate the items for clarity.` } } },
  { id: 'pc-q14', category: 'Semicolons', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('No, because "Because it rained" cannot stand alone as a sentence', ['Yes, because the two clauses are closely related in meaning', 'Yes, since a semicolon may replace a clause-ending comma', 'Yes, as long as the second clause is longer than the first']); return { id: this.id, category: this.category, question: `Can a semicolon replace the comma in: "Because it rained, the game was canceled"?`, options, correctIndex, explanation: `"Because it rained" is dependent — semicolons only join independent clauses.` } } },
  // Colons
  { id: 'pc-q15', category: 'Colons', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('She needed three things: a pen, paper, and determination.', ['She needed three things a pen, paper, and determination.', 'She needed three things; a pen, paper and determination.', 'She needed: three things a pen, paper, and determination.']); return { id: this.id, category: this.category, question: `Which correctly uses a colon?`, options, correctIndex, explanation: `A colon follows a complete sentence and introduces a list, explanation, or elaboration.` } } },
  { id: 'pc-q16', category: 'Colons', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('No, because "She bought" is not a complete sentence', ['Yes, because a colon may introduce any list of items', 'Yes, as long as the list contains at least three items', 'No, because a list after a verb needs a semicolon instead']); return { id: this.id, category: this.category, question: `Can you write: "She bought: milk, eggs, and bread"?`, options, correctIndex, explanation: `"She bought" is not a complete thought. Correct: "She bought milk, eggs, and bread." Or "She bought the essentials: milk, eggs, and bread."` } } },
  { id: 'pc-q17', category: 'Colons', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The verdict was clear: not guilty.', ['The verdict was clear, not guilty.', 'The verdict was clear; not guilty.', 'The verdict was: clear not guilty.']); return { id: this.id, category: this.category, question: `Which correctly uses a colon to introduce an explanation?`, options, correctIndex, explanation: `A colon after a complete sentence introduces the explanation "not guilty."` } } },
  // Dashes
  { id: 'pc-q18', category: 'Dashes', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('The results—surprising as they were—changed the entire study.', ['The results, surprising as they were, changed the entire study.', 'The results (surprising as they were) changed the entire study.', 'The results—surprising as they were, changed the entire study.']); return { id: this.id, category: this.category, question: `Which is more emphatic for setting off a parenthetical element?`, options, correctIndex, explanation: `Dashes add emphasis; commas are neutral and parentheses downplay the aside. A dash cannot be paired with a comma, so the mixed version is incorrect.` } } },
  { id: 'pc-q19', category: 'Dashes', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Two, one placed before the element and one after it', ['One, placed before the element, with a comma after it', 'Three: one before, one inside, and one after the element', 'A dash before the element and a colon after it']); return { id: this.id, category: this.category, question: `When using dashes to set off a parenthetical, how many are needed?`, options, correctIndex, explanation: `Like commas for nonessentials, dashes come in pairs: one before, one after.` } } },
  // Apostrophes
  { id: 'pc-q20', category: 'Apostrophes', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions("the children's toys", ["the childrens' toys", "the childrens toys", "the children toys'"]); return { id: this.id, category: this.category, question: `Possessive of "children" (irregular plural):`, options, correctIndex, explanation: `Irregular plurals not ending in "s" add 's: children's.` } } },
  { id: 'pc-q21', category: 'Apostrophes', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions("James's book", ["James' book", "Jameses' book", "Jame's book"]); return { id: this.id, category: this.category, question: `On the SAT, the preferred possessive of "James":`, options, correctIndex, explanation: `The SAT follows the convention of adding 's even after names ending in s: James's.` } } },
  { id: 'pc-q22', category: 'Apostrophes', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions("The dogs played in the park.", ["The dog's played in the park.", "The dogs' played in the park.", "The dog's played in the park's."]); return { id: this.id, category: this.category, question: `"Multiple dogs played in the park." (no possession) Correct form:`, options, correctIndex, explanation: `Simple plural — no apostrophe needed. Apostrophes show possession, not plurality.` } } },
  // Comma splice identification
  { id: 'pc-q23', category: 'Comma Splices', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A comma splice: two independent clauses joined by a comma alone', ['No error: a comma can join two short, closely related clauses', 'A fragment: the second clause is missing its own subject', 'An agreement error: "The movie was" should be "were"']); return { id: this.id, category: this.category, question: `"The movie was great, we should see it again." What is the error?`, options, correctIndex, explanation: `Two independent clauses joined by only a comma = comma splice. Fix with semicolon, period, or conjunction.` } } },
  { id: 'pc-q24', category: 'Comma Splices', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The movie was great, and we should see it again.', ['The movie was great, and, we should see it again.', 'The movie was great and, we should see it again.', 'The movie was great; and we should see it again.']); return { id: this.id, category: this.category, question: `Fix the comma splice: "The movie was great, we should see it again."`, options, correctIndex, explanation: `Add a coordinating conjunction after the comma: "great, and we." No comma follows "and," and a semicolon is not used before a coordinating conjunction here.` } } },
  // Mixed practice
  { id: 'pc-q25', category: 'Mixed Practice', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A comma', ['A semicolon', 'A period', 'A single dash']); return { id: this.id, category: this.category, question: `After an introductory dependent clause, you typically use:`, options, correctIndex, explanation: `Introductory dependent clauses are followed by a comma before the main clause.` } } },
  { id: 'pc-q26', category: 'Mixed Practice', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A semicolon', ['A comma', 'A pair of commas', 'No punctuation']); return { id: this.id, category: this.category, question: `Between two independent clauses with no conjunction, you use:`, options, correctIndex, explanation: `Two independent clauses without a conjunction need a semicolon (or a period).` } } },
  { id: 'pc-q27', category: 'Mixed Practice', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A colon', ['A comma', 'A semicolon', 'A period']); return { id: this.id, category: this.category, question: `After a complete sentence that introduces a list or explanation, you use:`, options, correctIndex, explanation: `A colon follows a complete sentence to introduce what follows.` } } },
  { id: 'pc-q28', category: 'Mixed Practice', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('A pair of commas or dashes', ['A single comma before it', 'A semicolon on each side', 'A colon before and a comma after']); return { id: this.id, category: this.category, question: `To set off a nonessential parenthetical element, use:`, options, correctIndex, explanation: `Nonessential elements are set off by paired commas or paired dashes.` } } },
  // SAT-specific patterns
  { id: 'pc-q29', category: 'SAT Patterns', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Remove the comma between the subject and verb', ['Keep it, since "The company" is an introductory phrase', 'Add a second comma after "plans" to pair it', 'Replace the comma with a semicolon']); return { id: this.id, category: this.category, question: `"The company, plans to expand." What should you do?`, options, correctIndex, explanation: `Never separate a subject from its verb with a single comma. Remove it.` } } },
  { id: 'pc-q30', category: 'SAT Patterns', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('No; delete the comma before "that"', ['Yes; "that" begins a new clause, so a comma is needed', 'No; move the comma so it follows "that"', 'No; replace that comma with a colon']); return { id: this.id, category: this.category, question: `"The study showed, that sleep improves memory." Correct?`, options, correctIndex, explanation: `No comma before "that" — this introduces an essential clause.` } } },
  { id: 'pc-q31', category: 'SAT Patterns', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('No; it has one subject with two verbs', ['Yes, before "and," since it joins two actions', 'Yes, after "report," to separate the verbs', 'No, but "and" should become a semicolon']); return { id: this.id, category: this.category, question: `"She wrote the report and submitted it on Friday." Is a comma needed?`, options, correctIndex, explanation: `A compound predicate (two verbs, one subject) doesn't need a comma before "and."` } } },
  { id: 'pc-q32', category: 'SAT Patterns', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Yes; the SAT consistently uses the serial comma', ['No; the comma before "and" should be removed', 'No; it should be a semicolon before "and"', 'Yes, but it belongs after "and" instead']); return { id: this.id, category: this.category, question: `"She packed shoes, socks, and a jacket." Is the comma before "and" correct?`, options, correctIndex, explanation: `The SAT consistently uses the Oxford comma (comma before "and" in a series).` } } },
  // Restrictive vs non-restrictive
  { id: 'pc-q33', category: 'Nonessential Clauses', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('My sister, who lives in Denver, is visiting next week.', ['My sister who lives in Denver is visiting next week.', 'My sister, who lives in Denver is visiting next week.', 'My sister who lives in Denver, is visiting next week.']); return { id: this.id, category: this.category, question: 'The writer has only ONE sister. Which choice punctuates the sentence correctly?', options, correctIndex, explanation: 'With only one sister, "who lives in Denver" is extra (nonessential) information — set it off with a PAIR of commas.' } } },
  { id: 'pc-q34', category: 'Nonessential Clauses', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('"which" — it introduces a nonessential clause', ['"that" — it introduces an essential clause', '"whom" — it is the object of "released"', '"who" — it refers to people and things alike']); return { id: this.id, category: this.category, question: `"The report, _____ was released yesterday, surprised everyone." Which relative pronoun with commas?`, options, correctIndex, explanation: `"Which" + commas = nonessential. "That" (no commas) = essential. Here, commas indicate nonessential; the pronoun is the subject of "was released," and "who" refers to people.` } } },
  // More SAT practice
  { id: 'pc-q35', category: 'Semicolons', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('No; a dependent clause follows the semicolon', ['Yes; a semicolon may join any two related clauses', 'No; the semicolon should be a colon instead', 'No; a comma is also needed after "because"']); return { id: this.id, category: this.category, question: `"She left early; because she was tired." Correct?`, options, correctIndex, explanation: `"Because she was tired" is dependent. Semicolons only join independent clauses.` } } },
  { id: 'pc-q36', category: 'Colons', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Yes; a full sentence comes before it', ['No; replace the colon with a comma', 'No; the colon should come after "was"', 'No; use a semicolon, since a saying follows']); return { id: this.id, category: this.category, question: `"The answer was obvious: practice makes perfect." Is the colon correct?`, options, correctIndex, explanation: `"The answer was obvious" is a complete sentence, so a colon properly introduces the explanation.` } } },
  { id: 'pc-q37', category: 'Introductory Commas', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Furthermore, the data supports our hypothesis.', ['Furthermore the data supports our hypothesis.', 'Furthermore the data, supports our hypothesis.', 'Furthermore; the data supports our hypothesis.']); return { id: this.id, category: this.category, question: `Which correctly punctuates the transition word?`, options, correctIndex, explanation: `Transitional words at the beginning of a sentence are followed by a comma.` } } },
  { id: 'pc-q38', category: 'Compound Sentences', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The test was hard, yet everyone passed.', ['The test was hard yet everyone passed.', 'The test was hard; yet, everyone passed.', 'The test was hard, yet, everyone passed.']); return { id: this.id, category: this.category, question: `Which correctly joins two independent clauses with "yet"?`, options, correctIndex, explanation: `Comma before a coordinating conjunction (yet) joining two independent clauses.` } } },
  { id: 'pc-q39', category: 'Dashes', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('The three items — a book, a pen, and a notebook — were on the desk.', ['The three items, a book, a pen, and a notebook, were on the desk.', 'The three items — a book, a pen, and a notebook were on the desk.', 'The three items a book, a pen, and a notebook — were on the desk.']); return { id: this.id, category: this.category, question: `When a parenthetical list contains commas, dashes prevent confusion:`, options, correctIndex, explanation: `Dashes are clearer than commas when the parenthetical itself contains commas.` } } },
  { id: 'pc-q40', category: 'Mixed Practice', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A period or a semicolon', ['A comma followed by "then"', 'A single comma', 'A comma or a semicolon']); return { id: this.id, category: this.category, question: `Two independent clauses, no conjunction. What punctuation between them?`, options, correctIndex, explanation: `Without a conjunction, independent clauses need a period or semicolon — a comma creates a splice, and "then" is not a coordinating conjunction.` } } },
]

function selectTemplates(count: number, difficulty?: 'easy' | 'medium' | 'hard'): QuestionTemplate[] {
  if (difficulty) {
    const fillOrder: Record<'easy' | 'medium' | 'hard', ('easy' | 'medium' | 'hard')[]> = { easy: ['easy', 'medium', 'hard'], medium: ['medium', 'easy', 'hard'], hard: ['hard', 'medium', 'easy'] }
    const selected: QuestionTemplate[] = []
    for (const tier of fillOrder[difficulty]) { if (selected.length >= count) break; for (const q of shuffle(questionPool.filter(t => t.difficulty === tier))) { if (selected.length >= count) break; selected.push(q) } }
    return selected
  }
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return selected
}

export function generateExitQuiz(count: number = 10, _topicSlug?: string, difficulty?: 'easy' | 'medium' | 'hard'): ExitQuizQuestion[] {
  return shuffle(selectTemplates(count, difficulty)).map(t => ({ ...t.generate(), difficulty: t.difficulty }))
}
