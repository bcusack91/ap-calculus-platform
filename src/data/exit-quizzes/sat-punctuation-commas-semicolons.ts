/**
 * Exit Quiz — SAT Punctuation: Commas and Semicolons
 * 40 randomized questions on comma rules, semicolons, colons, dashes, and apostrophes
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string }
interface QuestionTemplate { id: string; category: string; generate: () => ExitQuizQuestion }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const fillers = ['No change', 'Cannot be determined', 'None of these']; for (const f of fillers) { if (u.length >= 3) break; if (f !== c && !u.includes(f)) u.push(f) } const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }

const questionPool: QuestionTemplate[] = [
  // Commas after introductory elements
  { id: 'pc-q1', category: 'Introductory Commas', generate() { const { options, correctIndex } = makeStringOptions('After finishing her homework, she went outside.', ['After finishing her homework she went outside.', 'After, finishing her homework she went outside.', 'After finishing, her homework she went outside.']); return { id: this.id, category: this.category, question: `Which is correctly punctuated?`, options, correctIndex, explanation: `A comma follows an introductory phrase or clause before the main clause.` } } },
  { id: 'pc-q2', category: 'Introductory Commas', generate() { const { options, correctIndex } = makeStringOptions('In 2024, the program expanded to three new states.', ['In 2024 the program expanded to three new states.', 'In, 2024 the program expanded to three new states.', 'In 2024, the program, expanded to three new states.']); return { id: this.id, category: this.category, question: `Which is correctly punctuated?`, options, correctIndex, explanation: `Introductory prepositional phrases are typically followed by a comma.` } } },
  { id: 'pc-q3', category: 'Introductory Commas', generate() { const { options, correctIndex } = makeStringOptions('However, the results were inconclusive.', ['However the results were inconclusive.', 'However the results, were inconclusive.', 'However; the results were inconclusive.']); return { id: this.id, category: this.category, question: `Which correctly punctuates the transitional word?`, options, correctIndex, explanation: `Transitional words like "however" at the start of a sentence take a comma after them.` } } },
  { id: 'pc-q4', category: 'Introductory Commas', generate() { const { options, correctIndex } = makeStringOptions('Surprisingly, the underdog won the championship.', ['Surprisingly the underdog won the championship.', 'Surprisingly; the underdog won the championship.', 'Surprisingly the underdog, won the championship.']); return { id: this.id, category: this.category, question: `Which is correct?`, options, correctIndex, explanation: `Introductory adverbs modifying the whole sentence take a comma.` } } },
  // Commas in compound sentences
  { id: 'pc-q5', category: 'Compound Sentences', generate() { const { options, correctIndex } = makeStringOptions('She studied hard, and she passed the exam.', ['She studied hard and she passed the exam.', 'She studied hard, and, she passed the exam.', 'She, studied hard and she passed the exam.']); return { id: this.id, category: this.category, question: `Which correctly joins two independent clauses?`, options, correctIndex, explanation: `Two independent clauses joined by a coordinating conjunction (FANBOYS) need a comma before the conjunction.` } } },
  { id: 'pc-q6', category: 'Compound Sentences', generate() { const { options, correctIndex } = makeStringOptions('The concert was sold out, but we found tickets online.', ['The concert was sold out but we found tickets online.', 'The concert was sold out but, we found tickets online.', 'The concert was sold out, but, we found tickets online.']); return { id: this.id, category: this.category, question: `Correct punctuation for this compound sentence?`, options, correctIndex, explanation: `Comma before "but" when joining two independent clauses.` } } },
  { id: 'pc-q7', category: 'Compound Sentences', generate() { const { options, correctIndex } = makeStringOptions('He likes tea and coffee. (no comma needed)', ['He likes tea, and coffee.', 'He likes, tea and coffee.', 'He, likes tea and coffee.']); return { id: this.id, category: this.category, question: `"He likes tea and coffee." Is a comma needed?`, options, correctIndex, explanation: `No comma before "and" when it joins two items (not two independent clauses).` } } },
  // Commas with nonessential clauses
  { id: 'pc-q8', category: 'Nonessential Clauses', generate() { const { options, correctIndex } = makeStringOptions('My sister, who lives in Boston, is visiting next week.', ['My sister who lives in Boston is visiting next week.', 'My sister, who lives in Boston is visiting next week.', 'My sister who lives in Boston, is visiting next week.']); return { id: this.id, category: this.category, question: `If you have only ONE sister (nonessential info), correct punctuation:`, options, correctIndex, explanation: `Nonessential clauses (extra info) need commas on BOTH sides.` } } },
  { id: 'pc-q9', category: 'Nonessential Clauses', generate() { const { options, correctIndex } = makeStringOptions('The book that I borrowed is overdue. (no commas)', ['The book, that I borrowed, is overdue.', 'The book, that I borrowed is overdue.', 'The book that I borrowed, is overdue.']); return { id: this.id, category: this.category, question: `Essential clause: "The book that I borrowed is overdue." Correct?`, options, correctIndex, explanation: `Essential (restrictive) clauses with "that" do NOT take commas.` } } },
  { id: 'pc-q10', category: 'Nonessential Clauses', generate() { const { options, correctIndex } = makeStringOptions('Einstein, arguably the greatest physicist ever, developed general relativity.', ['Einstein arguably the greatest physicist ever developed general relativity.', 'Einstein, arguably the greatest physicist ever developed general relativity.', 'Einstein arguably, the greatest physicist ever, developed general relativity.']); return { id: this.id, category: this.category, question: `Which correctly sets off the appositive phrase?`, options, correctIndex, explanation: `The appositive "arguably the greatest physicist ever" is nonessential → commas on both sides.` } } },
  // Semicolons
  { id: 'pc-q11', category: 'Semicolons', generate() { const { options, correctIndex } = makeStringOptions('She loves hiking; he prefers swimming.', ['She loves hiking, he prefers swimming.', 'She loves hiking: he prefers swimming.', 'She loves hiking he prefers swimming.']); return { id: this.id, category: this.category, question: `Two related independent clauses without a conjunction. Correct punctuation:`, options, correctIndex, explanation: `A semicolon joins two related independent clauses without a conjunction.` } } },
  { id: 'pc-q12', category: 'Semicolons', generate() { const { options, correctIndex } = makeStringOptions('The project was challenging; nevertheless, the team succeeded.', ['The project was challenging, nevertheless, the team succeeded.', 'The project was challenging; nevertheless the team succeeded.', 'The project was challenging nevertheless, the team succeeded.']); return { id: this.id, category: this.category, question: `Correct punctuation with a conjunctive adverb:`, options, correctIndex, explanation: `Semicolon before the conjunctive adverb, comma after: "; nevertheless,"` } } },
  { id: 'pc-q13', category: 'Semicolons', generate() { const { options, correctIndex } = makeStringOptions('The conference had speakers from Paris, France; London, England; and Tokyo, Japan.', ['The conference had speakers from Paris, France, London, England, and Tokyo, Japan.', 'The conference had speakers from Paris France; London England; and Tokyo Japan.', 'The conference had speakers from Paris, France: London, England: and Tokyo, Japan.']); return { id: this.id, category: this.category, question: `Which correctly uses semicolons in a list with internal commas?`, options, correctIndex, explanation: `When list items contain commas, semicolons separate the items for clarity.` } } },
  { id: 'pc-q14', category: 'Semicolons', generate() { const { options, correctIndex } = makeStringOptions('No — a semicolon can only join independent clauses, not a dependent and independent clause', ['Yes, semicolons can replace any comma', 'Yes, if the clauses are related', 'Yes, but only with "because"']); return { id: this.id, category: this.category, question: `Can a semicolon replace the comma in: "Because it rained, the game was canceled"?`, options, correctIndex, explanation: `"Because it rained" is dependent — semicolons only join independent clauses.` } } },
  // Colons
  { id: 'pc-q15', category: 'Colons', generate() { const { options, correctIndex } = makeStringOptions('She needed three things: a pen, paper, and determination.', ['She needed three things, a pen, paper, and determination.', 'She needed three things; a pen, paper, and determination.', 'She needed: three things a pen paper and determination.']); return { id: this.id, category: this.category, question: `Which correctly uses a colon?`, options, correctIndex, explanation: `A colon follows a complete sentence and introduces a list, explanation, or elaboration.` } } },
  { id: 'pc-q16', category: 'Colons', generate() { const { options, correctIndex } = makeStringOptions('No — the text before a colon must be a complete sentence', ['Yes, colons can go anywhere before a list', 'Yes, always use a colon before a list', 'Only if the list has 3+ items']); return { id: this.id, category: this.category, question: `Can you write: "She bought: milk, eggs, and bread"?`, options, correctIndex, explanation: `"She bought" is not a complete thought. Correct: "She bought milk, eggs, and bread." Or "She bought the essentials: milk, eggs, and bread."` } } },
  { id: 'pc-q17', category: 'Colons', generate() { const { options, correctIndex } = makeStringOptions('The verdict was clear: not guilty.', ['The verdict was clear, not guilty.', 'The verdict was clear; not guilty.', 'The verdict was: clear not guilty.']); return { id: this.id, category: this.category, question: `Which correctly uses a colon to introduce an explanation?`, options, correctIndex, explanation: `A colon after a complete sentence introduces the explanation "not guilty."` } } },
  // Dashes
  { id: 'pc-q18', category: 'Dashes', generate() { const { options, correctIndex } = makeStringOptions('The results — surprising as they were — changed the entire study.', ['The results, surprising as they were, changed the entire study.', 'Both dash and comma versions are acceptable here', 'The results surprising as they were changed the entire study.']); return { id: this.id, category: this.category, question: `Which is more emphatic for setting off a parenthetical element?`, options, correctIndex, explanation: `Dashes add emphasis; commas are neutral. Both are grammatically acceptable — but the question asks for more emphatic.` } } },
  { id: 'pc-q19', category: 'Dashes', generate() { const { options, correctIndex } = makeStringOptions('BOTH dashes must be used — one before and one after', ['Only one dash is needed', 'Dashes are interchangeable with semicolons', 'Use a dash and a comma as a pair']); return { id: this.id, category: this.category, question: `When using dashes to set off a parenthetical, how many are needed?`, options, correctIndex, explanation: `Like commas for nonessentials, dashes come in pairs: one before, one after.` } } },
  // Apostrophes
  { id: 'pc-q20', category: 'Apostrophes', generate() { const { options, correctIndex } = makeStringOptions("the children's toys", ["the childrens' toys", "the childrens toys", "the children toys'"]); return { id: this.id, category: this.category, question: `Possessive of "children" (irregular plural):`, options, correctIndex, explanation: `Irregular plurals not ending in "s" add 's: children's.` } } },
  { id: 'pc-q21', category: 'Apostrophes', generate() { const { options, correctIndex } = makeStringOptions("James's book", ["James' book", "James book", "Jame's book"]); return { id: this.id, category: this.category, question: `On the SAT, the preferred possessive of "James":`, options, correctIndex, explanation: `The SAT follows the convention of adding 's even after names ending in s: James's.` } } },
  { id: 'pc-q22', category: 'Apostrophes', generate() { const { options, correctIndex } = makeStringOptions("The dogs played in the park. (no apostrophe)", ["The dog's played in the park.", "The dogs' played in the park.", "The dog's played in the park's."]); return { id: this.id, category: this.category, question: `"Multiple dogs played in the park." (no possession) Correct form:`, options, correctIndex, explanation: `Simple plural — no apostrophe needed. Apostrophes show possession, not plurality.` } } },
  // Comma splice identification
  { id: 'pc-q23', category: 'Comma Splices', generate() { const { options, correctIndex } = makeStringOptions('It is a comma splice — two independent clauses joined only by a comma', ['It is correct', 'It needs more commas', 'It is a fragment']); return { id: this.id, category: this.category, question: `"The movie was great, we should see it again." What is the error?`, options, correctIndex, explanation: `Two independent clauses joined by only a comma = comma splice. Fix with semicolon, period, or conjunction.` } } },
  { id: 'pc-q24', category: 'Comma Splices', generate() { const { options, correctIndex } = makeStringOptions('The movie was great, and we should see it again.', ['The movie was great, we should see it again.', 'The movie was great we should see it again.', 'The movie, was great and we should see it again.']); return { id: this.id, category: this.category, question: `Fix the comma splice: "The movie was great, we should see it again."`, options, correctIndex, explanation: `Add a coordinating conjunction after the comma: "great, and we."` } } },
  // Mixed practice
  { id: 'pc-q25', category: 'Mixed Practice', generate() { const { options, correctIndex } = makeStringOptions('A comma', ['A semicolon', 'A colon', 'A dash']); return { id: this.id, category: this.category, question: `After an introductory dependent clause, you typically use:`, options, correctIndex, explanation: `Introductory dependent clauses are followed by a comma before the main clause.` } } },
  { id: 'pc-q26', category: 'Mixed Practice', generate() { const { options, correctIndex } = makeStringOptions('A semicolon', ['A comma', 'A colon', 'No punctuation']); return { id: this.id, category: this.category, question: `Between two independent clauses with no conjunction, you use:`, options, correctIndex, explanation: `Two independent clauses without a conjunction need a semicolon (or a period).` } } },
  { id: 'pc-q27', category: 'Mixed Practice', generate() { const { options, correctIndex } = makeStringOptions('A colon', ['A comma', 'A semicolon', 'A period']); return { id: this.id, category: this.category, question: `After a complete sentence that introduces a list or explanation, you use:`, options, correctIndex, explanation: `A colon follows a complete sentence to introduce what follows.` } } },
  { id: 'pc-q28', category: 'Mixed Practice', generate() { const { options, correctIndex } = makeStringOptions('Commas or dashes (in pairs)', ['A single comma', 'A semicolon', 'A colon']); return { id: this.id, category: this.category, question: `To set off a nonessential parenthetical element, use:`, options, correctIndex, explanation: `Nonessential elements are set off by paired commas or paired dashes.` } } },
  // SAT-specific patterns
  { id: 'pc-q29', category: 'SAT Patterns', generate() { const { options, correctIndex } = makeStringOptions('Remove the comma — no comma between subject and verb', ['Keep the comma', 'Add another comma after "plans"', 'Replace with a semicolon']); return { id: this.id, category: this.category, question: `"The company, plans to expand." What should you do?`, options, correctIndex, explanation: `Never separate a subject from its verb with a single comma. Remove it.` } } },
  { id: 'pc-q30', category: 'SAT Patterns', generate() { const { options, correctIndex } = makeStringOptions('Remove the comma — no comma before "that" in essential clauses', ['Keep the comma', 'Replace with a semicolon', 'Add another comma after "discovered"']); return { id: this.id, category: this.category, question: `"The study showed, that sleep improves memory." Correct?`, options, correctIndex, explanation: `No comma before "that" — this introduces an essential clause.` } } },
  { id: 'pc-q31', category: 'SAT Patterns', generate() { const { options, correctIndex } = makeStringOptions('No comma needed before "and" in a two-item compound predicate', ['Add a comma before "and"', 'Replace "and" with a semicolon', 'The sentence is a fragment']); return { id: this.id, category: this.category, question: `"She wrote the report and submitted it on Friday." Is a comma needed?`, options, correctIndex, explanation: `A compound predicate (two verbs, one subject) doesn't need a comma before "and."` } } },
  { id: 'pc-q32', category: 'SAT Patterns', generate() { const { options, correctIndex } = makeStringOptions('Yes — the serial (Oxford) comma is standard on the SAT', ['No, skip the last comma', 'Only use two commas total', 'Replace all commas with semicolons']); return { id: this.id, category: this.category, question: `"She packed shoes, socks, and a jacket." Is the comma before "and" correct?`, options, correctIndex, explanation: `The SAT consistently uses the Oxford comma (comma before "and" in a series).` } } },
  // Restrictive vs non-restrictive
  { id: 'pc-q33', category: 'Nonessential Clauses', generate() { const { options, correctIndex } = makeStringOptions('If removing the clause changes the meaning of the sentence, it is essential (no commas)', ['All clauses need commas', 'Only "which" clauses need commas', 'Only "that" clauses need commas']); return { id: this.id, category: this.category, question: `How do you decide whether to use commas around a clause?`, options, correctIndex, explanation: `Test: remove the clause. If the meaning changes, it's essential → no commas. If it's bonus info → commas.` } } },
  { id: 'pc-q34', category: 'Nonessential Clauses', generate() { const { options, correctIndex } = makeStringOptions('"which" — it introduces a nonessential clause', ['"that" — it introduces an essential clause', 'Either is correct', '"who" — it refers to things']); return { id: this.id, category: this.category, question: `"The report, _____ was released yesterday, surprised everyone." Which relative pronoun with commas?`, options, correctIndex, explanation: `"Which" + commas = nonessential. "That" (no commas) = essential. Here, commas indicate nonessential.` } } },
  // More SAT practice
  { id: 'pc-q35', category: 'Semicolons', generate() { const { options, correctIndex } = makeStringOptions('Incorrect — a semicolon cannot join an independent clause to a dependent clause', ['Correct as written', 'Replace with a colon', 'Add a comma after "because"']); return { id: this.id, category: this.category, question: `"She left early; because she was tired." Correct?`, options, correctIndex, explanation: `"Because she was tired" is dependent. Semicolons only join independent clauses.` } } },
  { id: 'pc-q36', category: 'Colons', generate() { const { options, correctIndex } = makeStringOptions('Correct — the text before the colon is a complete sentence', ['Incorrect — remove the colon', 'Incorrect — use a semicolon', 'Incorrect — use a comma']); return { id: this.id, category: this.category, question: `"The answer was obvious: practice makes perfect." Is the colon correct?`, options, correctIndex, explanation: `"The answer was obvious" is a complete sentence, so a colon properly introduces the explanation.` } } },
  { id: 'pc-q37', category: 'Introductory Commas', generate() { const { options, correctIndex } = makeStringOptions('Furthermore, the data supports our hypothesis.', ['Furthermore the data supports our hypothesis.', 'Furthermore the data, supports our hypothesis.', 'Furthermore; the data supports our hypothesis.']); return { id: this.id, category: this.category, question: `Which correctly punctuates the transition word?`, options, correctIndex, explanation: `Transitional words at the beginning of a sentence are followed by a comma.` } } },
  { id: 'pc-q38', category: 'Compound Sentences', generate() { const { options, correctIndex } = makeStringOptions('The test was hard, yet everyone passed.', ['The test was hard yet everyone passed.', 'The test was hard; yet, everyone passed.', 'The test was hard, yet, everyone passed.']); return { id: this.id, category: this.category, question: `Which correctly joins two independent clauses with "yet"?`, options, correctIndex, explanation: `Comma before a coordinating conjunction (yet) joining two independent clauses.` } } },
  { id: 'pc-q39', category: 'Dashes', generate() { const { options, correctIndex } = makeStringOptions('The three items — a book, a pen, and a notebook — were on the desk.', ['The three items, a book, a pen, and a notebook, were on the desk.', 'The three items — a book, a pen, and a notebook were on the desk.', 'The three items a book, a pen, and a notebook — were on the desk.']); return { id: this.id, category: this.category, question: `When a parenthetical list contains commas, dashes prevent confusion:`, options, correctIndex, explanation: `Dashes are clearer than commas when the parenthetical itself contains commas.` } } },
  { id: 'pc-q40', category: 'Mixed Practice', generate() { const { options, correctIndex } = makeStringOptions('Period or semicolon — not a comma', ['A comma is fine', 'Use a colon', 'Use a dash']); return { id: this.id, category: this.category, question: `Two independent clauses, no conjunction. What punctuation between them?`, options, correctIndex, explanation: `Without a conjunction, independent clauses need a period or semicolon — a comma creates a splice.` } } },
]

export function generateExitQuiz(count: number = 10): ExitQuizQuestion[] {
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return shuffle(selected).map(t => t.generate())
}
