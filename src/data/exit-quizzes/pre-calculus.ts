/**
 * Exit Quiz — Pre-Calculus
 * 40 randomized questions covering functions, polynomials, exponentials, logs, trig, analytic trig, sequences, polar
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string }
interface QuestionTemplate { id: string; category: string; generate: () => ExitQuizQuestion }
function randInt(a: number, b: number) { return Math.floor(Math.random() * (b - a + 1)) + a }
function randNonZero(a: number, b: number) { let v = 0; while (v === 0) v = randInt(a, b); return v }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const fillers = ['None of the above', 'Cannot be determined', 'None of these']; for (const f of fillers) { if (u.length >= 3) break; if (f !== c && !u.includes(f)) u.push(f) } const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }
function makeOptions(c: number, s = 3) { const d = new Set<number>(); while (d.size < 3) { const v = c + randInt(-s * 2, s * 2); if (v !== c) d.add(v) }; const all = shuffle([c, ...d]); return { options: all.map(String), correctIndex: all.indexOf(c) } }
function gcd(a: number, b: number): number { a = Math.abs(a); b = Math.abs(b); while (b) { [a, b] = [b, a % b] } return a }
// Format (num/den)·π in lowest terms, e.g. 2/2 -> "\pi", 2/3 -> "\frac{2\pi}{3}", 1/2 -> "\frac{\pi}{2}"
function fmtPiFrac(num: number, den: number): string { const g = gcd(num, den) || 1; const n = num / g, d = den / g; if (d === 1) return n === 1 ? '\\pi' : `${n}\\pi`; return n === 1 ? `\\frac{\\pi}{${d}}` : `\\frac{${n}\\pi}{${d}}` }

const questionPool: QuestionTemplate[] = [
  // ── Functions & Their Graphs ──
  { id: 'pc-q1', category: 'Functions', generate() { const a = randInt(2, 5); const b = randNonZero(-6, 6); const c = randInt(1, 5); const ans = a * c + b; const { options, correctIndex } = makeOptions(ans, 5); return { id: this.id, category: this.category, question: `$f(x) = ${a}x + ${b}$. Find $f(${c})$.`, options, correctIndex, explanation: `$f(${c}) = ${a}(${c}) + ${b} = ${ans}$.` } } },
  { id: 'pc-q2', category: 'Functions', generate() { const a = randInt(2, 5); const { options, correctIndex } = makeStringOptions(`$x \\geq 0$`, [`All real numbers`, `$x > 0$`, `$x \\neq 0$`]); return { id: this.id, category: this.category, question: `Domain of $f(x) = \\sqrt{${a}x}$:`, options, correctIndex, explanation: `Radicand must be $\\geq 0$: $${a}x \\geq 0 \\Rightarrow x \\geq 0$.` } } },
  { id: 'pc-q3', category: 'Functions', generate() { const a = randInt(2, 6); const { options, correctIndex } = makeStringOptions(`$x \\neq ${a}$`, [`$x \\neq -${a}$`, `All real numbers`, `$x > ${a}$`]); return { id: this.id, category: this.category, question: `Domain of $g(x) = \\frac{1}{x - ${a}}$:`, options, correctIndex, explanation: `$x - ${a} \\neq 0$, so $x \\neq ${a}$.` } } },
  { id: 'pc-q4', category: 'Functions', generate() { const h = randNonZero(-5, 5); const k = randNonZero(-5, 5); const dir = h > 0 ? 'right' : 'left'; const vdir = k > 0 ? 'up' : 'down'; const { options, correctIndex } = makeStringOptions(`Shift ${Math.abs(h)} ${dir}, ${Math.abs(k)} ${vdir}`, [`Shift ${Math.abs(h)} ${h > 0 ? 'left' : 'right'}, ${Math.abs(k)} ${vdir}`, `Shift ${Math.abs(k)} ${dir}, ${Math.abs(h)} ${vdir}`, `Reflect over x-axis`]); return { id: this.id, category: this.category, question: `$f(x) = (x - ${h})^2 + ${k}$ compared to $y = x^2$:`, options, correctIndex, explanation: `$(x-h)$ shifts right $h$ units; $+k$ shifts up $k$ units.` } } },
  { id: 'pc-q5', category: 'Functions', generate() { const a = randInt(2, 4); const b = randNonZero(-5, 5); const x = randInt(1, 5); const f = a * x + b; const g = x * x; const { options, correctIndex } = makeOptions(a * (x * x) + b, 6); return { id: this.id, category: this.category, question: `$f(x) = ${a}x + ${b}$, $g(x) = x^2$. Find $f(g(${x}))$.`, options, correctIndex, explanation: `$g(${x}) = ${x * x}$. $f(${x * x}) = ${a}(${x * x}) + ${b} = ${a * x * x + b}$.` } } },
  { id: 'pc-q6', category: 'Functions', generate() { const a = randInt(2, 5); const b = randNonZero(-6, 6); const { options, correctIndex } = makeStringOptions(`$f^{-1}(x) = \\frac{x - ${b}}{${a}}$`, [`$f^{-1}(x) = \\frac{x + ${b}}{${a}}$`, `$f^{-1}(x) = ${a}x - ${b}$`, `$f^{-1}(x) = \\frac{${a}}{x - ${b}}$`]); return { id: this.id, category: this.category, question: `Find the inverse of $f(x) = ${a}x + ${b}$:`, options, correctIndex, explanation: `$y = ${a}x + ${b} \\Rightarrow x = \\frac{y - ${b}}{${a}}$. So $f^{-1}(x) = \\frac{x - ${b}}{${a}}$.` } } },
  // ── Polynomial & Rational Functions ──
  { id: 'pc-q7', category: 'Polynomial & Rational', generate() { const deg = randInt(3, 6); const { options, correctIndex } = makeOptions(deg - 1, 2); return { id: this.id, category: this.category, question: `A degree-${deg} polynomial has at most how many turning points?`, options, correctIndex, explanation: `At most $n - 1 = ${deg - 1}$ turning points.` } } },
  { id: 'pc-q8', category: 'Polynomial & Rational', generate() { const a = randInt(1, 4); const b = randInt(1, 4); const { options, correctIndex } = makeStringOptions(`$y = \\frac{${a}}{${b}}$`, [`$y = 0$`, `$y = ${a + b}$`, `No horizontal asymptote`]); return { id: this.id, category: this.category, question: `Horizontal asymptote of $\\frac{${a}x^2 + 1}{${b}x^2 - 3}$:`, options, correctIndex, explanation: `Same degree → ratio of leading coefficients: $y = \\frac{${a}}{${b}}$.` } } },
  { id: 'pc-q9', category: 'Polynomial & Rational', generate() { const c = randInt(1, 6); const { options, correctIndex } = makeStringOptions(`$x = ${c}$`, [`$x = -${c}$`, `$y = ${c}$`, `$x = 0$`]); return { id: this.id, category: this.category, question: `Vertical asymptote of $\\frac{x + 1}{x - ${c}}$:`, options, correctIndex, explanation: `Set denominator = 0: $x - ${c} = 0 \\Rightarrow x = ${c}$.` } } },
  { id: 'pc-q10', category: 'Polynomial & Rational', generate() { const { options, correctIndex } = makeStringOptions('Falls left, rises right', ['Both rise', 'Both fall', 'Rises left, falls right']); return { id: this.id, category: this.category, question: `End behavior of $f(x) = x^5 - 3x^2 + 1$:`, options, correctIndex, explanation: `Odd degree, positive leading coefficient → falls left, rises right.` } } },
  // ── Exponential & Logarithmic ──
  { id: 'pc-q11', category: 'Exponential & Log', generate() { const base = randInt(2, 5); const exp = randInt(2, 4); const result = Math.pow(base, exp); const { options, correctIndex } = makeOptions(exp, 2); return { id: this.id, category: this.category, question: `$\\log_{${base}} ${result} = ?$`, options, correctIndex, explanation: `$${base}^{${exp}} = ${result}$, so $\\log_{${base}} ${result} = ${exp}$.` } } },
  { id: 'pc-q12', category: 'Exponential & Log', generate() { const { options, correctIndex } = makeStringOptions('$\\ln e = 1$', ['$\\ln e = 0$', '$\\ln e = e$', '$\\ln e = 2$']); return { id: this.id, category: this.category, question: `Evaluate $\\ln e$:`, options, correctIndex, explanation: `$\\ln e = \\log_e e = 1$.` } } },
  { id: 'pc-q13', category: 'Exponential & Log', generate() { const { options, correctIndex } = makeStringOptions('$(0, \\infty)$', ['$(-\\infty, \\infty)$', '$[0, \\infty)$', '$(1, \\infty)$']); return { id: this.id, category: this.category, question: `Domain of $y = \\ln x$:`, options, correctIndex, explanation: `$\\ln x$ is defined for $x > 0$, i.e., $(0, \\infty)$.` } } },
  { id: 'pc-q14', category: 'Exponential & Log', generate() { const { options, correctIndex } = makeStringOptions('They are reflections over $y = x$', ['They are the same graph', 'They are parallel', 'They are reflections over the x-axis']); return { id: this.id, category: this.category, question: `The graphs of $y = e^x$ and $y = \\ln x$ are:`, options, correctIndex, explanation: `Inverse functions are reflections of each other over the line $y = x$.` } } },
  { id: 'pc-q15', category: 'Exponential & Log', generate() { const P = randInt(1, 5) * 1000; const r = randInt(3, 8); const { options, correctIndex } = makeStringOptions(`$A = ${P}e^{0.0${r}t}$`, [`$A = ${P}(1 + 0.0${r})^t$`, `$A = ${P} + 0.0${r}t$`, `$A = ${P}e^{${r}t}$`]); return { id: this.id, category: this.category, question: `Continuous compounding: $P = ${P}$, $r = ${r}\\%$. Formula?`, options, correctIndex, explanation: `Continuous compounding: $A = Pe^{rt} = ${P}e^{0.0${r}t}$.` } } },
  // ── Trigonometric Functions ──
  { id: 'pc-q16', category: 'Trigonometry', generate() { const { options, correctIndex } = makeStringOptions('$\\frac{\\pi}{6}$', ['$\\frac{\\pi}{4}$', '$\\frac{\\pi}{3}$', '$\\frac{\\pi}{2}$']); return { id: this.id, category: this.category, question: `Convert $30°$ to radians:`, options, correctIndex, explanation: `$30° \\times \\frac{\\pi}{180°} = \\frac{\\pi}{6}$.` } } },
  { id: 'pc-q17', category: 'Trigonometry', generate() { const { options, correctIndex } = makeStringOptions('$\\frac{\\sqrt{3}}{2}$', ['$\\frac{1}{2}$', '$\\frac{\\sqrt{2}}{2}$', '$1$']); return { id: this.id, category: this.category, question: `$\\cos(30°) = ?$`, options, correctIndex, explanation: `$\\cos 30° = \\frac{\\sqrt{3}}{2}$.` } } },
  { id: 'pc-q18', category: 'Trigonometry', generate() { const { options, correctIndex } = makeStringOptions('$\\frac{\\sqrt{2}}{2}$', ['$\\frac{1}{2}$', '$\\frac{\\sqrt{3}}{2}$', '$1$']); return { id: this.id, category: this.category, question: `$\\sin(45°) = ?$`, options, correctIndex, explanation: `$\\sin 45° = \\frac{\\sqrt{2}}{2}$.` } } },
  { id: 'pc-q19', category: 'Trigonometry', generate() { const { options, correctIndex } = makeStringOptions('$2\\pi$', ['$\\pi$', '$360$', '$\\frac{\\pi}{2}$']); return { id: this.id, category: this.category, question: `The period of $y = \\sin x$ is:`, options, correctIndex, explanation: `Sine has period $2\\pi$.` } } },
  { id: 'pc-q20', category: 'Trigonometry', generate() { const a = randInt(2, 5); const { options, correctIndex } = makeOptions(a, 2); return { id: this.id, category: this.category, question: `The amplitude of $y = ${a}\\cos x$ is:`, options, correctIndex, explanation: `Amplitude = $|${a}| = ${a}$.` } } },
  { id: 'pc-q21', category: 'Trigonometry', generate() { const b = randInt(2, 4); const { options, correctIndex } = makeStringOptions(`$${fmtPiFrac(2, b)}$`, [`$${b}\\pi$`, `$2\\pi$`, `$\\frac{\\pi}{${b}}$`]); return { id: this.id, category: this.category, question: `The period of $y = \\sin(${b}x)$ is:`, options, correctIndex, explanation: `Period $= \\frac{2\\pi}{b} = \\frac{2\\pi}{${b}} = ${fmtPiFrac(2, b)}$.` } } },
  // ── Trig Identities & Analytic Trig ──
  { id: 'pc-q22', category: 'Identities', generate() { const { options, correctIndex } = makeStringOptions('$\\sin^2\\theta + \\cos^2\\theta = 1$', ['$\\sin^2\\theta - \\cos^2\\theta = 1$', '$\\sin\\theta + \\cos\\theta = 1$', '$\\tan^2\\theta + 1 = \\sec^2\\theta$']); return { id: this.id, category: this.category, question: `The Pythagorean identity is:`, options, correctIndex, explanation: `$\\sin^2\\theta + \\cos^2\\theta = 1$.` } } },
  { id: 'pc-q23', category: 'Identities', generate() { const { options, correctIndex } = makeStringOptions('$2\\sin\\theta\\cos\\theta$', ['$\\sin^2\\theta - \\cos^2\\theta$', '$2\\cos^2\\theta - 1$', '$\\sin\\theta + \\cos\\theta$']); return { id: this.id, category: this.category, question: `$\\sin(2\\theta) = ?$ (double angle formula)`, options, correctIndex, explanation: `$\\sin(2\\theta) = 2\\sin\\theta\\cos\\theta$.` } } },
  { id: 'pc-q24', category: 'Identities', generate() { const { options, correctIndex } = makeStringOptions('$\\frac{\\sin\\theta}{\\cos\\theta}$', ['$\\frac{\\cos\\theta}{\\sin\\theta}$', '$\\sin\\theta \\cdot \\cos\\theta$', '$\\sin\\theta - \\cos\\theta$']); return { id: this.id, category: this.category, question: `$\\tan\\theta = ?$ in terms of $\\sin$ and $\\cos$:`, options, correctIndex, explanation: `$\\tan\\theta = \\frac{\\sin\\theta}{\\cos\\theta}$.` } } },
  // ── Law of Sines / Cosines ──
  { id: 'pc-q25', category: 'Law of Sines/Cosines', generate() { const { options, correctIndex } = makeStringOptions('$\\frac{a}{\\sin A} = \\frac{b}{\\sin B} = \\frac{c}{\\sin C}$', ['$a^2 = b^2 + c^2 - 2bc\\cos A$', '$\\frac{\\sin A}{\\sin B} = \\frac{\\sin B}{\\sin C}$', '$a\\sin A = b\\sin B$']); return { id: this.id, category: this.category, question: `The Law of Sines states:`, options, correctIndex, explanation: `Law of Sines: $\\frac{a}{\\sin A} = \\frac{b}{\\sin B} = \\frac{c}{\\sin C}$.` } } },
  { id: 'pc-q26', category: 'Law of Sines/Cosines', generate() { const { options, correctIndex } = makeStringOptions('$c^2 = a^2 + b^2 - 2ab\\cos C$', ['$\\frac{a}{\\sin A} = \\frac{b}{\\sin B}$', '$c = a + b - 2ab\\cos C$', '$c^2 = a^2 + b^2 + 2ab\\cos C$']); return { id: this.id, category: this.category, question: `The Law of Cosines states:`, options, correctIndex, explanation: `$c^2 = a^2 + b^2 - 2ab\\cos C$.` } } },
  // ── Inverse Trig ──
  { id: 'pc-q27', category: 'Inverse Trig', generate() { const { options, correctIndex } = makeStringOptions('$[-\\frac{\\pi}{2}, \\frac{\\pi}{2}]$', ['$[0, \\pi]$', ['$(-\\infty, \\infty)$', '$[0, 2\\pi]$'][0], '$[0, 2\\pi]$']); return { id: this.id, category: this.category, question: `The range of $\\sin^{-1}(x)$ is:`, options, correctIndex, explanation: `$\\arcsin$ has range $[-\\frac{\\pi}{2}, \\frac{\\pi}{2}]$.` } } },
  { id: 'pc-q28', category: 'Inverse Trig', generate() { const { options, correctIndex } = makeStringOptions('$[0, \\pi]$', ['$[-\\frac{\\pi}{2}, \\frac{\\pi}{2}]$', '$[0, 2\\pi]$', '$(-\\frac{\\pi}{2}, \\frac{\\pi}{2})$']); return { id: this.id, category: this.category, question: `The range of $\\cos^{-1}(x)$ is:`, options, correctIndex, explanation: `$\\arccos$ has range $[0, \\pi]$.` } } },
  // ── Sequences & Series ──
  { id: 'pc-q29', category: 'Sequences & Series', generate() { const a1 = randInt(1, 5); const d = randInt(2, 5); const n = randInt(6, 10); const an = a1 + (n - 1) * d; const sum = n * (a1 + an) / 2; const { options, correctIndex } = makeOptions(sum, 10); return { id: this.id, category: this.category, question: `Arithmetic series: $a_1=${a1}$, $d=${d}$, $n=${n}$. Find $S_{${n}}$.`, options, correctIndex, explanation: `$a_{${n}} = ${a1}+${n - 1}(${d}) = ${an}$. $S_{${n}} = \\frac{${n}}{2}(${a1}+${an}) = ${sum}$.` } } },
  { id: 'pc-q30', category: 'Sequences & Series', generate() { const a1 = randInt(2, 5); const r = 2; const n = randInt(4, 6); const an = a1 * Math.pow(r, n - 1); const sum = a1 * (Math.pow(r, n) - 1) / (r - 1); const { options, correctIndex } = makeOptions(sum, 10); return { id: this.id, category: this.category, question: `Geometric series: $a_1=${a1}$, $r=2$, $n=${n}$. Find $S_{${n}}$.`, options, correctIndex, explanation: `$S_n = a_1 \\frac{r^n - 1}{r - 1} = ${a1} \\cdot \\frac{2^${n}-1}{1} = ${sum}$.` } } },
  // ── Polar Coordinates & Vectors ──
  { id: 'pc-q31', category: 'Polar & Vectors', generate() { const r = randInt(2, 6); const theta = 60; const x = r / 2; const y = r; const { options, correctIndex } = makeStringOptions(`$(\\frac{${r}}{2}, \\frac{${r}\\sqrt{3}}{2})$`, [`$(${r}, 0)$`, `$(0, ${r})$`, `$(\\frac{${r}\\sqrt{3}}{2}, \\frac{${r}}{2})$`]); return { id: this.id, category: this.category, question: `Convert $(${r}, 60°)$ from polar to Cartesian:`, options, correctIndex, explanation: `$x = ${r}\\cos 60° = \\frac{${r}}{2}$, $y = ${r}\\sin 60° = \\frac{${r}\\sqrt{3}}{2}$.` } } },
  { id: 'pc-q32', category: 'Polar & Vectors', generate() { const { options, correctIndex } = makeStringOptions('$r^2 = x^2 + y^2$', ['$r = x + y$', '$r = x^2 + y^2$', '$r^2 = x^2 - y^2$']); return { id: this.id, category: this.category, question: `Relationship between polar $r$ and Cartesian $(x,y)$:`, options, correctIndex, explanation: `$r^2 = x^2 + y^2$ (Pythagorean theorem).` } } },
  { id: 'pc-q33', category: 'Polar & Vectors', generate() { const a1 = randInt(1, 5); const a2 = randInt(1, 5); const b1 = randInt(1, 5); const b2 = randInt(1, 5); const rx = a1 + b1; const ry = a2 + b2; const { options, correctIndex } = makeStringOptions(`$\\langle ${rx}, ${ry} \\rangle$`, [`$\\langle ${a1 * b1}, ${a2 * b2} \\rangle$`, `$\\langle ${rx + 1}, ${ry - 1} \\rangle$`, `$\\langle ${a1 - b1}, ${a2 - b2} \\rangle$`]); return { id: this.id, category: this.category, question: `$\\vec{u} = \\langle ${a1}, ${a2} \\rangle$, $\\vec{v} = \\langle ${b1}, ${b2} \\rangle$. Find $\\vec{u} + \\vec{v}$.`, options, correctIndex, explanation: `Add component-wise: $\\langle ${a1}+${b1}, ${a2}+${b2} \\rangle = \\langle ${rx}, ${ry} \\rangle$.` } } },
  { id: 'pc-q34', category: 'Polar & Vectors', generate() { const a = randInt(1, 5); const b = randInt(1, 5); const mag = a * a + b * b; const { options, correctIndex } = makeStringOptions(`$\\sqrt{${mag}}$`, [`$${a + b}$`, `$${mag}$`, `$\\sqrt{${a * b}}$`]); return { id: this.id, category: this.category, question: `Magnitude of $\\vec{v} = \\langle ${a}, ${b} \\rangle$:`, options, correctIndex, explanation: `$|\\vec{v}| = \\sqrt{${a}^2 + ${b}^2} = \\sqrt{${mag}}$.` } } },
  // ── Mixed ──
  { id: 'pc-q35', category: 'Mixed', generate() { const { options, correctIndex } = makeStringOptions('Even function', ['Odd function', 'Neither', 'Both']); return { id: this.id, category: this.category, question: `$f(x) = x^2 + 3$ is:`, options, correctIndex, explanation: `$f(-x) = (-x)^2 + 3 = x^2 + 3 = f(x)$. Even.` } } },
  { id: 'pc-q36', category: 'Mixed', generate() { const { options, correctIndex } = makeStringOptions('Odd function', ['Even function', 'Neither', 'Both']); return { id: this.id, category: this.category, question: `$f(x) = x^3$ is:`, options, correctIndex, explanation: `$f(-x) = (-x)^3 = -x^3 = -f(x)$. Odd.` } } },
  { id: 'pc-q37', category: 'Mixed', generate() { const a = randInt(2, 5); const limit = randInt(1, 10); const { options, correctIndex } = makeStringOptions(`$(-\\infty, ${limit}]$`, [`$(-\\infty, \\infty)$`, `$[${limit}, \\infty)$`, `$(0, ${limit})$`]); return { id: this.id, category: this.category, question: `Range of $f(x) = -${a}(x-1)^2 + ${limit}$:`, options, correctIndex, explanation: `Parabola opens down with vertex at $(1, ${limit})$. Range: $(-\\infty, ${limit}]$.` } } },
  { id: 'pc-q38', category: 'Trigonometry', generate() { const { options, correctIndex } = makeStringOptions('$0$', ['$1$', '$-1$', 'Undefined']); return { id: this.id, category: this.category, question: `$\\sin(0) = ?$`, options, correctIndex, explanation: `$\\sin(0) = 0$.` } } },
  { id: 'pc-q39', category: 'Trigonometry', generate() { const { options, correctIndex } = makeStringOptions('Undefined', ['$0$', '$1$', '$-1$']); return { id: this.id, category: this.category, question: `$\\tan(90°) = ?$`, options, correctIndex, explanation: `$\\tan 90° = \\frac{\\sin 90°}{\\cos 90°} = \\frac{1}{0}$, which is undefined.` } } },
  { id: 'pc-q40', category: 'Exponential & Log', generate() { const a = randInt(2, 4); const b = randInt(2, 4); const { options, correctIndex } = makeStringOptions(`$\\log_{${a}} ${b}$`, [`$\\frac{\\ln ${a}}{\\ln ${b}}$`, `$\\ln(${a} \\cdot ${b})$`, `$\\frac{${a}}{${b}}$`]); return { id: this.id, category: this.category, question: `Change of base: $\\frac{\\ln ${b}}{\\ln ${a}} = ?$`, options, correctIndex, explanation: `$\\frac{\\ln ${b}}{\\ln ${a}} = \\log_{${a}} ${b}$ (change of base formula).` } } },
]

export function generateExitQuiz(count: number = 10): ExitQuizQuestion[] {
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return shuffle(selected).map(t => t.generate())
}
