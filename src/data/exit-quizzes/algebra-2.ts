/**
 * Exit Quiz — Algebra 2
 * 40 randomized questions covering quadratics, polynomials, rational expressions, exponentials, logs, radicals, complex numbers, sequences
 */
import { textMatchesSlug } from './relevance-fallback'
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string }
interface QuestionTemplate { id: string; category: string; generate: () => ExitQuizQuestion }
function randInt(a: number, b: number) { return Math.floor(Math.random() * (b - a + 1)) + a }
function randNonZero(a: number, b: number) { let v = 0; while (v === 0) v = randInt(a, b); return v }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const fillers = ['None of the above', 'Cannot be determined', 'None of these']; for (const f of fillers) { if (u.length >= 3) break; if (f !== c && !u.includes(f)) u.push(f) } const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }
function makeOptions(c: number, s = 3) { const d = new Set<number>(); while (d.size < 3) { const v = c + randInt(-s * 2, s * 2); if (v !== c) d.add(v) }; const all = shuffle([c, ...d]); return { options: all.map(String), correctIndex: all.indexOf(c) } }

const questionPool: QuestionTemplate[] = [
  // ── Quadratics & Completing the Square ──
  { id: 'a2-q1', category: 'Quadratics', generate() { const h = randInt(1, 6); const k = randNonZero(-8, 8); const { options, correctIndex } = makeStringOptions(`$(${h}, ${k})$`, [`$(-${h}, ${k})$`, `$(${h}, ${-k})$`, `$(${-h}, ${-k})$`]); return { id: this.id, category: this.category, question: `Find the vertex of $y = (x - ${h})^2 + ${k}$`, options, correctIndex, explanation: `Vertex form $y = (x-h)^2+k$ gives vertex $(h,k) = (${h}, ${k})$.` } } },
  { id: 'a2-q2', category: 'Quadratics', generate() { const _a = randInt(1, 3); const r1 = randInt(-5, -1); const r2 = randInt(1, 5); const sum = -(r1 + r2); const prod = r1 * r2; const { options, correctIndex } = makeStringOptions(`$x = ${r1}$ or $x = ${r2}$`, [`$x = ${-r1}$ or $x = ${-r2}$`, `$x = ${sum}$`, `$x = ${prod}$`]); return { id: this.id, category: this.category, question: `Solve: $x^2 + ${sum}x + ${prod} = 0$`, options, correctIndex, explanation: `Factor: $(x - ${r1 < 0 ? '(' + r1 + ')' : r1})(x - ${r2}) = 0$.` } } },
  { id: 'a2-q3', category: 'Quadratics', generate() { const _a = 1; const bHalf = randInt(1, 5); const b = 2 * bHalf; const { options, correctIndex } = makeStringOptions(`$(x + ${bHalf})^2 - ${bHalf * bHalf}$`, [`$(x + ${b})^2 - ${b * b}$`, `$(x - ${bHalf})^2 + ${bHalf * bHalf}$`, `$(x + ${bHalf})^2 + ${bHalf * bHalf}$`]); return { id: this.id, category: this.category, question: `Complete the square: $x^2 + ${b}x$`, options, correctIndex, explanation: `$x^2 + ${b}x = (x + ${bHalf})^2 - ${bHalf * bHalf}$.` } } },
  { id: 'a2-q4', category: 'Quadratics', generate() { const a = 1; const b = randInt(2, 8); const c = randInt(1, 10); const disc = b * b - 4 * a * c; let ans: string; if (disc > 0) ans = 'Two real solutions'; else if (disc === 0) ans = 'One repeated real solution'; else ans = 'Two complex solutions'; const wrong = ['Two real solutions', 'One repeated real solution', 'Two complex solutions', 'No solutions'].filter(x => x !== ans); const { options, correctIndex } = makeStringOptions(ans, wrong); return { id: this.id, category: this.category, question: `How many solutions does $x^2 + ${b}x + ${c} = 0$ have? ($\\Delta = ${disc}$)`, options, correctIndex, explanation: `Discriminant = ${disc}. ${disc > 0 ? 'Positive → two real' : disc === 0 ? 'Zero → one repeated' : 'Negative → two complex'}.` } } },
  // ── Polynomials ──
  { id: 'a2-q5', category: 'Polynomials', generate() { const deg = randInt(3, 5); const turns = deg - 1; const { options, correctIndex } = makeOptions(turns, 2); return { id: this.id, category: this.category, question: `A polynomial of degree ${deg} has at most how many turning points?`, options, correctIndex, explanation: `At most $n - 1 = ${turns}$ turning points.` } } },
  { id: 'a2-q6', category: 'Polynomials', generate() { const a = randInt(2, 4); const b = randNonZero(-5, 5); const c = randNonZero(-5, 5); const r = randInt(-3, 3); const val = a * r * r * r + b * r + c; const { options, correctIndex } = makeOptions(val, 6); return { id: this.id, category: this.category, question: `$f(x) = ${a}x^3 + ${b}x + ${c}$. Find $f(${r})$.`, options, correctIndex, explanation: `$f(${r}) = ${a}(${r})^3 + ${b}(${r}) + ${c} = ${val}$.` } } },
  { id: 'a2-q7', category: 'Polynomials', generate() { const { options, correctIndex } = makeStringOptions('Falls left, rises right', ['Both ends rise', 'Both ends fall', 'Rises left, falls right']); return { id: this.id, category: this.category, question: `End behavior of $f(x) = 2x^3 - x + 4$? (odd degree, positive leading coeff)`, options, correctIndex, explanation: `Odd degree + positive → falls left, rises right.` } } },
  { id: 'a2-q8', category: 'Polynomials', generate() { const a = randInt(1, 4); const b = randInt(1, 4); const c = randInt(1, 4); const { options, correctIndex } = makeStringOptions(`$x^2 + ${a + b}x + ${a * b}$`, [`$x^2 + ${a * b}x + ${a + b}$`, `$x^2 + ${a}x + ${b}$`, `$x^2 - ${a + b}x + ${a * b}$`]); return { id: this.id, category: this.category, question: `The quotient when $x^3 + ${a + b + c}x^2 + ${a * b + a * c + b * c}x + ${a * b * c}$ is divided by $(x + ${c})$ is:`, options, correctIndex, explanation: `Synthetic division with $-${c}$ gives $x^2 + ${a + b}x + ${a * b}$.` } } },
  { id: 'a2-q9', category: 'Polynomials', generate() { const r = randInt(1, 6); const { options, correctIndex } = makeStringOptions(`$(x - ${r})$ is a factor`, [`$(x + ${r})$ is a factor`, `$(x - ${r})$ is not a factor`, `$f(${r}) = ${r}$`]); return { id: this.id, category: this.category, question: `If $f(${r}) = 0$, the Factor Theorem tells us:`, options, correctIndex, explanation: `If $f(c)=0$ then $(x-c)$ is a factor.` } } },
  // ── Rational Expressions ──
  { id: 'a2-q10', category: 'Rational Expressions', generate() { const a = randInt(2, 6); const { options, correctIndex } = makeStringOptions(`$x \\neq -${a}$`, [`$x \\neq ${a}$`, `All real numbers`, `$x > 0$`]); return { id: this.id, category: this.category, question: `State the restriction: $\\frac{3x}{x + ${a}}$`, options, correctIndex, explanation: `Denominator $\\neq 0$: $x + ${a} \\neq 0$, so $x \\neq -${a}$.` } } },
  { id: 'a2-q11', category: 'Rational Expressions', generate() { const a = randInt(2, 5); const b = randInt(1, 5); const { options, correctIndex } = makeStringOptions(`$\\frac{${a}}{x + ${b}}$`, [`$\\frac{${a}}{x - ${b}}$`, `$\\frac{x + ${b}}{${a}}$`, `$\\frac{1}{${a}(x + ${b})}$`]); return { id: this.id, category: this.category, question: `Simplify: $\\frac{${a}x + ${a * b}}{x^2 + ${2 * b}x + ${b * b}}$`, options, correctIndex, explanation: `Factor: $\\frac{${a}(x+${b})}{(x+${b})^2} = \\frac{${a}}{x+${b}}$.` } } },
  { id: 'a2-q12', category: 'Rational Expressions', generate() { const a = randInt(2, 5); const { options, correctIndex } = makeStringOptions(`$x = -${a}$`, [`$x = ${a}$`, `$x = 0$ and $x = -${a}$`, `No solution`]); return { id: this.id, category: this.category, question: `Identify the vertical asymptote of $y = \\frac{1}{x + ${a}}$:`, options, correctIndex, explanation: `Set denominator = 0: $x + ${a} = 0$, so $x = -${a}$.` } } },
  // ── Exponential & Logarithmic ──
  { id: 'a2-q13', category: 'Exponential & Log', generate() { const base = randInt(2, 5); const exp = randInt(2, 4); const result = Math.pow(base, exp); const { options, correctIndex } = makeOptions(exp, 2); return { id: this.id, category: this.category, question: `Evaluate: $\\log_{${base}} ${result}$`, options, correctIndex, explanation: `$${base}^{${exp}} = ${result}$, so $\\log_{${base}} ${result} = ${exp}$.` } } },
  { id: 'a2-q14', category: 'Exponential & Log', generate() { const { options, correctIndex } = makeStringOptions('$\\log a + \\log b$', ['$\\log a \\cdot \\log b$', '$\\log(a - b)$', '$\\frac{\\log a}{\\log b}$']); return { id: this.id, category: this.category, question: `$\\log(ab) = ?$ (product rule)`, options, correctIndex, explanation: `Product rule: $\\log(ab) = \\log a + \\log b$.` } } },
  { id: 'a2-q15', category: 'Exponential & Log', generate() { const n = randInt(2, 4); const { options, correctIndex } = makeStringOptions(`$${n}\\log x$`, [`$\\log(${n}x)$`, `$\\log x + ${n}$`, `$(\\log x)^{${n}}$`]); return { id: this.id, category: this.category, question: `$\\log(x^{${n}}) = ?$ (power rule)`, options, correctIndex, explanation: `Power rule: $\\log(x^n) = n \\log x = ${n}\\log x$.` } } },
  { id: 'a2-q16', category: 'Exponential & Log', generate() { const a = randInt(2, 5); const b = randInt(2, 4); const result = Math.pow(a, b); const { options, correctIndex } = makeOptions(result, 10); return { id: this.id, category: this.category, question: `If $\\log_{${a}} x = ${b}$, find $x$.`, options, correctIndex, explanation: `$x = ${a}^{${b}} = ${result}$.` } } },
  { id: 'a2-q17', category: 'Exponential & Log', generate() { const { options, correctIndex } = makeStringOptions('Exponential growth', ['Exponential decay', 'Linear growth', 'Logarithmic growth']); return { id: this.id, category: this.category, question: `$f(x) = 3 \\cdot 2^x$ represents:`, options, correctIndex, explanation: `Base > 1 → exponential growth.` } } },
  { id: 'a2-q18', category: 'Exponential & Log', generate() { const { options, correctIndex } = makeStringOptions('Exponential decay', ['Exponential growth', 'Linear decay', 'No change']); return { id: this.id, category: this.category, question: `$f(x) = 5 \\cdot (0.5)^x$ represents:`, options, correctIndex, explanation: `$0 < $ base $< 1$ → exponential decay.` } } },
  { id: 'a2-q19', category: 'Exponential & Log', generate() { const base = randInt(2, 4); const { options, correctIndex } = makeStringOptions('$y = 0$', [`$y = ${base}$`, `$y = 1$`, `$x = 0$`]); return { id: this.id, category: this.category, question: `The horizontal asymptote of $y = ${base}^x$ is:`, options, correctIndex, explanation: `Exponential functions have horizontal asymptote $y = 0$.` } } },
  // ── Radical Functions & Complex Numbers ──
  { id: 'a2-q20', category: 'Radicals & Complex', generate() { const a = randInt(2, 6); const b = randInt(2, 6); const sum = a + b; const { options, correctIndex } = makeStringOptions(`$${sum}\\sqrt{3}$`, [`$\\sqrt{${sum * 3}}$`, `$${a * b}\\sqrt{3}$`, `$${sum}$`]); return { id: this.id, category: this.category, question: `Simplify: $${a}\\sqrt{3} + ${b}\\sqrt{3}$`, options, correctIndex, explanation: `Like radicals: $(${a}+${b})\\sqrt{3} = ${sum}\\sqrt{3}$.` } } },
  { id: 'a2-q21', category: 'Radicals & Complex', generate() { const a = randInt(2, 5); const sq = a * a; const { options, correctIndex } = makeStringOptions(`$${a}i$`, [`$-${a}$`, `$${a}$`, `$-${a}i$`]); return { id: this.id, category: this.category, question: `Simplify: $\\sqrt{-${sq}}$`, options, correctIndex, explanation: `$\\sqrt{-${sq}} = \\sqrt{${sq}} \\cdot \\sqrt{-1} = ${a}i$.` } } },
  { id: 'a2-q22', category: 'Radicals & Complex', generate() { const a = randInt(1, 5); const b = randInt(1, 5); const c = randInt(1, 5); const d = randInt(1, 5); const real = a + c; const imag = b + d; const { options, correctIndex } = makeStringOptions(`$${real} + ${imag}i$`, [`$${real} - ${imag}i$`, `$${a * c} + ${b * d}i$`, `$${real + imag}i$`]); return { id: this.id, category: this.category, question: `$(${a} + ${b}i) + (${c} + ${d}i) = ?$`, options, correctIndex, explanation: `Add real parts and imaginary parts: $(${a}+${c}) + (${b}+${d})i = ${real} + ${imag}i$.` } } },
  { id: 'a2-q23', category: 'Radicals & Complex', generate() { const { options, correctIndex } = makeStringOptions('$-1$', ['$1$', '$i$', '$-i$']); return { id: this.id, category: this.category, question: `$i^2 = ?$`, options, correctIndex, explanation: `By definition, $i^2 = -1$.` } } },
  { id: 'a2-q24', category: 'Radicals & Complex', generate() { const a = randInt(2, 6); const _n = 3; const cube = a * a * a; const { options, correctIndex } = makeOptions(a, 2); return { id: this.id, category: this.category, question: `$\\sqrt[3]{${cube}} = ?$`, options, correctIndex, explanation: `$${a}^3 = ${cube}$, so $\\sqrt[3]{${cube}} = ${a}$.` } } },
  // ── Sequences & Series ──
  { id: 'a2-q25', category: 'Sequences & Series', generate() { const a1 = randInt(1, 10); const d = randInt(2, 6); const n = randInt(5, 10); const an = a1 + (n - 1) * d; const { options, correctIndex } = makeOptions(an, 5); return { id: this.id, category: this.category, question: `Arithmetic sequence: $a_1 = ${a1}$, $d = ${d}$. Find $a_{${n}}$.`, options, correctIndex, explanation: `$a_n = a_1 + (n-1)d = ${a1} + ${n - 1}(${d}) = ${an}$.` } } },
  { id: 'a2-q26', category: 'Sequences & Series', generate() { const a1 = randInt(1, 4); const r = randInt(2, 3); const n = randInt(4, 6); const an = a1 * Math.pow(r, n - 1); const { options, correctIndex } = makeOptions(an, 10); return { id: this.id, category: this.category, question: `Geometric sequence: $a_1 = ${a1}$, $r = ${r}$. Find $a_{${n}}$.`, options, correctIndex, explanation: `$a_n = a_1 \\cdot r^{n-1} = ${a1} \\cdot ${r}^{${n - 1}} = ${an}$.` } } },
  { id: 'a2-q27', category: 'Sequences & Series', generate() { const a1 = randInt(1, 5); const an = a1 + randInt(10, 20); const n = randInt(5, 8); const sum = n * (a1 + an) / 2; const { options, correctIndex } = makeOptions(sum, 8); return { id: this.id, category: this.category, question: `Sum of arithmetic series: $a_1=${a1}$, $a_{${n}}=${an}$, $n=${n}$. $S_{${n}} = ?$`, options, correctIndex, explanation: `$S_n = \\frac{n}{2}(a_1 + a_n) = \\frac{${n}}{2}(${a1}+${an}) = ${sum}$.` } } },
  { id: 'a2-q28', category: 'Sequences & Series', generate() { const { options, correctIndex } = makeStringOptions('Common difference', ['Common ratio', 'First term', 'Sum']); return { id: this.id, category: this.category, question: `In an arithmetic sequence, the constant added each time is the:`, options, correctIndex, explanation: `Arithmetic sequences have a common difference $d$.` } } },
  // ── Counting & Probability ──
  { id: 'a2-q29', category: 'Counting & Probability', generate() { const n = randInt(4, 7); let fact = 1; for (let i = 2; i <= n; i++) fact *= i; const { options, correctIndex } = makeOptions(fact, 20); return { id: this.id, category: this.category, question: `$${n}! = ?$`, options, correctIndex, explanation: `$${n}! = ${Array.from({ length: n }, (_, i) => n - i).join(' \\times ')} = ${fact}$.` } } },
  { id: 'a2-q30', category: 'Counting & Probability', generate() { const n = randInt(2, 4); const coeffs = [1]; const row = n; for (let k = 1; k <= row; k++) coeffs.push(coeffs[k - 1] * (row - k + 1) / k); const { options, correctIndex } = makeStringOptions(coeffs.join(', '), [coeffs.reverse().join(', '), coeffs.map((_, i) => i + 1).join(', '), coeffs.map(() => row).join(', ')]); return { id: this.id, category: this.category, question: `Row ${n} of Pascal's triangle (starting from row 0) is:`, options, correctIndex, explanation: `Use binomial coefficients: $\\binom{${n}}{k}$ for $k = 0, 1, \\ldots, ${n}$.` } } },
  // ── Mixed / Application ──
  { id: 'a2-q31', category: 'Application', generate() { const r = randInt(5, 15); const { options, correctIndex } = makeStringOptions(`$f(x) = 1000(1.0${r})^x$`, [`$f(x) = 1000 + ${r}x$`, `$f(x) = 1000(0.${100 - r})^x$`, `$f(x) = 1000(${r})^x$`]); return { id: this.id, category: this.category, question: `\\$1000 grows at ${r}% per year. Which models the amount after $x$ years?`, options, correctIndex, explanation: `Exponential growth: $A = P(1 + r)^t = 1000(1.0${r})^x$.` } } },
  { id: 'a2-q32', category: 'Application', generate() { const half = randInt(2, 8); const { options, correctIndex } = makeStringOptions(`$A = A_0 \\cdot (\\frac{1}{2})^{t/${half}}$`, [`$A = A_0 \\cdot 2^{t/${half}}$`, `$A = A_0 - ${half}t$`, `$A = A_0 \\cdot (\\frac{1}{${half}})^t$`]); return { id: this.id, category: this.category, question: `A substance has a half-life of ${half} years. The decay model is:`, options, correctIndex, explanation: `Half-life formula: $A = A_0 (1/2)^{t/h}$ where $h = ${half}$.` } } },
  { id: 'a2-q33', category: 'Quadratics', generate() { const _a = 1; const b = randInt(-6, -2); const c = randInt(1, 8); const vertex_x = -b / 2; const { options, correctIndex } = makeOptions(vertex_x, 3); return { id: this.id, category: this.category, question: `The axis of symmetry of $y = x^2 + ${b}x + ${c}$ is $x = ?$`, options, correctIndex, explanation: `$x = -\\frac{b}{2a} = -\\frac{${b}}{2} = ${vertex_x}$.` } } },
  { id: 'a2-q34', category: 'Polynomials', generate() { const a = randInt(2, 5); const cube = a * a * a; const { options, correctIndex } = makeStringOptions(`$(x - ${a})(x^2 + ${a}x + ${a * a})$`, [`$(x + ${a})(x^2 - ${a}x + ${a * a})$`, `$(x - ${a})^3$`, `Cannot be factored`]); return { id: this.id, category: this.category, question: `Factor: $x^3 - ${cube}$ (difference of cubes)`, options, correctIndex, explanation: `$a^3 - b^3 = (a-b)(a^2+ab+b^2)$. $x^3 - ${a}^3 = (x-${a})(x^2+${a}x+${a * a})$.` } } },
  { id: 'a2-q35', category: 'Rational Expressions', generate() { const a = randInt(2, 5); const b = randInt(1, 5); const { options, correctIndex } = makeStringOptions(`$\\frac{${a}x + ${b}}{x}$`, [`$${a} + ${b}$`, `$\\frac{${a}}{x} + ${b}$`, `$${a}x + \\frac{${b}}{x}$`]); return { id: this.id, category: this.category, question: `Add: $${a} + \\frac{${b}}{x}$. Write as a single fraction.`, options, correctIndex, explanation: `$\\frac{${a}x}{x} + \\frac{${b}}{x} = \\frac{${a}x + ${b}}{x}$.` } } },
  { id: 'a2-q36', category: 'Exponential & Log', generate() { const { options, correctIndex } = makeStringOptions('$e \\approx 2.718$', ['$e = 3$', '$e = \\pi$', '$e \\approx 1.618$']); return { id: this.id, category: this.category, question: `The natural base $e$ is approximately:`, options, correctIndex, explanation: `$e \\approx 2.71828\\ldots$` } } },
  { id: 'a2-q37', category: 'Exponential & Log', generate() { const { options, correctIndex } = makeStringOptions('$\\ln x$', ['$\\log_{10} x$', '$\\log_2 x$', '$e^x$']); return { id: this.id, category: this.category, question: `$\\log_e x$ is commonly written as:`, options, correctIndex, explanation: `The natural logarithm $\\log_e x = \\ln x$.` } } },
  { id: 'a2-q38', category: 'Sequences & Series', generate() { const a1 = randInt(1, 5); const r = 0.5; const sum = a1 / (1 - r); const { options, correctIndex } = makeOptions(sum * 2, 4); return { id: this.id, category: this.category, question: `Infinite geometric series: $a_1 = ${a1 * 2}$, $r = \\frac{1}{2}$. $S = ?$`, options, correctIndex, explanation: `$S = \\frac{a_1}{1-r} = \\frac{${a1 * 2}}{1/2} = ${a1 * 2 * 2}$. Wait — $S = \\frac{${a1 * 2}}{1 - 1/2} = \\frac{${a1 * 2}}{1/2} = ${a1 * 4}$.` } } },
  { id: 'a2-q39', category: 'Radicals & Complex', generate() { const a = randInt(2, 5); const { options, correctIndex } = makeStringOptions(`$x = ${a * a}$`, [`$x = ${a}$`, `$x = ${2 * a}$`, `$x = -${a * a}$`]); return { id: this.id, category: this.category, question: `Solve: $\\sqrt{x} = ${a}$`, options, correctIndex, explanation: `Square both sides: $x = ${a}^2 = ${a * a}$.` } } },
  { id: 'a2-q40', category: 'Quadratics', generate() { const a = randInt(1, 3); const b = randInt(1, 6); const c = -(a * 4 + b); const { options, correctIndex } = makeStringOptions('Upward', ['Downward', 'Left', 'Right']); return { id: this.id, category: this.category, question: `The parabola $y = ${a}x^2 + ${b}x + ${c}$ opens:`, options, correctIndex, explanation: `$a = ${a} > 0$, so the parabola opens upward.` } } },
]

export function generateExitQuiz(count: number = 10, topicSlug?: string): ExitQuizQuestion[] {
  let sourcePool = questionPool
  if (topicSlug) {
    // Match templates to the topic by category + a sample generation's text.
    const matched = questionPool.filter(t => { const s = t.generate(); return textMatchesSlug(`${t.category} ${s.question} ${s.explanation}`, topicSlug) })
    if (matched.length >= 3) sourcePool = matched
  }
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of sourcePool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = sourcePool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return shuffle(selected).map(t => t.generate())
}
