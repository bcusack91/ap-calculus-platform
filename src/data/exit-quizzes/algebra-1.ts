/**
 * Exit Quiz — Algebra 1
 * 40 randomized questions covering expressions, linear functions, systems, inequalities, polynomials, exponents
 */
import { textMatchesSlug } from './relevance-fallback'
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string }
interface QuestionTemplate { id: string; category: string; generate: () => ExitQuizQuestion }
function randInt(a: number, b: number) { return Math.floor(Math.random() * (b - a + 1)) + a }
function randNonZero(a: number, b: number) { let v = 0; while (v === 0) v = randInt(a, b); return v }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const fillers = ['None of the above', 'Cannot be determined', 'None of these']; for (const f of fillers) { if (u.length >= 3) break; if (f !== c && !u.includes(f)) u.push(f) } const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }
function makeOptions(c: number, s = 3) { const d = new Set<number>(); while (d.size < 3) { const v = c + randInt(-s * 2, s * 2); if (v !== c) d.add(v) }; const all = shuffle([c, ...d]); return { options: all.map(String), correctIndex: all.indexOf(c) } }

const questionPool: QuestionTemplate[] = [
  // ── Expressions & Equations ──
  { id: 'a1-q1', category: 'Expressions & Equations', generate() { const a = randInt(2, 9); const b = randInt(1, 12); const ans = a + b; const { options, correctIndex } = makeOptions(ans, 4); return { id: this.id, category: this.category, question: `Solve: $x - ${a} = ${b}$`, options, correctIndex, explanation: `Add ${a} to both sides: $x = ${b} + ${a} = ${ans}$.` } } },
  { id: 'a1-q2', category: 'Expressions & Equations', generate() { const a = randInt(2, 8); const b = randInt(2, 12); const prod = a * b; const { options, correctIndex } = makeOptions(b, 3); return { id: this.id, category: this.category, question: `Solve: $${a}x = ${prod}$`, options, correctIndex, explanation: `Divide both sides by ${a}: $x = ${prod}/${a} = ${b}$.` } } },
  { id: 'a1-q3', category: 'Expressions & Equations', generate() { const a = randInt(2, 5); const b = randInt(1, 6); const c = randInt(1, 20); const _x = (c - b) / a; const ans = c - b; const { options, correctIndex } = makeOptions(ans, 5); return { id: this.id, category: this.category, question: `Solve for the value of $${a}x + ${b}$ when $${a}x + ${b} = ${c}$. What is $${a}x$?`, options, correctIndex, explanation: `$${a}x = ${c} - ${b} = ${ans}$.` } } },
  { id: 'a1-q4', category: 'Expressions & Equations', generate() { const a = randInt(2, 6); const b = randInt(1, 10); const c = randInt(2, 6); const d = randInt(1, 10); const _rhs = c * 5 + d - a * 5 - b; const { options, correctIndex } = makeStringOptions(`$x = ${5}$`, [`$x = ${-5}$`, `$x = ${3}$`, `$x = ${7}$`]); return { id: this.id, category: this.category, question: `Solve: $${a}x + ${b} = ${a * 5 + b}$`, options, correctIndex, explanation: `$${a}x = ${a * 5}$, so $x = 5$.` } } },
  { id: 'a1-q5', category: 'Expressions & Equations', generate() { const coeff = randInt(2, 6); const val = randInt(3, 8); const rhs = coeff * val; const { options, correctIndex } = makeOptions(val, 3); return { id: this.id, category: this.category, question: `If $\\frac{${rhs}}{x} = ${coeff}$, what is $x$?`, options, correctIndex, explanation: `$x = \\frac{${rhs}}{${coeff}} = ${val}$.` } } },
  { id: 'a1-q6', category: 'Expressions & Equations', generate() { const a = randInt(2, 5); const b = randInt(1, 8); const c = randInt(1, 5); const d = randInt(1, 8); const lhs = a - c; const rhs = d - b; const { options, correctIndex } = makeOptions(rhs, 4); return { id: this.id, category: this.category, question: `Solve: $${a}x + ${b} = ${c}x + ${d}$. What is $${lhs}x$?`, options, correctIndex, explanation: `$${a}x - ${c}x = ${d} - ${b}$, so $${lhs}x = ${rhs}$.` } } },
  // ── Linear Functions & Graphing ──
  { id: 'a1-q7', category: 'Linear Functions', generate() { const m = randInt(1, 6); const b = randNonZero(-8, 8); const { options, correctIndex } = makeOptions(m, 3); return { id: this.id, category: this.category, question: `What is the slope of $y = ${m}x + ${b}$?`, options, correctIndex, explanation: `In $y = mx + b$, the slope $m = ${m}$.` } } },
  { id: 'a1-q8', category: 'Linear Functions', generate() { const m = randInt(1, 5); const b = randNonZero(-6, 6); const { options, correctIndex } = makeOptions(b, 4); return { id: this.id, category: this.category, question: `What is the y-intercept of $y = ${m}x + ${b}$?`, options, correctIndex, explanation: `In $y = mx + b$, the y-intercept is $b = ${b}$.` } } },
  { id: 'a1-q9', category: 'Linear Functions', generate() { const x1 = randInt(0, 4); const y1 = randInt(0, 6); const x2 = x1 + randInt(1, 4); const y2 = y1 + randInt(1, 8); const rise = y2 - y1; const run = x2 - x1; const { options, correctIndex } = makeStringOptions(`$\\frac{${rise}}{${run}}$`, [`$\\frac{${run}}{${rise}}$`, `$\\frac{-${rise}}{${run}}$`, `$${rise + run}$`]); return { id: this.id, category: this.category, question: `Find the slope between $(${x1}, ${y1})$ and $(${x2}, ${y2})$.`, options, correctIndex, explanation: `Slope $= \\frac{${y2}-${y1}}{${x2}-${x1}} = \\frac{${rise}}{${run}}$.` } } },
  { id: 'a1-q10', category: 'Linear Functions', generate() { const m = randInt(1, 4); const b = randNonZero(-5, 5); const xVal = randInt(1, 5); const yVal = m * xVal + b; const { options, correctIndex } = makeOptions(yVal, 4); return { id: this.id, category: this.category, question: `If $f(x) = ${m}x + ${b}$, find $f(${xVal})$.`, options, correctIndex, explanation: `$f(${xVal}) = ${m}(${xVal}) + ${b} = ${m * xVal} + ${b} = ${yVal}$.` } } },
  { id: 'a1-q11', category: 'Linear Functions', generate() { const m = randInt(2, 6); const { options, correctIndex } = makeStringOptions(`$y = ${m}x + b$`, [`$y = -${m}x + b$`, `$y = \\frac{1}{${m}}x + b$`, `$y = -\\frac{1}{${m}}x + b$`]); return { id: this.id, category: this.category, question: `A line parallel to $y = ${m}x - 3$ has slope:`, options, correctIndex, explanation: `Parallel lines have the same slope. Slope = ${m}.` } } },
  { id: 'a1-q12', category: 'Linear Functions', generate() { const m = randInt(2, 5); const { options, correctIndex } = makeStringOptions(`$-\\frac{1}{${m}}$`, [`$\\frac{1}{${m}}$`, `$${m}$`, `$-${m}$`]); return { id: this.id, category: this.category, question: `A line perpendicular to $y = ${m}x + 1$ has slope:`, options, correctIndex, explanation: `Perpendicular slope is the negative reciprocal: $-\\frac{1}{${m}}$.` } } },
  { id: 'a1-q13', category: 'Linear Functions', generate() { const m = randNonZero(-4, 4); const x1 = randInt(1, 5); const y1 = randInt(-5, 5); const { options, correctIndex } = makeStringOptions(`$y - ${y1} = ${m}(x - ${x1})$`, [`$y + ${y1} = ${m}(x + ${x1})$`, `$y = ${m}x - ${x1}$`, `$y - ${x1} = ${m}(x - ${y1})$`]); return { id: this.id, category: this.category, question: `Write point-slope form through $(${x1}, ${y1})$ with slope ${m}:`, options, correctIndex, explanation: `Point-slope: $y - y_1 = m(x - x_1)$.` } } },
  // ── Systems of Equations ──
  { id: 'a1-q14', category: 'Systems', generate() { const x = randInt(1, 5); const y = randInt(1, 5); const _a1 = 1; const _b1 = 1; const c1 = x + y; const _a2 = 1; const _b2 = -1; const c2 = x - y; const { options, correctIndex } = makeStringOptions(`$(${x}, ${y})$`, [`$(${y}, ${x})$`, `$(${x + 1}, ${y - 1})$`, `$(${-x}, ${-y})$`]); return { id: this.id, category: this.category, question: `Solve: $x + y = ${c1}$ and $x - y = ${c2}$`, options, correctIndex, explanation: `Adding: $2x = ${c1 + c2}$, $x = ${x}$. Then $y = ${y}$.` } } },
  { id: 'a1-q15', category: 'Systems', generate() { const { options, correctIndex } = makeStringOptions('One solution (intersecting lines)', ['No solution (parallel lines)', 'Infinitely many solutions', 'Cannot be determined']); return { id: this.id, category: this.category, question: `$y = 2x + 1$ and $y = -x + 4$ have:`, options, correctIndex, explanation: `Different slopes → lines intersect at exactly one point.` } } },
  { id: 'a1-q16', category: 'Systems', generate() { const m = randInt(2, 5); const b1 = randInt(1, 6); const b2 = b1 + randInt(1, 4); const { options, correctIndex } = makeStringOptions('No solution', ['One solution', 'Infinitely many solutions', 'Two solutions']); return { id: this.id, category: this.category, question: `$y = ${m}x + ${b1}$ and $y = ${m}x + ${b2}$ have:`, options, correctIndex, explanation: `Same slope, different y-intercepts → parallel lines → no solution.` } } },
  { id: 'a1-q17', category: 'Systems', generate() { const x = randInt(1, 6); const y = randInt(1, 6); const a = randInt(2, 4); const c = a * x + y; const { options, correctIndex } = makeOptions(y, 3); return { id: this.id, category: this.category, question: `Solve by substitution: $y = ${x}$ (given) and $${a}(${x}) + y = ${c + y}$. What is $y$?`, options, correctIndex, explanation: `Substitute $x=${x}$: $${a * x} + y = ${c + y}$. $y = ${y}$.` } } },
  // ── Inequalities ──
  { id: 'a1-q18', category: 'Inequalities', generate() { const a = randInt(2, 6); const b = randInt(6, 20); const ans = Math.floor(b / a); const { options, correctIndex } = makeStringOptions(`$x < ${ans}$`, [`$x > ${ans}$`, `$x \\leq ${ans}$`, `$x \\geq ${ans}$`]); return { id: this.id, category: this.category, question: `Solve: $${a}x < ${a * ans}$`, options, correctIndex, explanation: `Divide both sides by ${a}: $x < ${ans}$.` } } },
  { id: 'a1-q19', category: 'Inequalities', generate() { const a = randInt(2, 5); const bound = randInt(1, 5); const b = a * bound; /* keep b divisible by a so the stated bound is exact */ const { options, correctIndex } = makeStringOptions(`$x > -${bound}$`, [`$x < -${bound}$`, `$x > ${bound}$`, `$x < ${bound}$`]); return { id: this.id, category: this.category, question: `Solve: $-${a}x < ${b}$. (Remember to flip the inequality!)`, options, correctIndex, explanation: `Dividing by a negative flips the sign: $x > -${bound}$.` } } },
  { id: 'a1-q20', category: 'Inequalities', generate() { const a = randInt(1, 5); const b = randInt(6, 15); const { options, correctIndex } = makeStringOptions(`$${a} < x < ${b}$`, [`$x < ${a}$ or $x > ${b}$`, `$${a} \\leq x \\leq ${b}$`, `$x > ${b}$`]); return { id: this.id, category: this.category, question: `Solve: $${a} < x$ and $x < ${b}$ (compound inequality)`, options, correctIndex, explanation: `Both conditions give $${a} < x < ${b}$.` } } },
  { id: 'a1-q21', category: 'Inequalities', generate() { const a = randInt(2, 7); const { options, correctIndex } = makeStringOptions(`$-${a} \\leq x \\leq ${a}$`, [`$x \\leq -${a}$ or $x \\geq ${a}$`, `$x = ${a}$`, `$x = -${a}$ or $x = ${a}$`]); return { id: this.id, category: this.category, question: `Solve: $|x| \\leq ${a}$`, options, correctIndex, explanation: `$|x| \\leq ${a}$ means $-${a} \\leq x \\leq ${a}$.` } } },
  // ── Polynomials & Factoring ──
  { id: 'a1-q22', category: 'Polynomials & Factoring', generate() { const a = randInt(2, 6); const b = randInt(1, 8); const c = randInt(1, 8); const _sum = (a + b) + ' '; const { options, correctIndex } = makeStringOptions(`$${a + c}x + ${b}$`, [`$${a * c}x + ${b}$`, `$${a}x + ${b + c}$`, `$${a - c}x + ${b}$`]); return { id: this.id, category: this.category, question: `Simplify: $(${a}x + ${b}) + (${c}x)$`, options, correctIndex, explanation: `Combine like terms: $(${a}+${c})x + ${b} = ${a + c}x + ${b}$.` } } },
  { id: 'a1-q23', category: 'Polynomials & Factoring', generate() { const a = randInt(2, 5); const b = randInt(1, 6); const prod = a * b; const { options, correctIndex } = makeStringOptions(`$${a}x(x + ${b})$`, [`$${a}(x^2 + ${b})$`, `$x(${a}x + ${b})$`, `$${prod}x + x^2$`]); return { id: this.id, category: this.category, question: `Factor: $${a}x^2 + ${prod}x$`, options, correctIndex, explanation: `GCF is $${a}x$. $${a}x(x + ${b})$.` } } },
  { id: 'a1-q24', category: 'Polynomials & Factoring', generate() { const r = randInt(1, 8); const s = randInt(1, 8); const b = r + s; const c = r * s; const { options, correctIndex } = makeStringOptions(`$(x + ${r})(x + ${s})$`, [`$(x - ${r})(x - ${s})$`, `$(x + ${b})(x + 1)$`, `$(x + ${r})(x - ${s})$`]); return { id: this.id, category: this.category, question: `Factor: $x^2 + ${b}x + ${c}$`, options, correctIndex, explanation: `Find two numbers that add to ${b} and multiply to ${c}: ${r} and ${s}.` } } },
  { id: 'a1-q25', category: 'Polynomials & Factoring', generate() { const a = randInt(2, 7); const sq = a * a; const { options, correctIndex } = makeStringOptions(`$(x + ${a})(x - ${a})$`, [`$(x + ${a})^2$`, `$(x - ${a})^2$`, `$(x + ${sq})(x - 1)$`]); return { id: this.id, category: this.category, question: `Factor: $x^2 - ${sq}$`, options, correctIndex, explanation: `Difference of squares: $(x+${a})(x-${a})$.` } } },
  { id: 'a1-q26', category: 'Polynomials & Factoring', generate() { const a = randInt(2, 6); const sq = a * a; const dbl = 2 * a; const { options, correctIndex } = makeStringOptions(`$(x + ${a})^2$`, [`$(x + ${a})(x - ${a})$`, `$(x - ${a})^2$`, `$x^2 + ${sq}$`]); return { id: this.id, category: this.category, question: `Factor: $x^2 + ${dbl}x + ${sq}$`, options, correctIndex, explanation: `Perfect square trinomial: $(x + ${a})^2$.` } } },
  // ── Quadratic Equations ──
  { id: 'a1-q27', category: 'Quadratics', generate() { const r1 = randInt(1, 8); const r2 = randInt(1, 8); const { options, correctIndex } = makeStringOptions(`$x = ${r1}$ or $x = ${r2}$`, [`$x = -${r1}$ or $x = -${r2}$`, `$x = ${r1 + r2}$`, `$x = ${r1 * r2}$`]); return { id: this.id, category: this.category, question: `Solve: $(x - ${r1})(x - ${r2}) = 0$`, options, correctIndex, explanation: `Zero product property: $x = ${r1}$ or $x = ${r2}$.` } } },
  { id: 'a1-q28', category: 'Quadratics', generate() { const a = randInt(1, 3); const b = 2 * a; const c = a * a; const { options, correctIndex } = makeStringOptions(`$x = ${a}$`, [`$x = -${a}$`, `$x = ${b}$`, `$x = ${c}$`]); return { id: this.id, category: this.category, question: `Solve: $x^2 - ${b}x + ${c} = 0$`, options, correctIndex, explanation: `$(x - ${a})^2 = 0$. Double root $x = ${a}$.` } } },
  { id: 'a1-q29', category: 'Quadratics', generate() { const { options, correctIndex } = makeStringOptions('$x = \\frac{-b \\pm \\sqrt{b^2 - 4ac}}{2a}$', ['$x = \\frac{b \\pm \\sqrt{b^2 - 4ac}}{2a}$', '$x = \\frac{-b \\pm \\sqrt{b^2 + 4ac}}{2a}$', '$x = \\frac{-b}{2a}$']); return { id: this.id, category: this.category, question: `The quadratic formula is:`, options, correctIndex, explanation: `$x = \\frac{-b \\pm \\sqrt{b^2 - 4ac}}{2a}$.` } } },
  { id: 'a1-q30', category: 'Quadratics', generate() { const a = 1; const b = randInt(2, 6); const c = randInt(1, 8); const disc = b * b - 4 * a * c; const { options, correctIndex } = makeOptions(disc, 5); return { id: this.id, category: this.category, question: `Find the discriminant of $x^2 + ${b}x + ${c}$:`, options, correctIndex, explanation: `$\\Delta = b^2 - 4ac = ${b}^2 - 4(1)(${c}) = ${b * b} - ${4 * c} = ${disc}$.` } } },
  // ── Exponents & Radicals ──
  { id: 'a1-q31', category: 'Exponents & Radicals', generate() { const base = randInt(2, 5); const e1 = randInt(2, 4); const e2 = randInt(2, 4); const sum = e1 + e2; const { options, correctIndex } = makeStringOptions(`$${base}^{${sum}}$`, [`$${base}^{${e1 * e2}}$`, `$${base * 2}^{${e1}}$`, `$${base}^{${e1}}$`]); return { id: this.id, category: this.category, question: `Simplify: $${base}^{${e1}} \\cdot ${base}^{${e2}}$`, options, correctIndex, explanation: `Product rule: $${base}^{${e1}+${e2}} = ${base}^{${sum}}$.` } } },
  { id: 'a1-q32', category: 'Exponents & Radicals', generate() { const base = randInt(2, 5); const e1 = randInt(4, 7); const e2 = randInt(1, 3); const diff = e1 - e2; const { options, correctIndex } = makeStringOptions(`$${base}^{${diff}}$`, [`$${base}^{${e1 * e2}}$`, `$${base}^{${e1 + e2}}$`, `$${base}^{${e2}}$`]); return { id: this.id, category: this.category, question: `Simplify: $\\frac{${base}^{${e1}}}{${base}^{${e2}}}$`, options, correctIndex, explanation: `Quotient rule: $${base}^{${e1}-${e2}} = ${base}^{${diff}}$.` } } },
  { id: 'a1-q33', category: 'Exponents & Radicals', generate() { const base = randInt(2, 4); const exp = randInt(2, 3); const outer = randInt(2, 3); const prod = exp * outer; const { options, correctIndex } = makeStringOptions(`$${base}^{${prod}}$`, [`$${base}^{${exp + outer}}$`, `$${base * outer}^{${exp}}$`, `$${base}^{${exp}}$`]); return { id: this.id, category: this.category, question: `Simplify: $(${base}^{${exp}})^{${outer}}$`, options, correctIndex, explanation: `Power rule: $${base}^{${exp} \\cdot ${outer}} = ${base}^{${prod}}$.` } } },
  { id: 'a1-q34', category: 'Exponents & Radicals', generate() { const a = randInt(2, 6); const { options, correctIndex } = makeStringOptions('1', [`${a}`, '0', `$-${a}$`]); return { id: this.id, category: this.category, question: `Simplify: $${a}^0$`, options, correctIndex, explanation: `Any nonzero number to the 0th power equals 1.` } } },
  { id: 'a1-q35', category: 'Exponents & Radicals', generate() { const n = randInt(2, 12); const sq = n * n; const { options, correctIndex } = makeOptions(n, 3); return { id: this.id, category: this.category, question: `Simplify: $\\sqrt{${sq}}$`, options, correctIndex, explanation: `$\\sqrt{${sq}} = ${n}$.` } } },
  { id: 'a1-q36', category: 'Exponents & Radicals', generate() { const a = randInt(2, 5); const inner = randInt(2, 6); const sq = a * a * inner; const { options, correctIndex } = makeStringOptions(`$${a}\\sqrt{${inner}}$`, [`$\\sqrt{${sq}}$`, `$${inner}\\sqrt{${a}}$`, `$${a * inner}$`]); return { id: this.id, category: this.category, question: `Simplify: $\\sqrt{${sq}}$`, options, correctIndex, explanation: `$\\sqrt{${a}^2 \\cdot ${inner}} = ${a}\\sqrt{${inner}}$.` } } },
  // ── Functions ──
  { id: 'a1-q37', category: 'Functions', generate() { const a = randInt(2, 5); const b = randNonZero(-6, 6); const x = randInt(1, 5); const ans = a * x + b; const { options, correctIndex } = makeOptions(ans, 4); return { id: this.id, category: this.category, question: `$f(x) = ${a}x + ${b}$. Find $f(${x})$.`, options, correctIndex, explanation: `$f(${x}) = ${a}(${x}) + ${b} = ${ans}$.` } } },
  { id: 'a1-q38', category: 'Functions', generate() { const { options, correctIndex } = makeStringOptions('Each input has exactly one output', ['Each output has exactly one input', 'The graph is a straight line', 'It passes through the origin']); return { id: this.id, category: this.category, question: `What defines a function?`, options, correctIndex, explanation: `A function assigns exactly one output to each input.` } } },
  { id: 'a1-q39', category: 'Functions', generate() { const m = randInt(1, 4); const b = randNonZero(-5, 5); const { options, correctIndex } = makeStringOptions('All real numbers', [`$x \\geq 0$`, `$x > ${b}$`, `$x \\neq 0$`]); return { id: this.id, category: this.category, question: `What is the domain of $f(x) = ${m}x + ${b}$?`, options, correctIndex, explanation: `Linear functions have domain = all real numbers.` } } },
  { id: 'a1-q40', category: 'Functions', generate() { const a = randInt(1, 4); const b = randInt(1, 6); const x = randInt(1, 5); const ans = a * x * x + b; const { options, correctIndex } = makeOptions(ans, 5); return { id: this.id, category: this.category, question: `$g(x) = ${a}x^2 + ${b}$. Find $g(${x})$.`, options, correctIndex, explanation: `$g(${x}) = ${a}(${x})^2 + ${b} = ${a * x * x} + ${b} = ${ans}$.` } } },
]

export function generateExitQuiz(count: number = 10, topicSlug?: string): ExitQuizQuestion[] {
  let sourcePool = questionPool
  if (topicSlug) {
    // Match templates to the topic by category + a sample generation's text.
    const matched = questionPool.filter(t => { const s = t.generate(); return textMatchesSlug(`${t.category} ${s.question} ${s.explanation}`, topicSlug) })
    if (matched.length >= 3) sourcePool = matched
  }
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of sourcePool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = sourcePool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return shuffle(selected).map(t => t.generate())
}
