/**
 * Exit Quiz — SAT Time Management & Test Strategies
 * 40 randomized questions on pacing, test-day strategies, section timing, and process of elimination
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string; difficulty?: 'easy' | 'medium' | 'hard' }
interface QuestionTemplate { id: string; category: string; difficulty: 'easy' | 'medium' | 'hard'; generate: () => ExitQuizQuestion }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }

const questionPool: QuestionTemplate[] = [
  // Digital SAT structure
  { id: 'tm-q1', category: 'Test Structure', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('2 sections: Reading and Writing, then Math', ['3 sections: Reading, Writing, then Math', '4 sections: Reading, Grammar, Algebra, Geometry', '1 combined section covering all skills']); return { id: this.id, category: this.category, question: `The Digital SAT has how many major sections?`, options, correctIndex, explanation: `The Digital SAT has 2 sections: Reading and Writing (combined) and Math.` } } },
  { id: 'tm-q2', category: 'Test Structure', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('About 2 hours 14 minutes', ['About 3 hours 45 minutes', 'About 4 hours 10 minutes', 'About 1 hour 30 minutes']); return { id: this.id, category: this.category, question: `Total testing time for the Digital SAT:`, options, correctIndex, explanation: `The Digital SAT takes approximately 2 hours and 14 minutes.` } } },
  { id: 'tm-q3', category: 'Test Structure', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Yes — Module 2 adapts to your Module 1 performance', ['No — every student sees the same set of questions', 'Yes — but only the Math section adapts, not R&W', 'Yes — difficulty adjusts after every single question']); return { id: this.id, category: this.category, question: `Is the Digital SAT adaptive?`, options, correctIndex, explanation: `Each section has 2 modules. Module 2 difficulty adjusts based on Module 1 performance.` } } },
  { id: 'tm-q4', category: 'Test Structure', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('400–1600', ['200–800', '600–2400', '0–1000']); return { id: this.id, category: this.category, question: `The total SAT score range is:`, options, correctIndex, explanation: `Total SAT scores range from 400 to 1600 (200–800 per section).` } } },
  { id: 'tm-q5', category: 'Test Structure', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('No penalty; a wrong answer scores the same as a blank', ['Yes — each wrong answer subtracts 0.25 points', 'Yes — each wrong answer subtracts 0.5 points', 'Yes — blanks lose 1 point but wrong answers do not']); return { id: this.id, category: this.category, question: `Is there a penalty for wrong answers on the Digital SAT?`, options, correctIndex, explanation: `No penalty for wrong answers — a wrong answer and a blank both earn 0 points, so always guess if unsure.` } } },
  // Time management - Reading & Writing
  { id: 'tm-q6', category: 'R&W Timing', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('32 minutes per module (64 minutes total)', ['25 minutes per module (50 minutes total)', '45 minutes per module (90 minutes total)', '60 minutes per module (120 minutes total)']); return { id: this.id, category: this.category, question: `How much time for the Reading & Writing section?`, options, correctIndex, explanation: `R&W has two 32-minute modules for 64 minutes total.` } } },
  { id: 'tm-q7', category: 'R&W Timing', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('About 27 questions per module', ['About 10 questions per module', 'About 50 questions per module', 'About 40 questions per module']); return { id: this.id, category: this.category, question: `Approximately how many questions per R&W module?`, options, correctIndex, explanation: `Each R&W module has about 27 questions to complete in 32 minutes.` } } },
  { id: 'tm-q8', category: 'R&W Timing', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('About 71 seconds per question', ['About 30 seconds per question', 'About 2 minutes per question', 'About 3 minutes per question']); return { id: this.id, category: this.category, question: `Average time per R&W question:`, options, correctIndex, explanation: `32 minutes $\\div$ 27 questions $\\approx$ 71 seconds per question.` } } },
  { id: 'tm-q9', category: 'R&W Timing', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Short passages, each with a single question', ['Long passages with 10 to 11 questions each', 'Standalone questions with no passages at all', 'Paired passages with 5 questions per pair']); return { id: this.id, category: this.category, question: `Digital SAT R&W passages are:`, options, correctIndex, explanation: `Unlike the old SAT, the Digital SAT uses short passages (usually 1 paragraph) with 1 question each.` } } },
  // Time management - Math
  { id: 'tm-q10', category: 'Math Timing', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('35 minutes per module (70 minutes total)', ['20 minutes per module (40 minutes total)', '45 minutes per module (90 minutes total)', '30 minutes per module (60 minutes total)']); return { id: this.id, category: this.category, question: `How much time for the Math section?`, options, correctIndex, explanation: `Math has two 35-minute modules for 70 minutes total.` } } },
  { id: 'tm-q11', category: 'Math Timing', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('About 22 questions per module', ['About 30 questions per module', 'About 15 questions per module', 'About 40 questions per module']); return { id: this.id, category: this.category, question: `Approximately how many questions per Math module?`, options, correctIndex, explanation: `Each Math module has about 22 questions.` } } },
  { id: 'tm-q12', category: 'Math Timing', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('About 95 seconds per question', ['About 30 seconds per question', 'About 3 minutes per question', 'About 5 minutes per question']); return { id: this.id, category: this.category, question: `Average time per Math question:`, options, correctIndex, explanation: `35 minutes $\\div$ 22 questions $\\approx$ 95 seconds per question.` } } },
  // Process of elimination
  { id: 'tm-q13', category: 'Process of Elimination', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('25%', ['0%', '50%', '100%']); return { id: this.id, category: this.category, question: `With 4 answer choices and random guessing, your probability of being correct is:`, options, correctIndex, explanation: `4 choices → 1/4 = 25% chance of random correct guess.` } } },
  { id: 'tm-q14', category: 'Process of Elimination', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('33%', ['25%', '50%', '20%']); return { id: this.id, category: this.category, question: `If you eliminate 1 wrong answer from 4 choices, your guess probability becomes:`, options, correctIndex, explanation: `3 remaining choices → $1/3 \\approx 33$%.` } } },
  { id: 'tm-q15', category: 'Process of Elimination', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('50%', ['25%', '33%', '75%']); return { id: this.id, category: this.category, question: `Eliminating 2 wrong answers from 4 choices gives you a _____ chance:`, options, correctIndex, explanation: `2 remaining choices → 1/2 = 50%.` } } },
  { id: 'tm-q16', category: 'Process of Elimination', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Cross out clearly wrong choices first', ['Choose whichever answer seems most familiar first', 'Pick the same letter each time you are unsure', 'Skip any question that has two tempting choices']); return { id: this.id, category: this.category, question: `The best process of elimination strategy is:`, options, correctIndex, explanation: `Quickly cross out clearly wrong choices to improve your odds, then focus on the remaining options.` } } },
  { id: 'tm-q17', category: 'Process of Elimination', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('An extreme choice (always, never) on a nuanced topic', ['A choice that closely paraphrases the passage', 'A choice that uses specific numbers from the text', 'The choice that is worded most cautiously and carefully']); return { id: this.id, category: this.category, question: `Which answer choice is usually the easiest to eliminate?`, options, correctIndex, explanation: `Extreme/absolute language (always, never, all, none) is usually wrong on nuanced passages.` } } },
  // Pacing strategies
  { id: 'tm-q18', category: 'Pacing', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Guess, flag it, and come back if time allows', ['Keep working on it until you are sure', 'Leave it blank and never return to it', 'Restart the module from the beginning']); return { id: this.id, category: this.category, question: `If a question is taking too long, you should:`, options, correctIndex, explanation: `The Digital SAT lets you flag questions. Guess, flag, and return if time permits.` } } },
  { id: 'tm-q19', category: 'Pacing', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Yes — you can move freely within each module', ['No — you must answer questions in strict order', 'Only within the Math section, not in R&W', 'Yes — even back into a module you finished']); return { id: this.id, category: this.category, question: `Can you go back to previous questions on the Digital SAT?`, options, correctIndex, explanation: `You can navigate freely within each module, but cannot go back to a completed module.` } } },
  { id: 'tm-q20', category: 'Pacing', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Do easier questions first, return to hard ones', ['Spend an equal amount of time on every question', 'Start with the hardest questions while you are fresh', 'Work in strict order and never skip or come back']); return { id: this.id, category: this.category, question: `Which pacing strategy maximizes your score?`, options, correctIndex, explanation: `Easy questions and hard questions are worth the same — secure the easy points first.` } } },
  // Calculator strategy
  { id: 'tm-q21', category: 'Calculator', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Yes — a built-in Desmos calculator for all questions', ['Yes — but only during the second Math module', 'No — all Math questions must be done by hand', 'Yes — but only a basic four-function calculator is allowed']); return { id: this.id, category: this.category, question: `Is a calculator allowed on the Digital SAT Math section?`, options, correctIndex, explanation: `The Digital SAT provides a built-in Desmos graphing calculator for all math questions.` } } },
  { id: 'tm-q22', category: 'Calculator', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Graphing, solving systems, and checking answers', ['Only basic arithmetic such as adding and multiplying', 'Only trigonometry problems involving right triangles', 'Nothing — it is slower than solving every problem by hand']); return { id: this.id, category: this.category, question: `The Desmos calculator is most useful for:`, options, correctIndex, explanation: `Desmos can graph, solve systems, check answers, and visualize — a powerful strategic tool.` } } },
  { id: 'tm-q23', category: 'Calculator', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Mental math for simple steps, calculator for complex ones', ['Use the calculator for every step, even simple arithmetic', 'Never use the calculator, since it wastes too much time', 'Use the calculator only for geometry and nothing else']); return { id: this.id, category: this.category, question: `When should you use mental math vs. the calculator?`, options, correctIndex, explanation: `Simple arithmetic is faster mentally. Save the calculator for graphing, complex algebra, and checking work.` } } },
  // Test day strategies
  { id: 'tm-q24', category: 'Test Day', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Sleep well, eat breakfast, and arrive early with materials', ['Stay up late the night before reviewing every topic', 'Skip breakfast so you can arrive as early as possible', 'Bring only your phone, since everything else is provided']); return { id: this.id, category: this.category, question: `Best test-day preparation:`, options, correctIndex, explanation: `Sleep, nutrition, timeliness, and materials are key. Cramming the night before hurts performance.` } } },
  { id: 'tm-q25', category: 'Test Day', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Photo ID, pencils, and required materials', ['Only your phone, since all materials are provided', 'Nothing at all — the testing staff provide everything', 'Textbooks and notes to review during the breaks']); return { id: this.id, category: this.category, question: `What should you bring on test day?`, options, correctIndex, explanation: `A photo ID is required, and you should bring pencils plus any other materials your test center requires. Notes and textbooks are not allowed.` } } },
  { id: 'tm-q26', category: 'Test Day', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Breathe deeply, skip the hard one, and return to it later', ['Stop working until the anxious feeling goes away fully', 'Spend extra time on the exact question that caused the stress', 'Rush through the remaining questions to finish sooner']); return { id: this.id, category: this.category, question: `If you feel anxious during the test, you should:`, options, correctIndex, explanation: `Brief breathing exercises reduce anxiety. Skip the stressor and return when calmer.` } } },
  // Answer choice strategies
  { id: 'tm-q27', category: 'Answer Strategy', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('No — correct letters are spread roughly evenly', ['Yes — C is correct more often than any other letter', 'Yes — use the same letter for every single guess', 'Yes — the longest answer choice is usually correct']); return { id: this.id, category: this.category, question: `Is there a "best letter" to guess on the SAT?`, options, correctIndex, explanation: `No letter is statistically better. Correct answers are spread roughly equally across the four positions.` } } },
  { id: 'tm-q28', category: 'Answer Strategy', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Yes — always answer, since there is no guessing penalty', ['No — leave hard questions blank to protect your score', 'Only answer if you are completely sure you are right', 'No — skip any question that you would have to guess on']); return { id: this.id, category: this.category, question: `Should you answer questions you are unsure about?`, options, correctIndex, explanation: `No guessing penalty means you should ALWAYS answer every question.` } } },
  { id: 'tm-q29', category: 'Answer Strategy', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Keep it unless you find a clear reason to change it', ['Always change it, because second thoughts are better', 'Never change it, even if you spot a clear mistake', 'Change it to C whenever you feel at all unsure']); return { id: this.id, category: this.category, question: `If you review a question and want to change your answer:`, options, correctIndex, explanation: `Change an answer only when you find a specific reason, such as a calculation error or a misread word.` } } },
  // Reading-specific strategies
  { id: 'tm-q30', category: 'Reading Strategy', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Read the question first, then the short passage', ['Read the passage twice, then read the question', 'Skip the passage and guess from the choices', 'Read only the first sentence of the passage']); return { id: this.id, category: this.category, question: `For Digital SAT R&W questions (short passages), the best approach is:`, options, correctIndex, explanation: `With short passages (1 question each), reading the question first helps you focus on what matters.` } } },
  { id: 'tm-q31', category: 'Reading Strategy', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Predict an answer in your own words, then match it', ['Read all four choices first, then read the passage', 'Pick the choice that sounds the most sophisticated', 'Choose the longest choice, since it has the most detail']); return { id: this.id, category: this.category, question: `To avoid trap answers in reading questions:`, options, correctIndex, explanation: `Forming your own answer first prevents you from being swayed by attractive-sounding wrong answers.` } } },
  // Math-specific strategies
  { id: 'tm-q32', category: 'Math Strategy', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Plug in the answer choices to see which works', ['Leave it blank and never come back to it later', 'Choose whichever choice has the largest number', 'Restart the problem using a longer, harder method']); return { id: this.id, category: this.category, question: `When stuck on a multiple-choice math problem, try:`, options, correctIndex, explanation: `Backsolving (plugging in answer choices) is a powerful strategy when algebraic methods get complicated.` } } },
  { id: 'tm-q33', category: 'Math Strategy', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Substitute simple numbers like 2, 3, or 5 for the variables', ['Replace every variable with 0 or 1 to simplify it', 'Skip the problem, since variables cannot be tested', 'Rename the variables x and y before doing anything']); return { id: this.id, category: this.category, question: `"Picking numbers" strategy: when a problem has variables, you should:`, options, correctIndex, explanation: `Substituting simple numbers makes abstract problems concrete and testable. Avoid 0 and 1, which often make different expressions look equal.` } } },
  { id: 'tm-q34', category: 'Math Strategy', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Glance at the answer choices — they hint at the approach', ['Ignore the choices entirely until you have an answer', 'Look only at the first and last answer choices for a pattern', 'Assume the choices are misleading and avoid them all']); return { id: this.id, category: this.category, question: `Before solving a math question, what should you do?`, options, correctIndex, explanation: `Answer choices can reveal the form of the answer expected and guide your approach.` } } },
  // Score improvement
  { id: 'tm-q35', category: 'Score Improvement', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Accuracy on the easy and medium questions', ['Speed on the hardest questions', 'Answering each in under 30 seconds', 'Getting every hard question right']); return { id: this.id, category: this.category, question: `What has the biggest impact on SAT scores?`, options, correctIndex, explanation: `Missing easy/medium questions hurts more than skipping hard ones. Accuracy first, then speed.` } } },
  { id: 'tm-q36', category: 'Score Improvement', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Take full practice tests under timed conditions', ['Study content only and skip practice tests entirely', 'Take practice tests untimed so there is no pressure', 'Review only the questions you answered correctly']); return { id: this.id, category: this.category, question: `Best way to build time management skills for the SAT:`, options, correctIndex, explanation: `Timed practice tests build pacing instincts and test-day stamina.` } } },
  { id: 'tm-q37', category: 'Score Improvement', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Review each missed question and understand why you missed it', ['Immediately take another full practice test', 'Memorize every formula on the reference sheet', 'Read more books to improve general vocabulary']); return { id: this.id, category: this.category, question: `After a practice test, the most important step is:`, options, correctIndex, explanation: `Understanding mistakes prevents repeating them — this is where real learning happens.` } } },
  // Module strategy (adaptive)
  { id: 'tm-q38', category: 'Adaptive Strategy', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('It sets whether you get an easier or harder Module 2', ['It does not matter, since only Module 2 is scored', 'It is a warm-up, so rushing it saves energy for later', 'It has no effect; Module 2 is always the same difficulty']); return { id: this.id, category: this.category, question: `Why is Module 1 performance critical in the adaptive Digital SAT?`, options, correctIndex, explanation: `Strong Module 1 performance → harder Module 2 → access to higher score ceiling.` } } },
  { id: 'tm-q39', category: 'Adaptive Strategy', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('You likely did well on Module 1, raising your ceiling', ['You likely did poorly on Module 1', 'Both modules are always equally difficult', 'Module 2 will not affect your final score']); return { id: this.id, category: this.category, question: `If Module 2 feels harder than Module 1, this means:`, options, correctIndex, explanation: `Harder Module 2 = you did well on Module 1 → higher score ceiling. This is good news!` } } },
  { id: 'tm-q40', category: 'Test Day', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Review flagged questions and check your work', ['Submit right away to end the section sooner', 'Wait quietly until the timer runs out', 'Change a few answers at random to be safe']); return { id: this.id, category: this.category, question: `If you finish a module early, you should:`, options, correctIndex, explanation: `Use remaining time to review flagged questions, check work, and catch careless errors.` } } },
]

function selectTemplates(count: number, difficulty?: 'easy' | 'medium' | 'hard'): QuestionTemplate[] {
  if (difficulty) {
    const fillOrder: Record<'easy' | 'medium' | 'hard', ('easy' | 'medium' | 'hard')[]> = { easy: ['easy', 'medium', 'hard'], medium: ['medium', 'easy', 'hard'], hard: ['hard', 'medium', 'easy'] }
    const selected: QuestionTemplate[] = []
    for (const tier of fillOrder[difficulty]) { if (selected.length >= count) break; for (const q of shuffle(questionPool.filter(t => t.difficulty === tier))) { if (selected.length >= count) break; selected.push(q) } }
    return selected
  }
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return selected
}

export function generateExitQuiz(count: number = 10, _topicSlug?: string, difficulty?: 'easy' | 'medium' | 'hard'): ExitQuizQuestion[] {
  return shuffle(selectTemplates(count, difficulty)).map(t => ({ ...t.generate(), difficulty: t.difficulty }))
}
