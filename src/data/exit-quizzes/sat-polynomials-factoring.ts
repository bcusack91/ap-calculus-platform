/**
 * Exit Quiz — SAT Polynomials and Factoring
 * 40 randomized questions on factoring, polynomial operations, zeros, end behavior
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string }
interface QuestionTemplate { id: string; category: string; generate: () => ExitQuizQuestion }
function randInt(a: number, b: number) { return Math.floor(Math.random() * (b - a + 1)) + a }
function randNonZero(a: number, b: number) { let v = 0; while (v === 0) v = randInt(a, b); return v }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const fillers = ['None of the above', 'Cannot be determined', 'None of these']; for (const f of fillers) { if (u.length >= 3) break; if (f !== c && !u.includes(f)) u.push(f) } const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }
function makeOptions(c: number, s = 3) { const d = new Set<number>(); while (d.size < 3) { const v = c + randInt(-s * 2, s * 2); if (v !== c) d.add(v) }; const all = shuffle([c, ...d]); return { options: all.map(String), correctIndex: all.indexOf(c) } }

const questionPool: QuestionTemplate[] = [
  // GCF and basic factoring
  { id: 'pf-q1', category: 'GCF Factoring', generate() { const a = randInt(2, 6); const b = randInt(1, 10); const c = randInt(1, 10); const correct = `${a}(x + ${b > 0 ? '' : ''}${Math.floor(b*a/a)})`.replace(/.*/, () => `${a}x(x + ${c})`); const expr = `${a}x^2 + ${a*c}x`; const { options, correctIndex } = makeStringOptions(`${a}x(x + ${c})`, [`${a}(x^2 + ${c}x)`, `x(${a}x + ${c})`, `${a*c}x(x + 1)`]); return { id: this.id, category: this.category, question: `Factor: $${expr}$`, options, correctIndex, explanation: `GCF is $${a}x$. $${a}x(x + ${c})$.` } } },
  { id: 'pf-q2', category: 'GCF Factoring', generate() { const a = randInt(2, 5); const b = randInt(2, 5); const c = randInt(1, 8); const expr = `${a*b}x^2 + ${a*c}x`; const { options, correctIndex } = makeStringOptions(`${a}x(${b}x + ${c})`, [`${b}x(${a}x + ${c})`, `${a}(${b}x^2 + ${c}x)`, `x(${a*b}x + ${a*c})`]); return { id: this.id, category: this.category, question: `Factor completely: $${expr}$`, options, correctIndex, explanation: `GCF = $${a}x$. Result: $${a}x(${b}x + ${c})$.` } } },
  // Difference of squares
  { id: 'pf-q3', category: 'Special Patterns', generate() { const a = randInt(2, 8); const a2 = a*a; const { options, correctIndex } = makeStringOptions(`$(x + ${a})(x - ${a})$`, [`$(x + ${a})^2$`, `$(x - ${a})^2$`, `$(x + ${a2})(x - 1)$`]); return { id: this.id, category: this.category, question: `Factor: $x^2 - ${a2}$`, options, correctIndex, explanation: `Difference of squares: $x^2 - ${a}^2 = (x+${a})(x-${a})$.` } } },
  { id: 'pf-q4', category: 'Special Patterns', generate() { const a = randInt(2, 6); const a2 = a*a; const { options, correctIndex } = makeStringOptions(`$(x + ${a})^2$`, [`$(x + ${a})(x - ${a})$`, `$(x - ${a})^2$`, `$x^2 + ${a2}$`]); return { id: this.id, category: this.category, question: `Factor: $x^2 + ${2*a}x + ${a2}$`, options, correctIndex, explanation: `Perfect square trinomial: $(x+${a})^2$.` } } },
  { id: 'pf-q5', category: 'Special Patterns', generate() { const a = randInt(2, 5); const b = randInt(2, 5); const { options, correctIndex } = makeStringOptions(`$(${a}x + ${b})(${a}x - ${b})$`, [`$(${a}x + ${b})^2$`, `$(${a}x - ${b})^2$`, `$${a}(x + ${b})(x - ${b})$`]); return { id: this.id, category: this.category, question: `Factor: $${a*a}x^2 - ${b*b}$`, options, correctIndex, explanation: `$(${a}x)^2 - ${b}^2 = (${a}x+${b})(${a}x-${b})$.` } } },
  // Trinomial factoring (x^2 + bx + c)
  { id: 'pf-q6', category: 'Trinomial Factoring', generate() { const r = randInt(1, 9); const s = randInt(1, 9); const b = r + s; const c = r * s; const { options, correctIndex } = makeStringOptions(`$(x + ${r})(x + ${s})$`, [`$(x + ${b})(x + 1)$`, `$(x + ${c})(x + 1)$`, `$(x - ${r})(x - ${s})$`]); return { id: this.id, category: this.category, question: `Factor: $x^2 + ${b}x + ${c}$`, options, correctIndex, explanation: `Find two numbers that add to ${b} and multiply to ${c}: ${r} and ${s}.` } } },
  { id: 'pf-q7', category: 'Trinomial Factoring', generate() { const r = randInt(1, 8); const s = randInt(1, 8); const b = -(r + s); const c = r * s; const { options, correctIndex } = makeStringOptions(`$(x - ${r})(x - ${s})$`, [`$(x + ${r})(x + ${s})$`, `$(x - ${r})(x + ${s})$`, `$(x + ${r})(x - ${s})$`]); return { id: this.id, category: this.category, question: `Factor: $x^2 ${b}x + ${c}$`, options, correctIndex, explanation: `Numbers that add to ${b} and multiply to ${c}: $-${r}$ and $-${s}$.` } } },
  { id: 'pf-q8', category: 'Trinomial Factoring', generate() { const r = randInt(1, 8); const s = randInt(r+1, 12); const b = s - r; const c = -(r * s); const { options, correctIndex } = makeStringOptions(`$(x - ${r})(x + ${s})$`, [`$(x + ${r})(x - ${s})$`, `$(x - ${r})(x - ${s})$`, `$(x + ${r})(x + ${s})$`]); return { id: this.id, category: this.category, question: `Factor: $x^2 + ${b}x ${c}$`, options, correctIndex, explanation: `Numbers that add to ${b} and multiply to ${c}: $-${r}$ and $${s}$.` } } },
  // Polynomial degree and classification
  { id: 'pf-q9', category: 'Polynomial Basics', generate() { const deg = randInt(2, 5); const { options, correctIndex } = makeOptions(deg); return { id: this.id, category: this.category, question: `What is the degree of $3x^${deg} + 2x^${deg-1} - x + 7$?`, options, correctIndex, explanation: `The degree is the highest exponent: ${deg}.` } } },
  { id: 'pf-q10', category: 'Polynomial Basics', generate() { const { options, correctIndex } = makeStringOptions('Trinomial', ['Monomial', 'Binomial', 'Polynomial of degree 4']); return { id: this.id, category: this.category, question: `$5x^2 + 3x - 7$ is classified as a:`, options, correctIndex, explanation: `Three terms = trinomial.` } } },
  { id: 'pf-q11', category: 'Polynomial Basics', generate() { const a = randInt(2, 6); const { options, correctIndex } = makeOptions(a); return { id: this.id, category: this.category, question: `The leading coefficient of $${a}x^3 - 2x^2 + x - 1$ is:`, options, correctIndex, explanation: `Leading coefficient is the coefficient of the highest-degree term: ${a}.` } } },
  // Polynomial operations
  { id: 'pf-q12', category: 'Operations', generate() { const a = randInt(2, 5); const b = randInt(1, 5); const c = randInt(1, 4); const d = randInt(1, 5); const sumA = a + c; const sumB = b + d; const { options, correctIndex } = makeStringOptions(`${sumA}x + ${sumB}`, [`${a*c}x + ${b*d}`, `${sumA}x - ${sumB}`, `${a-c}x + ${b-d}`]); return { id: this.id, category: this.category, question: `$(${a}x + ${b}) + (${c}x + ${d}) = ?$`, options, correctIndex, explanation: `Combine like terms: $(${a}+${c})x + (${b}+${d}) = ${sumA}x + ${sumB}$.` } } },
  { id: 'pf-q13', category: 'Operations', generate() { const a = randInt(2, 5); const b = randInt(1, 6); const c = randInt(1, 4); const d = randInt(1, 6); const prod = `${a*c}x^2 + ${a*d + b*c}x + ${b*d}`; const { options, correctIndex } = makeStringOptions(`$${prod}$`, [`$${a*c}x^2 + ${b*d}$`, `$${a+c}x + ${b+d}$`, `$${a*c}x^2 + ${a*d}x + ${b*d}$`]); return { id: this.id, category: this.category, question: `$(${a}x + ${b})(${c}x + ${d}) = ?$`, options, correctIndex, explanation: `FOIL: $${a*c}x^2 + ${a*d}x + ${b*c}x + ${b*d} = ${prod}$.` } } },
  // Zeros / roots
  { id: 'pf-q14', category: 'Zeros', generate() { const r1 = randInt(1, 8); const r2 = randInt(1, 8); const { options, correctIndex } = makeStringOptions(`$x = ${r1}$ or $x = ${r2}$`, [`$x = -${r1}$ or $x = -${r2}$`, `$x = ${r1 + r2}$`, `$x = ${r1 * r2}$`]); return { id: this.id, category: this.category, question: `Solve: $(x - ${r1})(x - ${r2}) = 0$`, options, correctIndex, explanation: `Zero product property: $x = ${r1}$ or $x = ${r2}$.` } } },
  { id: 'pf-q15', category: 'Zeros', generate() { const r = randInt(1, 6); const s = randInt(1, 6); const b = r + s; const c = r * s; const { options, correctIndex } = makeStringOptions(`$x = -${r}$ or $x = -${s}$`, [`$x = ${r}$ or $x = ${s}$`, `$x = ${b}$ or $x = ${c}$`, `$x = -${b}$`]); return { id: this.id, category: this.category, question: `Find zeros: $x^2 + ${b}x + ${c} = 0$`, options, correctIndex, explanation: `Factor: $(x+${r})(x+${s})=0$. Zeros: $x=-${r}, x=-${s}$.` } } },
  { id: 'pf-q16', category: 'Zeros', generate() { const n = randInt(2, 5); const { options, correctIndex } = makeStringOptions(`At most ${n}`, [`Exactly ${n}`, `At most ${n-1}`, `${n+1}`]); return { id: this.id, category: this.category, question: `A polynomial of degree ${n} has at most how many real zeros?`, options, correctIndex, explanation: `A degree-$n$ polynomial has at most $n$ real zeros.` } } },
  // End behavior
  { id: 'pf-q17', category: 'End Behavior', generate() { const { options, correctIndex } = makeStringOptions('Both ends go up ($+\\infty$)', ['Both ends go down', 'Left down, right up', 'Left up, right down']); return { id: this.id, category: this.category, question: `End behavior of $f(x) = 2x^4 - 3x + 1$? (even degree, positive leading coeff)`, options, correctIndex, explanation: `Even degree + positive leading coefficient → both ends rise to $+\\infty$.` } } },
  { id: 'pf-q18', category: 'End Behavior', generate() { const { options, correctIndex } = makeStringOptions('Left end down, right end up', ['Both up', 'Both down', 'Left up, right down']); return { id: this.id, category: this.category, question: `End behavior of $f(x) = x^3 + 2x - 5$? (odd degree, positive leading coeff)`, options, correctIndex, explanation: `Odd degree + positive leading coefficient → falls left, rises right.` } } },
  { id: 'pf-q19', category: 'End Behavior', generate() { const { options, correctIndex } = makeStringOptions('Left end up, right end down', ['Both up', 'Both down', 'Left down, right up']); return { id: this.id, category: this.category, question: `End behavior of $f(x) = -x^3 + 4x$? (odd degree, negative leading coeff)`, options, correctIndex, explanation: `Odd degree + negative → rises left, falls right.` } } },
  { id: 'pf-q20', category: 'End Behavior', generate() { const { options, correctIndex } = makeStringOptions('Both ends go down ($-\\infty$)', ['Both up', 'Left down, right up', 'Left up, right down']); return { id: this.id, category: this.category, question: `End behavior of $f(x) = -3x^4 + x^2 - 7$?`, options, correctIndex, explanation: `Even degree + negative leading coefficient → both ends fall to $-\\infty$.` } } },
  // Remainder and factor theorem
  { id: 'pf-q21', category: 'Theorems', generate() { const a = randInt(1, 4); const r = randInt(1, 5); const remainder = a*r*r + 2*r - 3; const { options, correctIndex } = makeOptions(remainder, 5); return { id: this.id, category: this.category, question: `If $f(x) = ${a}x^2 + 2x - 3$, find $f(${r})$ (remainder when divided by $(x-${r})$).`, options, correctIndex, explanation: `$f(${r}) = ${a}(${r})^2 + 2(${r}) - 3 = ${a*r*r} + ${2*r} - 3 = ${remainder}$.` } } },
  { id: 'pf-q22', category: 'Theorems', generate() { const r = randInt(1, 6); const { options, correctIndex } = makeStringOptions(`$(x - ${r})$ is a factor`, [`$(x + ${r})$ is a factor`, `$(x - ${r})$ is NOT a factor`, `$f(x) = ${r}$`]); return { id: this.id, category: this.category, question: `If $f(${r}) = 0$, what does the Factor Theorem tell us?`, options, correctIndex, explanation: `Factor Theorem: if $f(c)=0$, then $(x-c)$ is a factor.` } } },
  // SAT-style polynomial questions
  { id: 'pf-q23', category: 'SAT-Style', generate() { const a = randInt(2, 5); const b = randNonZero(-5, 5); const c = randNonZero(-5, 5); const sum = a + b + c; const { options, correctIndex } = makeOptions(sum, 4); return { id: this.id, category: this.category, question: `If $p(x) = x^2 + ${a}x - 4$, what is $p(1)$?`, options, correctIndex, explanation: `$p(1) = 1 + ${a} - 4 = ${1+a-4}$.` } } },
  { id: 'pf-q24', category: 'SAT-Style', generate() { const r1 = randInt(-5, -1); const r2 = randInt(1, 5); const b = -(r1+r2); const c = r1*r2; const { options, correctIndex } = makeOptions(c, 5); return { id: this.id, category: this.category, question: `$x^2 + ${b}x + c = 0$ has roots $x=${r1}$ and $x=${r2}$. Find $c$.`, options, correctIndex, explanation: `$c = $ product of roots $= (${r1})(${r2}) = ${c}$.` } } },
  { id: 'pf-q25', category: 'SAT-Style', generate() { const k = randInt(2, 6); const { options, correctIndex } = makeStringOptions(`$x = 0$ or $x = ${k}$`, [`$x = ${k}$ only`, `$x = 0$ only`, `$x = -${k}$ or $x = ${k}$`]); return { id: this.id, category: this.category, question: `Solve: $x^2 - ${k}x = 0$`, options, correctIndex, explanation: `$x(x - ${k}) = 0$, so $x = 0$ or $x = ${k}$.` } } },
  { id: 'pf-q26', category: 'Operations', generate() { const a = randInt(2, 5); const b = randInt(1, 6); const sq = a*a; const prod2 = 2*a*b; const bsq = b*b; const { options, correctIndex } = makeStringOptions(`$${sq}x^2 + ${prod2}x + ${bsq}$`, [`$${sq}x^2 + ${bsq}$`, `$${sq}x^2 - ${prod2}x + ${bsq}$`, `$${a}x^2 + ${b}x + ${bsq}$`]); return { id: this.id, category: this.category, question: `Expand: $(${a}x + ${b})^2$`, options, correctIndex, explanation: `$(${a}x+${b})^2 = ${sq}x^2 + 2(${a}x)(${b}) + ${bsq} = ${sq}x^2 + ${prod2}x + ${bsq}$.` } } },
  { id: 'pf-q27', category: 'Zeros', generate() { const a = randInt(1, 4); const b = randInt(1, 5); const c = randInt(1, 5); const { options, correctIndex } = makeStringOptions('3', ['1', '2', '4']); return { id: this.id, category: this.category, question: `$f(x) = (x-${a})(x+${b})(x-${c})$ has how many x-intercepts?`, options, correctIndex, explanation: `Three distinct linear factors → three x-intercepts: $x=${a}, x=-${b}, x=${c}$.` } } },
  { id: 'pf-q28', category: 'SAT-Style', generate() { const a = randInt(1, 5); const b = randInt(1, 5); const { options, correctIndex } = makeStringOptions(`$(x^2 + ${a})(x^2 - ${b})$`, [`$(x + ${a})(x - ${b})$`, `$(x^2 - ${a})(x^2 + ${b})$`, `Cannot be factored`]); return { id: this.id, category: this.category, question: `Factor: $x^4 + ${a-b}x^2 - ${a*b}$`, options, correctIndex, explanation: `Let $u = x^2$. Then $u^2 + ${a-b}u - ${a*b} = (u+${a})(u-${b}) = (x^2+${a})(x^2-${b})$.` } } },
  { id: 'pf-q29', category: 'Theorems', generate() { const n = randInt(3, 6); const { options, correctIndex } = makeStringOptions('1', ['0', String(n), String(n-1)]); return { id: this.id, category: this.category, question: `According to the Fundamental Theorem of Algebra, a polynomial of degree ${n} has at least how many complex root(s)?`, options, correctIndex, explanation: `Every non-constant polynomial has at least one complex root. In fact it has exactly ${n} (counting multiplicity).` } } },
  { id: 'pf-q30', category: 'Special Patterns', generate() { const a = randInt(2, 5); const { options, correctIndex } = makeStringOptions(`$(x+${a})(x^2-${a}x+${a*a})$`, [`$(x-${a})(x^2+${a}x+${a*a})$`, `$(x+${a})^3$`, `Cannot be factored`]); return { id: this.id, category: this.category, question: `Factor: $x^3 + ${a*a*a}$ (sum of cubes)`, options, correctIndex, explanation: `$a^3+b^3 = (a+b)(a^2-ab+b^2)$. Here $a=x, b=${a}$.` } } },
  { id: 'pf-q31', category: 'GCF Factoring', generate() { const a = randInt(2, 4); const b = randInt(1, 5); const c = randInt(1, 5); const expr = `${a}x^3 + ${a*b}x^2 + ${a*c}x`; const { options, correctIndex } = makeStringOptions(`$${a}x(x^2 + ${b}x + ${c})$`, [`$x(${a}x^2 + ${b}x + ${c})$`, `$${a}(x^3 + ${b}x + ${c})$`, `$${a}x^2(x + ${b} + ${c})$`]); return { id: this.id, category: this.category, question: `Factor completely: $${expr}$`, options, correctIndex, explanation: `GCF = $${a}x$. $${a}x(x^2 + ${b}x + ${c})$.` } } },
  { id: 'pf-q32', category: 'Trinomial Factoring', generate() { const a = 2; const r = randInt(1, 5); const s = randInt(1, 5); const mid = a*s + r; const last = r*s; const { options, correctIndex } = makeStringOptions(`$(${a}x + ${r})(x + ${s})$`, [`$(${a}x + ${s})(x + ${r})$`, `$(${a}x - ${r})(x - ${s})$`, `$(x + ${r})(x + ${s})$`]); return { id: this.id, category: this.category, question: `Factor: $${a}x^2 + ${mid}x + ${last}$`, options, correctIndex, explanation: `Find factors of $${a} \\cdot ${last} = ${a*last}$ that add to ${mid}. $(${a}x+${r})(x+${s})$.` } } },
  { id: 'pf-q33', category: 'End Behavior', generate() { const a = randNonZero(-5, 5); const deg = randInt(3, 6); const isPos = a > 0; const isOdd = deg % 2 === 1; let endBehavior: string; if (isOdd && isPos) endBehavior = 'Falls left, rises right'; else if (isOdd && !isPos) endBehavior = 'Rises left, falls right'; else if (!isOdd && isPos) endBehavior = 'Both ends rise'; else endBehavior = 'Both ends fall'; const wrong = ['Falls left, rises right', 'Rises left, falls right', 'Both ends rise', 'Both ends fall'].filter(x => x !== endBehavior); const { options, correctIndex } = makeStringOptions(endBehavior, wrong); return { id: this.id, category: this.category, question: `$f(x) = ${a}x^${deg} + \\ldots$ End behavior?`, options, correctIndex, explanation: `Degree ${deg} (${isOdd?'odd':'even'}), leading coeff ${a} (${isPos?'positive':'negative'}) → ${endBehavior}.` } } },
  { id: 'pf-q34', category: 'Zeros', generate() { const r = randInt(1, 5); const mult = randInt(2, 3); const { options, correctIndex } = makeStringOptions(`The graph touches but doesn't cross at $x=${r}$`, [`Crosses at $x=${r}$`, `Has no x-intercept at $x=${r}$`, `Goes to infinity at $x=${r}$`]); return { id: this.id, category: this.category, question: `$f(x) = (x-${r})^${mult === 2 ? 2 : 2}(x+1)$. What happens at $x=${r}$?`, options, correctIndex, explanation: `Even multiplicity (2) means the graph touches but doesn't cross the x-axis.` } } },
  { id: 'pf-q35', category: 'SAT-Style', generate() { const a = randInt(1, 4); const b = randInt(1, 4); const c = randInt(1, 4); const sumZeros = a + b + c; const { options, correctIndex } = makeOptions(sumZeros, 3); return { id: this.id, category: this.category, question: `Zeros of $f(x)$ are $${a}$, $${b}$, $${c}$. Sum of zeros?`, options, correctIndex, explanation: `Sum = ${a} + ${b} + ${c} = ${sumZeros}.` } } },
  { id: 'pf-q36', category: 'Operations', generate() { const a = randInt(1, 5); const b = randInt(1, 5); const c = randInt(1, 5); const d = randInt(1, 5); const diffA = a - c; const diffB = b - d; const { options, correctIndex } = makeStringOptions(`${diffA > 0 ? '' : ''}${diffA}x^2 + ${diffB > 0 ? '' : ''}${diffB}`, [`${a+c}x^2 + ${b+d}`, `${diffA}x^2 - ${diffB}`, `${a*c}x^2 + ${b*d}`]); return { id: this.id, category: this.category, question: `$(${a}x^2 + ${b}) - (${c}x^2 + ${d}) = ?$`, options, correctIndex, explanation: `${a}x^2 + ${b} - ${c}x^2 - ${d} = ${diffA}x^2 + ${diffB}.` } } },
  { id: 'pf-q37', category: 'SAT-Style', generate() { const r1 = randInt(1, 5); const r2 = randInt(1, 5); const prod = r1 * r2; const sum = r1 + r2; const { options, correctIndex } = makeOptions(prod, 4); return { id: this.id, category: this.category, question: `$x^2 - ${sum}x + k = 0$ has root $x = ${r1}$. Find $k$.`, options, correctIndex, explanation: `If $x=${r1}$ is a root: ${r1}^2 - ${sum}(${r1}) + k = 0 → k = ${prod}.` } } },
  { id: 'pf-q38', category: 'Polynomial Basics', generate() { const n = randInt(2, 5); const turns = n - 1; const { options, correctIndex } = makeOptions(turns, 2); return { id: this.id, category: this.category, question: `A polynomial of degree ${n} has at most how many turning points?`, options, correctIndex, explanation: `At most $n-1 = ${turns}$ turning points.` } } },
  { id: 'pf-q39', category: 'SAT-Style', generate() { const a = randInt(2, 5); const b = randInt(1, 6); const { options, correctIndex } = makeStringOptions(`$x = \\frac{${b}}{${a}}$`, [`$x = \\frac{${a}}{${b}}$`, `$x = -\\frac{${b}}{${a}}$`, `$x = ${a*b}$`]); return { id: this.id, category: this.category, question: `If $(${a}x - ${b})$ is a factor of $f(x)$, then one zero is:`, options, correctIndex, explanation: `Set $${a}x - ${b} = 0$, so $x = ${b}/${a}$.` } } },
  { id: 'pf-q40', category: 'Theorems', generate() { const { options, correctIndex } = makeStringOptions('The quotient and remainder', ['Only the quotient', 'Only the remainder', 'The degree of the polynomial']); return { id: this.id, category: this.category, question: `Polynomial long division produces:`, options, correctIndex, explanation: `Like numeric division: dividend = divisor × quotient + remainder.` } } },
]

export function generateExitQuiz(count: number = 10): ExitQuizQuestion[] {
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return shuffle(selected).map(t => t.generate())
}
