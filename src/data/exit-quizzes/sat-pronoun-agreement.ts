/**
 * Exit Quiz — SAT Pronoun Agreement
 * 40 randomized questions on number agreement, gender, ambiguous reference, case, reflexive, relative, shift
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string }
interface QuestionTemplate { id: string; category: string; generate: () => ExitQuizQuestion }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const fillers = ['No change', 'Cannot be determined', 'None of these']; for (const f of fillers) { if (u.length >= 3) break; if (f !== c && !u.includes(f)) u.push(f) } const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }
function pick<T>(arr: T[]): T { return arr[Math.floor(Math.random() * arr.length)] }

const questionPool: QuestionTemplate[] = [
  // Number Agreement
  { id: 'pa-q1', category: 'Number Agreement', generate() { const subj = pick(['Every student', 'Each employee', 'Anyone']); const { options, correctIndex } = makeStringOptions('his or her', ['their', 'its', 'our']); return { id: this.id, category: this.category, question: `"${subj} must submit ___ report by Friday." (Formal agreement)`, options, correctIndex, explanation: `"${subj}" is singular → "his or her" (formal). Note: "their" is increasingly accepted.` } } },
  { id: 'pa-q2', category: 'Number Agreement', generate() { const { options, correctIndex } = makeStringOptions('their', ['his or her', 'its', 'our']); return { id: this.id, category: this.category, question: '"The students should bring ___ textbooks." Correct pronoun?', options, correctIndex, explanation: '"Students" is plural → "their."' } } },
  { id: 'pa-q3', category: 'Number Agreement', generate() { const { options, correctIndex } = makeStringOptions('its', ["it's", 'their', 'his']); return { id: this.id, category: this.category, question: '"The committee released ___ final report." Correct pronoun?', options, correctIndex, explanation: '"Committee" acting as one unit → singular "its." (Not "it\'s" = "it is.")' } } },
  { id: 'pa-q4', category: 'Number Agreement', generate() { const { options, correctIndex } = makeStringOptions('A pronoun must agree in number with the noun it replaces (its antecedent)', ['Pronouns can be any number', 'Only verbs need to agree', 'Pronouns always take plural form']); return { id: this.id, category: this.category, question: 'What is the basic rule of pronoun-antecedent agreement?', options, correctIndex, explanation: 'Pronouns must match their antecedents in number (singular/plural).' } } },
  { id: 'pa-q5', category: 'Number Agreement', generate() { const pronoun = pick(['Nobody', 'Everybody', 'Somebody']); const { options, correctIndex } = makeStringOptions('his or her', ['their', 'our', 'your']); return { id: this.id, category: this.category, question: `"${pronoun} should mind ___ own business." (Formal)`, options, correctIndex, explanation: `"${pronoun}" is singular → "his or her" in formal usage.` } } },
  // Gender
  { id: 'pa-q6', category: 'Gender', generate() { const { options, correctIndex } = makeStringOptions('Use "they/their" as a gender-neutral singular or "his or her"', ['Always use "he"', 'Always use "she"', 'Avoid pronouns entirely']); return { id: this.id, category: this.category, question: 'When gender is unknown or unspecified, what pronoun should you use?', options, correctIndex, explanation: '"They/their" (singular) or "his or her" are both acceptable for unknown gender.' } } },
  { id: 'pa-q7', category: 'Gender', generate() { const { options, correctIndex } = makeStringOptions('their', ['his', 'her', 'its']); return { id: this.id, category: this.category, question: '"A student should always check ___ work before submitting." (Gender-neutral)', options, correctIndex, explanation: 'When referring to an unspecified student, "their" is gender-neutral and widely accepted.' } } },
  { id: 'pa-q8', category: 'Gender', generate() { const { options, correctIndex } = makeStringOptions('Rewrite to make the subject plural: "Students should check their work"', ['Use "his" by default', 'Alternate "he" and "she" randomly', 'Use "it" for all people']); return { id: this.id, category: this.category, question: 'What is the simplest way to avoid gender-pronoun issues in formal writing?', options, correctIndex, explanation: 'Making the antecedent plural ("students...their") avoids the gender pronoun issue.' } } },
  // Ambiguous Reference
  { id: 'pa-q9', category: 'Ambiguous Reference', generate() { const scenario = pick(['When Sarah told Maria, she was upset.', 'After John met David, he left early.', 'When the teacher spoke to the student, she smiled.']); const { options, correctIndex } = makeStringOptions('Clarify by naming the specific person', ['Leave it — context is enough', 'Remove all pronouns', 'Add more pronouns']); return { id: this.id, category: this.category, question: `"${scenario}" The pronoun is ambiguous. How do you fix it?`, options, correctIndex, explanation: 'Ambiguous pronouns should be replaced with the specific noun they refer to.' } } },
  { id: 'pa-q10', category: 'Ambiguous Reference', generate() { const { options, correctIndex } = makeStringOptions('When a pronoun could refer to more than one noun', ['When a pronoun is misspelled', 'When a pronoun is at the start of a sentence', 'When a pronoun is in a question']); return { id: this.id, category: this.category, question: 'What makes a pronoun reference "ambiguous"?', options, correctIndex, explanation: 'An ambiguous pronoun could logically refer to two or more antecedents.' } } },
  { id: 'pa-q11', category: 'Ambiguous Reference', generate() { const { options, correctIndex } = makeStringOptions('The researchers analyzed the samples and found that the samples contained mercury.', ['The researchers analyzed the samples and found that they contained mercury.', 'They analyzed them and found they contained mercury.', 'The researchers analyzed them and found that it contained mercury.']); return { id: this.id, category: this.category, question: 'Fix the ambiguity: "The researchers analyzed the samples and found that they contained mercury." (Who/what is "they"?)', options, correctIndex, explanation: 'Replace the ambiguous "they" with the specific noun ("samples") for clarity.' } } },
  { id: 'pa-q12', category: 'Ambiguous Reference', generate() { const { options, correctIndex } = makeStringOptions('It has no clear antecedent — "this" refers to what?', ['It\'s perfectly clear', '"This" is always acceptable', 'The sentence is too short']); return { id: this.id, category: this.category, question: '"The policy changed, and the budget was cut. This was controversial." What\'s wrong?', options, correctIndex, explanation: '"This" is vague — does it refer to the policy change, the budget cut, or both? Clarify.' } } },
  // Pronoun Case
  { id: 'pa-q13', category: 'Pronoun Case', generate() { const { options, correctIndex } = makeStringOptions('me', ['I', 'myself', 'mine']); return { id: this.id, category: this.category, question: '"Between you and ___." Correct pronoun?', options, correctIndex, explanation: 'After a preposition ("between"), use the object pronoun "me."' } } },
  { id: 'pa-q14', category: 'Pronoun Case', generate() { const { options, correctIndex } = makeStringOptions('she and I', ['her and me', 'her and I', 'she and me']); return { id: this.id, category: this.category, question: '"___ went to the library." (Subject position)', options, correctIndex, explanation: 'Subject pronouns needed: "she" and "I." Test each alone: "She went" / "I went."' } } },
  { id: 'pa-q15', category: 'Pronoun Case', generate() { const { options, correctIndex } = makeStringOptions('him and me', ['he and I', 'he and me', 'him and I']); return { id: this.id, category: this.category, question: '"The teacher gave the books to ___." (Object position)', options, correctIndex, explanation: 'After a preposition ("to"), use object pronouns: "him and me."' } } },
  { id: 'pa-q16', category: 'Pronoun Case', generate() { const { options, correctIndex } = makeStringOptions('Subject case (I, he, she, we, they) for subjects; object case (me, him, her, us, them) for objects', ['Always use subject case', 'Always use object case', 'Case doesn\'t matter']); return { id: this.id, category: this.category, question: 'What is pronoun case?', options, correctIndex, explanation: 'Case = the form of a pronoun based on its function (subject or object) in the sentence.' } } },
  { id: 'pa-q17', category: 'Pronoun Case', generate() { const { options, correctIndex } = makeStringOptions('whom', ['who', 'whose', 'whoever']); return { id: this.id, category: this.category, question: '"To ___ should I address this letter?" Correct pronoun?', options, correctIndex, explanation: 'After a preposition ("to"), use object form: "whom."' } } },
  // Reflexive Pronouns
  { id: 'pa-q18', category: 'Reflexive Pronouns', generate() { const { options, correctIndex } = makeStringOptions('When the subject and object are the same person', ['To sound more formal', 'To replace any pronoun', 'In all compound subjects']); return { id: this.id, category: this.category, question: 'When should you use a reflexive pronoun (myself, herself, themselves)?', options, correctIndex, explanation: 'Reflexive pronouns are used when the subject acts on itself: "She taught herself."' } } },
  { id: 'pa-q19', category: 'Reflexive Pronouns', generate() { const { options, correctIndex } = makeStringOptions('me', ['myself', 'I', 'mine']); return { id: this.id, category: this.category, question: '"Please give the report to John and ___." (Not reflexive — different subject)', options, correctIndex, explanation: '"Myself" is wrong here because the subject ("you") is different. Use object pronoun "me."' } } },
  { id: 'pa-q20', category: 'Reflexive Pronouns', generate() { const { options, correctIndex } = makeStringOptions('herself', ['her', 'she', 'hers']); return { id: this.id, category: this.category, question: '"She prepared ___ for the interview." Correct pronoun?', options, correctIndex, explanation: 'Subject acts on itself ("she...herself") → reflexive pronoun is correct.' } } },
  { id: 'pa-q21', category: 'Reflexive Pronouns', generate() { const { options, correctIndex } = makeStringOptions('It is incorrect — "myself" cannot replace "I" or "me" as a subject', ['It is always correct to use "myself"', '"Myself" is more formal than "I"', '"Myself" is acceptable in all positions']); return { id: this.id, category: this.category, question: '"Myself and John will attend." Is this correct?', options, correctIndex, explanation: '"Myself" cannot be a subject. Correct: "John and I will attend."' } } },
  // Relative Pronouns
  { id: 'pa-q22', category: 'Relative Pronouns', generate() { const { options, correctIndex } = makeStringOptions('who', ['which', 'that', 'whom']); return { id: this.id, category: this.category, question: '"The doctor ___ treated me was very kind." Correct relative pronoun?', options, correctIndex, explanation: '"Who" is used for people in subject position.' } } },
  { id: 'pa-q23', category: 'Relative Pronouns', generate() { const { options, correctIndex } = makeStringOptions('which', ['who', 'whom', 'whose']); return { id: this.id, category: this.category, question: '"The car, ___ was parked outside, had a flat tire." Correct relative pronoun? (Nonrestrictive)', options, correctIndex, explanation: '"Which" is used for things in nonrestrictive clauses (with commas).' } } },
  { id: 'pa-q24', category: 'Relative Pronouns', generate() { const { options, correctIndex } = makeStringOptions('that', ['which', 'who', 'whom']); return { id: this.id, category: this.category, question: '"The book ___ I read was excellent." Correct relative pronoun? (Restrictive)', options, correctIndex, explanation: '"That" is used for things in restrictive (essential) clauses (no commas).' } } },
  { id: 'pa-q25', category: 'Relative Pronouns', generate() { const { options, correctIndex } = makeStringOptions('whom', ['who', 'which', 'that']); return { id: this.id, category: this.category, question: '"The candidate ___ the voters selected won the election." Correct pronoun?', options, correctIndex, explanation: '"Whom" is the object of "selected" — test by replacing: "the voters selected him."' } } },
  { id: 'pa-q26', category: 'Relative Pronouns', generate() { const { options, correctIndex } = makeStringOptions('whose', ["who's", 'whom', 'which']); return { id: this.id, category: this.category, question: '"The author ___ book was banned gave a speech." Correct pronoun?', options, correctIndex, explanation: '"Whose" = possessive. "Who\'s" = "who is."' } } },
  // Pronoun Shift
  { id: 'pa-q27', category: 'Pronoun Shift', generate() { const { options, correctIndex } = makeStringOptions('they find', ['you find', 'one finds', 'we find']); return { id: this.id, category: this.category, question: '"Students should study hard so that ___ the answers." If the passage uses third person:', options, correctIndex, explanation: 'Maintain consistent point of view — "students" (third person plural) → "they."' } } },
  { id: 'pa-q28', category: 'Pronoun Shift', generate() { const { options, correctIndex } = makeStringOptions('An unnecessary change in pronoun person within a passage', ['A change in verb tense', 'A misspelled pronoun', 'Using too many pronouns']); return { id: this.id, category: this.category, question: 'What is a "pronoun shift"?', options, correctIndex, explanation: 'A pronoun shift = switching from one person (e.g., "one") to another (e.g., "you") inconsistently.' } } },
  { id: 'pa-q29', category: 'Pronoun Shift', generate() { const { options, correctIndex } = makeStringOptions('Keep the same pronoun person throughout the passage', ['Switch between "you" and "one" for variety', 'Use "I" whenever possible', 'Avoid pronouns entirely']); return { id: this.id, category: this.category, question: 'How do you fix a pronoun shift?', options, correctIndex, explanation: 'Maintain consistency — if the passage uses "one," don\'t switch to "you" mid-sentence.' } } },
  { id: 'pa-q30', category: 'Pronoun Shift', generate() { const { options, correctIndex } = makeStringOptions('one needs', ['you need', 'they need', 'we need']); return { id: this.id, category: this.category, question: '"When one studies for the SAT, ___ to focus on weak areas." (Maintain "one")', options, correctIndex, explanation: 'Started with "one" → stay with "one needs" for consistency.' } } },
  // SAT-Style
  { id: 'pa-q31', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('their', ['its', 'his or her', 'our']); return { id: this.id, category: this.category, question: '"The team celebrated ___ victory." (Team acting as individuals celebrating)', options, correctIndex, explanation: 'When team members act individually (each celebrating), "their" is appropriate.' } } },
  { id: 'pa-q32', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('he or she', ['they', 'you', 'we']); return { id: this.id, category: this.category, question: '"If a driver sees a red light, ___ must stop." (Formal, singular)', options, correctIndex, explanation: '"A driver" is singular → formal agreement requires "he or she."' } } },
  { id: 'pa-q33', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('Remove the pronoun and replace with the specific noun for clarity', ['Add more pronouns', 'Change all nouns to pronouns', 'Use reflexive pronouns instead']); return { id: this.id, category: this.category, question: 'The SAT often tests unclear pronoun reference. What\'s the best fix?', options, correctIndex, explanation: 'Replace vague pronouns with specific nouns to eliminate ambiguity.' } } },
  { id: 'pa-q34', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('who', ['whom', 'which', 'whose']); return { id: this.id, category: this.category, question: '"Scientists ___ study climate change warn about rising temperatures." Correct pronoun?', options, correctIndex, explanation: '"Who" = subject of "study." (Test: "they study" = subject form.)' } } },
  { id: 'pa-q35', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('I', ['me', 'myself', 'mine']); return { id: this.id, category: this.category, question: '"It was ___ who made the decision." Correct pronoun after "was"?', options, correctIndex, explanation: 'After a linking verb ("was"), use the subject pronoun: "It was I." (Formal)' } } },
  { id: 'pa-q36', category: 'SAT-Style', generate() { const { options, correctIndex } = makeStringOptions('Check whether the pronoun clearly refers to one specific noun', ['Count the pronouns in the sentence', 'Check if the pronoun is capitalized', 'See if the pronoun is at the start of the sentence']); return { id: this.id, category: this.category, question: 'What should you always check when you see a pronoun underlined on the SAT?', options, correctIndex, explanation: 'For every underlined pronoun: check agreement (number), case, and clear reference.' } } },
  { id: 'pa-q37', category: 'Number Agreement', generate() { const { options, correctIndex } = makeStringOptions('her', ['their', 'its', 'his']); return { id: this.id, category: this.category, question: '"Neither of the girls forgot ___ lunch." Correct pronoun?', options, correctIndex, explanation: '"Neither" is singular. Context indicates female → "her."' } } },
  { id: 'pa-q38', category: 'Ambiguous Reference', generate() { const { options, correctIndex } = makeStringOptions('Add a specific noun: "This policy change was controversial."', ['Leave "this" as is', 'Change "this" to "it"', 'Delete the sentence']); return { id: this.id, category: this.category, question: '"The law was amended and taxes were raised. This was controversial." Fix the vague "this."', options, correctIndex, explanation: 'Vague "this" should be followed by a specific noun to clarify what was controversial.' } } },
  { id: 'pa-q39', category: 'Pronoun Case', generate() { const { options, correctIndex } = makeStringOptions('whoever', ['whomever', 'who', 'whom']); return { id: this.id, category: this.category, question: '"___ scores the highest will win the prize." Correct pronoun?', options, correctIndex, explanation: '"Whoever" is the subject of "scores" — use subject case.' } } },
  { id: 'pa-q40', category: 'Pronoun Shift', generate() { const { options, correctIndex } = makeStringOptions('you need to practice consistently', ['one needs to practice consistently', 'a person needs to practice', 'they need to practice']); return { id: this.id, category: this.category, question: '"To improve your SAT score, ___ ." If the passage uses second person ("your"):', options, correctIndex, explanation: 'Maintain second person throughout: "your" → "you need to practice."' } } },
]

export function generateExitQuiz(count: number = 10): ExitQuizQuestion[] {
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return shuffle(selected).map(t => t.generate())
}
