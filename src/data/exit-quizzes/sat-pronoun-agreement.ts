/**
 * Exit Quiz — SAT Pronoun Agreement
 * 40 randomized questions on number agreement, gender, ambiguous reference, case, reflexive, relative, shift
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string; difficulty?: 'easy' | 'medium' | 'hard' }
interface QuestionTemplate { id: string; category: string; difficulty: 'easy' | 'medium' | 'hard'; generate: () => ExitQuizQuestion }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }
function pick<T>(arr: T[]): T { return arr[Math.floor(Math.random() * arr.length)] }

const questionPool: QuestionTemplate[] = [
  // Number Agreement
  { id: 'pa-q1', category: 'Number Agreement', difficulty: 'medium', generate() { const subj = pick(['Every student', 'Each employee', 'Anyone']); const { options, correctIndex } = makeStringOptions('his or her', ['their', 'him or her', 'his and hers']); return { id: this.id, category: this.category, question: `"${subj} must submit ___ report by Friday." (Formal agreement)`, options, correctIndex, explanation: `"${subj}" is singular → "his or her" (formal). "Him or her" is object case, not possessive, and "his and hers" is not a possessive adjective. Note: "their" is increasingly accepted in informal usage.` } } },
  { id: 'pa-q2', category: 'Number Agreement', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('their', ['his or her', 'theirs', "they're"]); return { id: this.id, category: this.category, question: '"The students should bring ___ textbooks." Correct pronoun?', options, correctIndex, explanation: '"Students" is plural → "their." ("They\'re" means "they are," and "theirs" cannot come before a noun.)' } } },
  { id: 'pa-q3', category: 'Number Agreement', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('its', ["it's", 'their', 'his']); return { id: this.id, category: this.category, question: '"The committee released ___ final report." Correct pronoun?', options, correctIndex, explanation: '"Committee" acting as one unit → singular "its." (Not "it\'s" = "it is.")' } } },
  { id: 'pa-q4', category: 'Number Agreement', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A pronoun must match its antecedent in number', ['A pronoun must match the verb that follows', 'A pronoun must match the closest noun to it', 'A pronoun must match its verb in person']); return { id: this.id, category: this.category, question: 'What is the basic rule of pronoun-antecedent agreement?', options, correctIndex, explanation: 'Pronouns must match their antecedents (the nouns they replace) in number (singular/plural) — not a nearby verb or whichever noun happens to be closest.' } } },
  { id: 'pa-q5', category: 'Number Agreement', difficulty: 'medium', generate() { const pronoun = pick(['Nobody', 'Everybody', 'Somebody']); const { options, correctIndex } = makeStringOptions('his or her', ['he or she', 'him or her', 'his and her']); return { id: this.id, category: this.category, question: `"${pronoun} should mind ___ own business." (Formal)`, options, correctIndex, explanation: `"${pronoun}" is singular → "his or her" in formal usage. The blank needs a possessive, and "his and her" wrongly implies two owners.` } } },
  // Gender
  { id: 'pa-q6', category: 'Gender', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Singular "they" or "his or her"', ['"It," since the person\'s gender is unknown', 'Whichever gender is most common in that role', '"He," as the default for mixed groups']); return { id: this.id, category: this.category, question: 'When gender is unknown or unspecified, what pronoun should you use?', options, correctIndex, explanation: '"They/their" (singular) or "his or her" are both acceptable for unknown gender. "It" is not used for people, and defaulting to one gender is no longer standard.' } } },
  { id: 'pa-q7', category: 'Gender', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('their', ['his', 'its', "they're"]); return { id: this.id, category: this.category, question: '"A student should always check ___ work before submitting." (Gender-neutral)', options, correctIndex, explanation: 'When referring to an unspecified student, "their" is gender-neutral and widely accepted. "They\'re" means "they are."' } } },
  { id: 'pa-q8', category: 'Gender', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Make the antecedent plural and use "their"', ['Use "his" whenever the gender is unknown', 'Alternate "he" and "she" by paragraph', 'Use "it" so no gender is implied']); return { id: this.id, category: this.category, question: 'What is the simplest way to avoid gender-pronoun issues in formal writing?', options, correctIndex, explanation: 'Making the antecedent plural ("students...their") avoids the gender pronoun issue.' } } },
  // Ambiguous Reference
  { id: 'pa-q9', category: 'Ambiguous Reference', difficulty: 'easy', generate() { const scenario = pick(['When Sarah told Maria, she was upset.', 'After John met David, he left early.', 'When the teacher spoke to the student, she smiled.']); const { options, correctIndex } = makeStringOptions('Clarify by naming the specific person', ['Leave it; context makes it clear', 'Replace it with a reflexive pronoun', 'Put the pronoun before both names']); return { id: this.id, category: this.category, question: `"${scenario}" The pronoun is ambiguous. How do you fix it?`, options, correctIndex, explanation: 'Ambiguous pronouns should be replaced with the specific noun they refer to.' } } },
  { id: 'pa-q10', category: 'Ambiguous Reference', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('When it could refer to more than one noun', ['When it comes before its antecedent', 'When it is used as the object of a verb', 'When it begins a sentence or a clause']); return { id: this.id, category: this.category, question: 'What makes a pronoun reference "ambiguous"?', options, correctIndex, explanation: 'An ambiguous pronoun could logically refer to two or more antecedents.' } } },
  { id: 'pa-q11', category: 'Ambiguous Reference', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('The researchers analyzed the samples and found that the samples contained mercury.', ['The researchers analyzed them and found that the samples contained mercury.', 'The researchers analyzed the samples and found that it contained mercury.', 'They analyzed the samples and found that the samples contained mercury.']); return { id: this.id, category: this.category, question: 'Fix the ambiguity: "The researchers analyzed the samples and found that they contained mercury." (Who/what is "they"?)', options, correctIndex, explanation: 'Replace the ambiguous "they" with the specific noun ("samples") for clarity — without introducing a new pronoun that has no antecedent or does not agree in number.' } } },
  { id: 'pa-q12', category: 'Ambiguous Reference', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('"This" has no single, clear antecedent', ['"This" should be "these" to match two events', '"Was" should be "were" to match two clauses', 'Starting a sentence with "This" is too informal']); return { id: this.id, category: this.category, question: '"The policy changed, and the budget was cut. This was controversial." What\'s wrong?', options, correctIndex, explanation: '"This" is vague — does it refer to the policy change, the budget cut, or both? Clarify.' } } },
  // Pronoun Case
  { id: 'pa-q13', category: 'Pronoun Case', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('me', ['I', 'myself', 'mine']); return { id: this.id, category: this.category, question: '"Between you and ___." Correct pronoun?', options, correctIndex, explanation: 'After a preposition ("between"), use the object pronoun "me."' } } },
  { id: 'pa-q14', category: 'Pronoun Case', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('she and I', ['her and me', 'her and I', 'she and me']); return { id: this.id, category: this.category, question: '"___ went to the library." (Subject position)', options, correctIndex, explanation: 'Subject pronouns needed: "she" and "I." Test each alone: "She went" / "I went."' } } },
  { id: 'pa-q15', category: 'Pronoun Case', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('him and me', ['he and I', 'he and me', 'him and I']); return { id: this.id, category: this.category, question: '"The teacher gave the books to ___." (Object position)', options, correctIndex, explanation: 'After a preposition ("to"), use object pronouns: "him and me."' } } },
  { id: 'pa-q16', category: 'Pronoun Case', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('The form a pronoun takes to show its role', ['Whether a pronoun is singular or plural in form', 'Whether a pronoun is capitalized, as in "I"', 'The gender a pronoun shows, such as "he" or "she"']); return { id: this.id, category: this.category, question: 'What is pronoun case?', options, correctIndex, explanation: 'Case = the form of a pronoun based on its function in the sentence: subject (I, he, she, we, they) or object (me, him, her, us, them). Singular/plural is number, not case.' } } },
  { id: 'pa-q17', category: 'Pronoun Case', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('whom', ['who', 'whose', 'whoever']); return { id: this.id, category: this.category, question: '"To ___ should I address this letter?" Correct pronoun?', options, correctIndex, explanation: 'After a preposition ("to"), use object form: "whom."' } } },
  // Reflexive Pronouns
  { id: 'pa-q18', category: 'Reflexive Pronouns', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('me', ['myself', 'I', 'mine']); return { id: this.id, category: this.category, question: '"The director asked Priya and ___ to revise the script." Which choice conforms to the conventions of Standard English?', options, correctIndex, explanation: 'The pronoun is an OBJECT of "asked," so use "me." "Myself" is only correct when the subject and object are the same person ("I asked myself").' } } },
  { id: 'pa-q19', category: 'Reflexive Pronouns', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('me', ['myself', 'I', 'mine']); return { id: this.id, category: this.category, question: '"Please give the report to John and ___." (Not reflexive — different subject)', options, correctIndex, explanation: '"Myself" is wrong here because the subject ("you") is different. Use object pronoun "me."' } } },
  { id: 'pa-q20', category: 'Reflexive Pronouns', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('herself', ['she', 'hers', 'herselves']); return { id: this.id, category: this.category, question: '"She prepared ___ for the interview." Correct pronoun?', options, correctIndex, explanation: 'Subject acts on itself ("she...herself") → reflexive pronoun is correct. "Herselves" is not a word.' } } },
  { id: 'pa-q21', category: 'Reflexive Pronouns', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('No; "myself" cannot act as a subject', ['Yes; "myself" is more polite than "I"', 'Yes; "myself" works beside a name', 'No; use "Me and John will attend"']); return { id: this.id, category: this.category, question: '"Myself and John will attend." Is this correct?', options, correctIndex, explanation: '"Myself" cannot be a subject, and neither can the object pronoun "me." Correct: "John and I will attend."' } } },
  // Relative Pronouns
  { id: 'pa-q22', category: 'Relative Pronouns', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('who', ['which', 'whose', 'whom']); return { id: this.id, category: this.category, question: '"The doctor ___ treated me was very kind." Correct relative pronoun?', options, correctIndex, explanation: '"Who" is used for people in subject position.' } } },
  { id: 'pa-q23', category: 'Relative Pronouns', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('which', ['who', 'whom', 'whose']); return { id: this.id, category: this.category, question: '"The car, ___ was parked outside, had a flat tire." Correct relative pronoun? (Nonrestrictive)', options, correctIndex, explanation: '"Which" is used for things in nonrestrictive clauses (with commas).' } } },
  { id: 'pa-q24', category: 'Relative Pronouns', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('that', ['whose', 'who', 'whom']); return { id: this.id, category: this.category, question: '"The book ___ I read was excellent." Correct relative pronoun? (Restrictive)', options, correctIndex, explanation: '"That" is used for things in restrictive (essential) clauses (no commas). "Who" and "whom" refer to people, and "whose" shows possession.' } } },
  { id: 'pa-q25', category: 'Relative Pronouns', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('whom', ['who', 'which', 'whose']); return { id: this.id, category: this.category, question: '"The candidate ___ the voters selected won the election." Correct pronoun?', options, correctIndex, explanation: '"Whom" is the object of "selected" — test by replacing: "the voters selected him."' } } },
  { id: 'pa-q26', category: 'Relative Pronouns', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('whose', ["who's", 'whom', 'which']); return { id: this.id, category: this.category, question: '"The author ___ book was banned gave a speech." Correct pronoun?', options, correctIndex, explanation: '"Whose" = possessive. "Who\'s" = "who is."' } } },
  // Pronoun Shift
  { id: 'pa-q27', category: 'Pronoun Shift', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('they find', ['you find', 'one finds', 'we find']); return { id: this.id, category: this.category, question: '"Students should study hard so that ___ the answers." If the passage uses third person:', options, correctIndex, explanation: 'Maintain consistent point of view — "students" (third person plural) → "they."' } } },
  { id: 'pa-q28', category: 'Pronoun Shift', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('one must', ['you must', 'they must', 'we must']); return { id: this.id, category: this.category, question: '"If one wants to master an instrument, ___ practice daily." Which choice maintains a consistent pronoun?', options, correctIndex, explanation: 'The sentence begins with "one," so it must continue with "one" — switching to "you" or "they" is a pronoun shift.' } } },
  { id: 'pa-q29', category: 'Pronoun Shift', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('their', ['your', 'our', "they're"]); return { id: this.id, category: this.category, question: '"Students who complete the internship receive credit toward ___ degrees." Which choice conforms to the conventions of Standard English?', options, correctIndex, explanation: 'The antecedent is "students" (third person plural), so "their" — shifting to "your" changes person mid-sentence, and "they\'re" means "they are."' } } },
  { id: 'pa-q30', category: 'Pronoun Shift', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('one needs', ['you need', 'they need', 'we need']); return { id: this.id, category: this.category, question: '"When one studies for the SAT, ___ to focus on weak areas." (Maintain "one")', options, correctIndex, explanation: 'Started with "one" → stay with "one needs" for consistency.' } } },
  // SAT-Style
  { id: 'pa-q31', category: 'SAT-Style', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('their', ['its', 'his or her', 'our']); return { id: this.id, category: this.category, question: '"The team celebrated ___ victory." (Team acting as individuals celebrating)', options, correctIndex, explanation: 'When team members act individually (each celebrating), "their" is appropriate.' } } },
  { id: 'pa-q32', category: 'SAT-Style', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('he or she', ['they', 'him or her', 'his or her']); return { id: this.id, category: this.category, question: '"If a driver sees a red light, ___ must stop." (Formal, singular)', options, correctIndex, explanation: '"A driver" is singular and the pronoun is the subject of "must stop" → formal agreement requires the subject form "he or she."' } } },
  { id: 'pa-q33', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Replace the unclear pronoun with the noun it means', ['Replace the unclear pronoun with a reflexive one', 'Move the pronoun nearer to the sentence start', 'Add a second pronoun that refers to the same noun']); return { id: this.id, category: this.category, question: 'The SAT often tests unclear pronoun reference. What\'s the best fix?', options, correctIndex, explanation: 'Replace vague pronouns with specific nouns to eliminate ambiguity.' } } },
  { id: 'pa-q34', category: 'SAT-Style', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('who', ['whom', 'which', 'whose']); return { id: this.id, category: this.category, question: '"Scientists ___ study climate change warn about rising temperatures." Correct pronoun?', options, correctIndex, explanation: '"Who" = subject of "study." (Test: "they study" = subject form.)' } } },
  { id: 'pa-q35', category: 'SAT-Style', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('I', ['me', 'myself', 'mine']); return { id: this.id, category: this.category, question: '"It was ___ who made the decision." Correct pronoun after "was"? (Formal)', options, correctIndex, explanation: 'After a linking verb ("was"), use the subject pronoun: "It was I." (Formal)' } } },
  { id: 'pa-q36', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Whether it clearly refers to one specific noun', ['Whether it is closer to the verb than to the noun', 'Whether it matches the number of the nearest verb', 'Whether it appears at the start of its sentence']); return { id: this.id, category: this.category, question: 'What should you always check when you see a pronoun underlined on the SAT?', options, correctIndex, explanation: 'For every underlined pronoun: check agreement with its antecedent (number), case, and clear reference.' } } },
  { id: 'pa-q37', category: 'Number Agreement', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('her', ['their', 'hers', 'his']); return { id: this.id, category: this.category, question: '"Neither of the girls forgot ___ lunch." Correct pronoun? (Formal)', options, correctIndex, explanation: '"Neither" is singular. Context indicates female → "her." "Hers" stands alone and cannot come before a noun.' } } },
  { id: 'pa-q38', category: 'Ambiguous Reference', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Add a noun after it, as in "This tax increase"', ['Change "This" to "It" for a clearer link', 'Join the sentences with a comma before "this"', 'Move "This was controversial" to the start']); return { id: this.id, category: this.category, question: '"The law was amended and taxes were raised. This was controversial." Fix the vague "this."', options, correctIndex, explanation: 'Vague "this" should be followed by a specific noun to clarify what was controversial. "It" is just as vague, and a comma would create a splice.' } } },
  { id: 'pa-q39', category: 'Pronoun Case', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('whoever', ['whomever', 'who', 'whom']); return { id: this.id, category: this.category, question: '"___ scores the highest will win the prize." Correct pronoun?', options, correctIndex, explanation: '"Whoever" is the subject of "scores" — use subject case.' } } },
  { id: 'pa-q40', category: 'Pronoun Shift', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('you need to practice consistently', ['one needs to practice consistently', 'a person needs to practice consistently', 'they need to practice consistently']); return { id: this.id, category: this.category, question: '"To improve your SAT score, ___ ." If the passage uses second person ("your"):', options, correctIndex, explanation: 'Maintain second person throughout: "your" → "you need to practice."' } } },
]

function selectTemplates(count: number, difficulty?: 'easy' | 'medium' | 'hard'): QuestionTemplate[] {
  if (difficulty) {
    const fillOrder: Record<'easy' | 'medium' | 'hard', ('easy' | 'medium' | 'hard')[]> = { easy: ['easy', 'medium', 'hard'], medium: ['medium', 'easy', 'hard'], hard: ['hard', 'medium', 'easy'] }
    const selected: QuestionTemplate[] = []
    for (const tier of fillOrder[difficulty]) { if (selected.length >= count) break; for (const q of shuffle(questionPool.filter(t => t.difficulty === tier))) { if (selected.length >= count) break; selected.push(q) } }
    return selected
  }
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return selected
}

export function generateExitQuiz(count: number = 10, _topicSlug?: string, difficulty?: 'easy' | 'medium' | 'hard'): ExitQuizQuestion[] {
  return shuffle(selectTemplates(count, difficulty)).map(t => ({ ...t.generate(), difficulty: t.difficulty }))
}
