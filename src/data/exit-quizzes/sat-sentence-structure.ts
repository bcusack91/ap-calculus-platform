/**
 * Exit Quiz — SAT Sentence Structure
 * 40 randomized questions on run-ons, fragments, subordination, coordination, sentence types
 */
export interface ExitQuizQuestion { id: string; question: string; options: string[]; correctIndex: number; explanation: string; category: string; difficulty?: 'easy' | 'medium' | 'hard' }
interface QuestionTemplate { id: string; category: string; difficulty: 'easy' | 'medium' | 'hard'; generate: () => ExitQuizQuestion }
function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]] }; return a }
function makeStringOptions(c: string, o: string[]) { const u = [...new Set(o)].filter(x => x !== c).slice(0, 3); const fillers = ['No change', 'Cannot be determined', 'None of these']; for (const f of fillers) { if (u.length >= 3) break; if (f !== c && !u.includes(f)) u.push(f) } const all = shuffle([c, ...u]); return { options: all, correctIndex: all.indexOf(c) } }
function pick<T>(arr: T[]): T { return arr[Math.floor(Math.random() * arr.length)] }

const questionPool: QuestionTemplate[] = [
  // Run-On Sentences
  { id: 'ss-q1', category: 'Run-On Sentences', difficulty: 'medium', generate() { const subj = pick(['The experiment failed', 'The team won the championship', 'The deadline passed']); const clause2 = pick(['the researchers tried again', 'the fans celebrated', 'the project was incomplete']); const { options, correctIndex } = makeStringOptions(`${subj}; ${clause2}.`, [`${subj} ${clause2}.`, `${subj}, ${clause2}.`, `${subj} and, ${clause2}.`]); return { id: this.id, category: this.category, question: `Which correctly fixes this run-on: "${subj} ${clause2}."?`, options, correctIndex, explanation: 'A semicolon correctly joins two independent clauses without a conjunction.' } } },
  { id: 'ss-q2', category: 'Run-On Sentences', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('The storm knocked out power; crews worked through the night.', ['The storm knocked out power crews worked through the night.', 'The storm knocked out power, crews worked through the night.', 'The storm knocked out power crews, worked through the night.']); return { id: this.id, category: this.category, question: 'Which choice correctly joins the two ideas?\n\n"The storm knocked out power. Crews worked through the night."', options, correctIndex, explanation: 'A semicolon properly joins two related independent clauses; the other choices are run-ons or comma splices.' } } },
  { id: 'ss-q3', category: 'Run-On Sentences', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('The recipe calls for basil, but we only had oregano.', ['The recipe calls for basil, we only had oregano.', 'The recipe calls for basil we only had oregano.', 'The recipe, calls for basil we only had oregano.']); return { id: this.id, category: this.category, question: 'Which choice conforms to the conventions of Standard English?', options, correctIndex, explanation: 'Two independent clauses need a comma PLUS a conjunction ("but"). A comma alone is a comma splice.' } } },
  { id: 'ss-q4', category: 'Run-On Sentences', difficulty: 'medium', generate() { const _fix = pick(['Add a period', 'Add a semicolon', 'Add a comma + conjunction']); const { options, correctIndex } = makeStringOptions('All three are valid ways to fix a run-on', ['Only periods fix run-ons', 'Only semicolons fix run-ons', 'Run-ons cannot be fixed']); return { id: this.id, category: this.category, question: `"Add a period," "add a semicolon," or "add a comma + conjunction" — which fixes a run-on?`, options, correctIndex, explanation: 'Run-ons can be fixed with a period, semicolon, or comma + coordinating conjunction.' } } },
  { id: 'ss-q5', category: 'Run-On Sentences', difficulty: 'medium', generate() { const sentence = pick(['I love reading, it helps me relax.', 'She studied hard, she passed the test.', 'The sun set, the stars appeared.']); const parts = sentence.split(', '); const { options, correctIndex } = makeStringOptions(`${parts[0]}; ${parts[1]}`, [`${parts[0]} ${parts[1]}`, sentence, `${parts[0]}. and ${parts[1]}`]); return { id: this.id, category: this.category, question: `Fix the comma splice: "${sentence}"`, options, correctIndex, explanation: 'Replace the comma with a semicolon to properly join two independent clauses.' } } },
  // Fragments
  { id: 'ss-q6', category: 'Fragments', difficulty: 'medium', generate() { const frag = pick(['Because the weather was cold.', 'Running through the park on a sunny day.', 'Which was the best option available.']); const { options, correctIndex } = makeStringOptions('It lacks an independent clause — it cannot stand alone', ['It is too short', 'It has too many words', 'It has no adjectives']); return { id: this.id, category: this.category, question: `Why is "${frag}" a sentence fragment?`, options, correctIndex, explanation: 'A fragment lacks a complete independent clause (subject + verb + complete thought).' } } },
  { id: 'ss-q7', category: 'Fragments', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Because the museum closed early, we missed the exhibit.', ['Because the museum closed early. We missed the exhibit.', 'Because the museum closed early; we missed the exhibit was missed.', 'The museum closed early because.']); return { id: this.id, category: this.category, question: 'Which choice avoids a sentence fragment?', options, correctIndex, explanation: 'A "because" clause is dependent — it must attach to an independent clause, not stand alone as its own sentence.' } } },
  { id: 'ss-q8', category: 'Fragments', difficulty: 'medium', generate() { const frag = pick(['Although she studied hard.', 'While the team practiced.', 'Since the project began.']); const fix = frag === 'Although she studied hard.' ? 'Although she studied hard, she didn\'t pass.' : frag === 'While the team practiced.' ? 'While the team practiced, the coach watched.' : 'Since the project began, costs have risen.'; const { options, correctIndex } = makeStringOptions(fix, [frag, frag.replace('.', ', and'), frag.replace('.', '!')]); return { id: this.id, category: this.category, question: `Fix this fragment: "${frag}"`, options, correctIndex, explanation: 'A subordinate clause needs to be attached to an independent clause.' } } },
  { id: 'ss-q9', category: 'Fragments', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('A group of words that looks like a sentence but lacks a subject, verb, or complete thought', ['Any sentence under five words', 'A sentence with a period', 'A dependent clause with a comma']); return { id: this.id, category: this.category, question: 'What defines a sentence fragment?', options, correctIndex, explanation: 'Fragments are incomplete — missing a subject, verb, or a complete thought.' } } },
  { id: 'ss-q10', category: 'Fragments', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Yes, if used intentionally for rhetorical effect (rare on SAT)', ['Never — fragments are always wrong', 'Only in poetry', 'Only in dialogue']); return { id: this.id, category: this.category, question: 'Can a fragment ever be acceptable in writing?', options, correctIndex, explanation: 'Fragments can be used intentionally for effect, but the SAT usually tests correcting them.' } } },
  // Subordination
  { id: 'ss-q11', category: 'Subordination', difficulty: 'easy', generate() { const sub = pick(['because', 'although', 'since', 'while', 'unless']); const { options, correctIndex } = makeStringOptions('It introduces a dependent clause that cannot stand alone', ['It joins two independent clauses equally', 'It ends a sentence', 'It replaces a noun']); return { id: this.id, category: this.category, question: `What does the subordinating conjunction "${sub}" do?`, options, correctIndex, explanation: 'Subordinating conjunctions create dependent clauses — they show an unequal relationship.' } } },
  { id: 'ss-q12', category: 'Subordination', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('because, although, since, while, if, unless, when, after, before', ['and, but, or, so', 'however, therefore, moreover', 'the, a, an']); return { id: this.id, category: this.category, question: 'Which of these are subordinating conjunctions?', options, correctIndex, explanation: 'Subordinating conjunctions (because, although, since, etc.) introduce dependent clauses.' } } },
  { id: 'ss-q13', category: 'Subordination', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Although she was tired, she finished the race.', ['Although she was tired. She finished the race.', 'She was tired, although she finished the race.', 'Although, she was tired she finished the race.']); return { id: this.id, category: this.category, question: 'Which sentence correctly uses subordination?', options, correctIndex, explanation: 'The dependent clause ("Although she was tired") + comma + independent clause is correct.' } } },
  { id: 'ss-q14', category: 'Subordination', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Use a comma after the dependent clause when it comes first', ['Never use commas with subordination', 'Always put the dependent clause last', 'Use a semicolon before the dependent clause']); return { id: this.id, category: this.category, question: 'When a subordinate clause begins a sentence, what punctuation rule applies?', options, correctIndex, explanation: 'Dependent clause first → comma → independent clause.' } } },
  { id: 'ss-q15', category: 'Subordination', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Subordination shows one idea depends on or is less important than another', ['Both ideas are equally important', 'Both ideas are independent', 'The sentence becomes a fragment']); return { id: this.id, category: this.category, question: 'What relationship does subordination create between ideas?', options, correctIndex, explanation: 'Subordination makes one clause dependent on the other — showing hierarchy of ideas.' } } },
  // Coordination
  { id: 'ss-q16', category: 'Coordination', difficulty: 'easy', generate() { const conj = pick(['and', 'but', 'or', 'so', 'yet']); const { options, correctIndex } = makeStringOptions('FANBOYS: for, and, nor, but, or, yet, so', ['because, although, since', 'however, therefore, moreover', 'who, which, that']); return { id: this.id, category: this.category, question: `"${conj}" is a coordinating conjunction. What is the full FANBOYS list?`, options, correctIndex, explanation: 'FANBOYS = for, and, nor, but, or, yet, so — the coordinating conjunctions.' } } },
  { id: 'ss-q17', category: 'Coordination', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('It joins two independent clauses of equal importance', ['It creates a dependent clause', 'It introduces a fragment', 'It replaces a semicolon always']); return { id: this.id, category: this.category, question: 'What does coordination do in a sentence?', options, correctIndex, explanation: 'Coordination joins elements of equal grammatical rank — typically two independent clauses.' } } },
  { id: 'ss-q18', category: 'Coordination', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('She studied hard, and she passed the exam.', ['She studied hard and, she passed the exam.', 'She studied hard, she passed the exam.', 'She studied hard and she, passed the exam.']); return { id: this.id, category: this.category, question: 'Which sentence correctly uses a coordinating conjunction?', options, correctIndex, explanation: 'Comma + coordinating conjunction correctly joins two independent clauses.' } } },
  { id: 'ss-q19', category: 'Coordination', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Use a comma before the coordinating conjunction when joining independent clauses', ['Never use a comma with coordinating conjunctions', 'Always use a semicolon instead', 'Put the comma after the conjunction']); return { id: this.id, category: this.category, question: 'What punctuation rule applies when using coordinating conjunctions to join clauses?', options, correctIndex, explanation: 'Independent clause + comma + FANBOYS + independent clause.' } } },
  // Sentence Types
  { id: 'ss-q20', category: 'Sentence Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Simple, compound, complex, compound-complex', ['Simple and compound only', 'Short, medium, long, very long', 'Statement, question, command, exclamation']); return { id: this.id, category: this.category, question: 'What are the four sentence types by structure?', options, correctIndex, explanation: 'The four structural types: simple, compound, complex, and compound-complex.' } } },
  { id: 'ss-q21', category: 'Sentence Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('One independent clause', ['Two independent clauses', 'One dependent and one independent clause', 'No clauses']); return { id: this.id, category: this.category, question: 'What does a simple sentence contain?', options, correctIndex, explanation: 'A simple sentence has exactly one independent clause (may have compound subjects/verbs).' } } },
  { id: 'ss-q22', category: 'Sentence Types', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('Two or more independent clauses joined by a conjunction or semicolon', ['One independent clause', 'One independent and one dependent clause', 'Only dependent clauses']); return { id: this.id, category: this.category, question: 'What defines a compound sentence?', options, correctIndex, explanation: 'Compound = two+ independent clauses joined by a conjunction or semicolon.' } } },
  { id: 'ss-q23', category: 'Sentence Types', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('One independent clause and at least one dependent clause', ['Two independent clauses', 'One independent clause only', 'Two dependent clauses']); return { id: this.id, category: this.category, question: 'What defines a complex sentence?', options, correctIndex, explanation: 'Complex = one independent clause + one or more dependent clauses.' } } },
  // Clause Identification
  { id: 'ss-q24', category: 'Clause Identification', difficulty: 'medium', generate() { const clause = pick(['when the bell rang', 'because it was raining', 'that she mentioned']); const { options, correctIndex } = makeStringOptions('Dependent clause', ['Independent clause', 'Phrase', 'Fragment that should be deleted']); return { id: this.id, category: this.category, question: `Identify: "${clause}"`, options, correctIndex, explanation: 'It begins with a subordinating word and cannot stand alone — it\'s a dependent clause.' } } },
  { id: 'ss-q25', category: 'Clause Identification', difficulty: 'medium', generate() { const clause = pick(['The students completed the assignment', 'She runs every morning', 'The results were surprising']); const { options, correctIndex } = makeStringOptions('Independent clause', ['Dependent clause', 'Phrase', 'Fragment']); return { id: this.id, category: this.category, question: `Identify: "${clause}"`, options, correctIndex, explanation: 'It has a subject and verb and expresses a complete thought — independent clause.' } } },
  { id: 'ss-q26', category: 'Clause Identification', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('"after the concert ended" — it has a subject and a verb but cannot stand alone', ['"after the concert ended" — it is a complete sentence', '"in the crowded lobby" — it has a subject and verb', '"waiting by the door" — it is an independent clause']); return { id: this.id, category: this.category, question: 'In "We found our friends in the crowded lobby after the concert ended," which is a DEPENDENT CLAUSE, and why?', options, correctIndex, explanation: '"After the concert ended" has a subject (concert) and verb (ended) — a clause — but the word "after" keeps it from standing alone. "In the crowded lobby" is a phrase (no subject-verb).' } } },
  { id: 'ss-q27', category: 'Clause Identification', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('who, which, that', ['and, but, or', 'because, although, since', 'however, therefore, moreover']); return { id: this.id, category: this.category, question: 'Which words typically begin a relative clause?', options, correctIndex, explanation: 'Relative clauses begin with relative pronouns: who, whom, whose, which, that.' } } },
  // Combining Sentences
  { id: 'ss-q28', category: 'Combining Sentences', difficulty: 'hard', generate() { const s1 = pick(['The movie was long.', 'The test was difficult.', 'The road was narrow.']); const s2 = s1.includes('movie') ? 'It was entertaining.' : s1.includes('test') ? 'Most students passed.' : 'It was scenic.'; const combined = s1.includes('movie') ? 'The movie was long but entertaining.' : s1.includes('test') ? 'Although the test was difficult, most students passed.' : 'The road was narrow but scenic.'; const { options, correctIndex } = makeStringOptions(combined, [`${s1} ${s2}`, `${s1.replace('.', ',')} ${s2.toLowerCase()}`, `${s1.replace('.', '')} and ${s2.toLowerCase()}`]); return { id: this.id, category: this.category, question: `Combine: "${s1}" and "${s2}" — which is most effective?`, options, correctIndex, explanation: 'Combining with a conjunction or subordination creates a more fluid, effective sentence.' } } },
  { id: 'ss-q29', category: 'Combining Sentences', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('To improve flow, reduce choppiness, and show relationships between ideas', ['To make sentences longer', 'To add more commas', 'To impress the reader with complexity']); return { id: this.id, category: this.category, question: 'Why does the SAT test sentence combining?', options, correctIndex, explanation: 'Combining shows you understand how ideas relate and can express them fluently.' } } },
  { id: 'ss-q30', category: 'Combining Sentences', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Use subordination, coordination, or appositive phrases', ['Just put "and" between every sentence', 'Make one long run-on', 'Delete one sentence']); return { id: this.id, category: this.category, question: 'What are effective techniques for combining sentences?', options, correctIndex, explanation: 'Subordination, coordination, appositives, and participial phrases are key combining techniques.' } } },
  // SAT-Style
  { id: 'ss-q31', category: 'SAT-Style', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('The researchers, who had studied the phenomenon for years, published their findings.', ['The researchers who had studied the phenomenon for years published their findings.', 'The researchers, who had studied the phenomenon for years published their findings.', 'The researchers who had studied the phenomenon for years, published their findings.']); return { id: this.id, category: this.category, question: 'Which uses commas correctly with a nonrestrictive clause?', options, correctIndex, explanation: 'Nonrestrictive (extra info) clauses need commas on both sides.' } } },
  { id: 'ss-q32', category: 'SAT-Style', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Because the data was inconclusive, the team repeated the experiment.', ['Because, the data was inconclusive the team repeated the experiment.', 'The data was inconclusive, because the team repeated the experiment.', 'Because the data was inconclusive the team, repeated the experiment.']); return { id: this.id, category: this.category, question: 'Which sentence is correctly structured?', options, correctIndex, explanation: 'Dependent clause first → comma → independent clause is the correct pattern.' } } },
  { id: 'ss-q33', category: 'SAT-Style', difficulty: 'easy', generate() { const { options, correctIndex } = makeStringOptions('However, the results were unexpected.', ['However the results, were unexpected.', 'The results however were unexpected.', 'The results, were however unexpected.']); return { id: this.id, category: this.category, question: 'Which correctly punctuates a conjunctive adverb at the start of a sentence?', options, correctIndex, explanation: 'Conjunctive adverbs at the start of a sentence are followed by a comma.' } } },
  { id: 'ss-q34', category: 'SAT-Style', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('A semicolon', ['A comma', 'Nothing', 'A colon']); return { id: this.id, category: this.category, question: '"The study was groundbreaking ___ it changed the entire field." What punctuation joins these clauses?', options, correctIndex, explanation: 'A semicolon correctly joins two closely related independent clauses.' } } },
  { id: 'ss-q35', category: 'SAT-Style', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Not only did she excel in math, but she also mastered science.', ['Not only she excelled in math, but also she mastered science.', 'Not only did she excel in math but she also mastered science.', 'Not only did she excel in math, but, she also mastered science.']); return { id: this.id, category: this.category, question: 'Which correctly uses the correlative conjunction "not only...but also"?', options, correctIndex, explanation: '"Not only...but also" requires parallel structure and proper comma placement.' } } },
  { id: 'ss-q36', category: 'SAT-Style', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('It creates variety and helps emphasize key ideas', ['It is never necessary', 'Simple sentences are always better', 'Only complex sentences should be used']); return { id: this.id, category: this.category, question: 'Why is varying sentence structure important in writing?', options, correctIndex, explanation: 'Varied structure creates rhythm, emphasis, and engagement for the reader.' } } },
  { id: 'ss-q37', category: 'Combining Sentences', difficulty: 'medium', generate() { const { options, correctIndex } = makeStringOptions('Marie Curie, a pioneering physicist, discovered radium.', ['Marie Curie a pioneering physicist discovered radium.', 'Marie Curie, a pioneering physicist discovered radium.', 'Marie Curie a pioneering physicist, discovered radium.']); return { id: this.id, category: this.category, question: 'Which correctly combines "Marie Curie discovered radium" with the fact that she was a pioneering physicist?', options, correctIndex, explanation: 'An appositive phrase set off by two commas combines the information smoothly.' } } },
  { id: 'ss-q38', category: 'Clause Identification', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Adjective clause', ['Adverb clause', 'Noun clause', 'Independent clause']); return { id: this.id, category: this.category, question: '"The book that I borrowed was fascinating." What type of clause is "that I borrowed"?', options, correctIndex, explanation: '"That I borrowed" modifies "book" — it\'s an adjective (relative) clause.' } } },
  { id: 'ss-q39', category: 'Subordination', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Although the trail was steep, the hikers reached the summit.', ['The trail was steep, the hikers reached the summit.', 'The trail was steep and the hikers, reached the summit.', 'The trail was steep the hikers reached the summit.']); return { id: this.id, category: this.category, question: 'Which choice best combines the sentences to show CONTRAST?\n\n"The trail was steep. The hikers reached the summit."', options, correctIndex, explanation: '"Although" subordinates the first idea and signals contrast; the other options are comma splices or run-ons.' } } },
  { id: 'ss-q40', category: 'Sentence Types', difficulty: 'hard', generate() { const { options, correctIndex } = makeStringOptions('Compound-complex', ['Simple', 'Compound', 'Complex']); return { id: this.id, category: this.category, question: '"Although he was tired, he finished the report, and he submitted it on time." What sentence type is this?', options, correctIndex, explanation: 'It has a dependent clause ("Although...") and two independent clauses joined by "and" — compound-complex.' } } },
]

function selectTemplates(count: number, difficulty?: 'easy' | 'medium' | 'hard'): QuestionTemplate[] {
  if (difficulty) {
    const fillOrder: Record<'easy' | 'medium' | 'hard', ('easy' | 'medium' | 'hard')[]> = { easy: ['easy', 'medium', 'hard'], medium: ['medium', 'easy', 'hard'], hard: ['hard', 'medium', 'easy'] }
    const selected: QuestionTemplate[] = []
    for (const tier of fillOrder[difficulty]) { if (selected.length >= count) break; for (const q of shuffle(questionPool.filter(t => t.difficulty === tier))) { if (selected.length >= count) break; selected.push(q) } }
    return selected
  }
  const byCategory: Record<string, QuestionTemplate[]> = {}
  for (const q of questionPool) { if (!byCategory[q.category]) byCategory[q.category] = []; byCategory[q.category].push(q) }
  const selected: QuestionTemplate[] = []; const usedIds = new Set<string>()
  for (const cat of shuffle(Object.keys(byCategory))) { if (selected.length >= count) break; const pool = byCategory[cat]; const q = pool[Math.floor(Math.random() * pool.length)]; if (!usedIds.has(q.id)) { selected.push(q); usedIds.add(q.id) } }
  const remaining = questionPool.filter(q => !usedIds.has(q.id))
  for (const q of shuffle(remaining)) { if (selected.length >= count) break; selected.push(q); usedIds.add(q.id) }
  return selected
}

export function generateExitQuiz(count: number = 10, _topicSlug?: string, difficulty?: 'easy' | 'medium' | 'hard'): ExitQuizQuestion[] {
  return shuffle(selectTemplates(count, difficulty)).map(t => ({ ...t.generate(), difficulty: t.difficulty }))
}
